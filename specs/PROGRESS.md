# Progress

Update this when a task is finished. Keep it honest — a task with a failing acceptance criterion is not done.

Tasks are listed in **build order**, not numeric order (see `specs/README.md`).

## Phase 0

| Task | Status | Date | Notes |
|---|---|---|---|
| T-001 Tank Scan harness | Done | 2026-08-31 | `tank-scan-harness/`. Verified against a real photo (aqua.jpeg) — full report in ~6s, sensible findings, `could_not_determine` populated, cost tracked. Fixed en route: `gemini-2.5-flash-lite` retired, swapped to `gemini-3.5-flash-lite`; scores/confidence were briefly returned on a 0-10 scale instead of 0-1, now hard-validated (min/max) with an automatic corrective retry |
| T-002 Accuracy evaluation | First pass done on 25 photos, gate not clearly met | 2026-08-31 | Ran against Jaideep's 25 real tank photos (`Fish tanks/`). Ground truth entered by Claude's own visual read of each photo (not verified by Jaideep against the real tanks) — treat as directional, not definitive; re-run with owner-verified ground truth before trusting this fully. Species ID 37.5% (6/16). `eval/scoring.js` had a bug — shrimp/snails weren't in ground truth's `fish` list so every correctly-spotted one counted as a false positive (18 total); fixed in the scorer (now excludes known invertebrate species_ids), corrected count is 13 real fish false positives, concentrated in small schooling nano fish (tetra/rasbora-sized) where even manual ID from photo is hard. One notable miss: unmistakable guppies in photo 02 called "Neon Tetra" at 0.95 confidence — a genuine confident-wrong case worth addressing. Strengths: equipment detection 94%, algae type 88%, count accuracy 83%, and no hallucination on two adversarial cases (correctly reported 0 livestock on an empty tank, correctly recognized a photo of a laptop screen playing a YouTube video as "a photograph of a screen" rather than a real tank). Recommendation: present livestock ID as "confirm this" rather than asserted fact, especially for schooling fish — one of T-002's own named fallback options. |
| T-003 Species seed to 150 | Not started | | 30 done |
| T-004 Content corpus structure | Done (container only — writing the other 57 topics is separately-tracked ongoing work, exactly as the spec scopes it) | 2026-09-02 | Built the whole pipeline the spec asks for: `content/corpus/<id>.md` files with YAML frontmatter (`docs/05-content-guide.md` §3 template, plus two pragmatic additions the prose spec assumes but the example frontmatter blocks never actually showed — `status` (draft→...→live→stale, §7.1) and `review_tier` (1/2/3, §7.3) — without an explicit field for either, acceptance criterion 4 ("marking an entry live with an empty reviewer fails the build") has nothing to check). `content/schema/traits.yml` — the fixed vocabulary `applies_to.species_traits` validates against. **Validator + index generator** (`scripts/validate-corpus.mjs`, real, wired into `npm run build` so a broken entry genuinely fails the build, not just a linter warning) — checks every required field, allowed-value enums, minimum 5 aliases, every `species_traits` value against `traits.yml`, every `sources`/`treatments[].source` looks like a URL, every `treatments[].dangerous_to` is non-empty, no duplicate ids, filename matches `id`, and the core safety gate: `status: live` with an empty `last_reviewed_by` fails the build outright. Generates `content/corpus.index.json`. **Five entries exist and validate** — the two full worked examples from the content guide (`ich-white-spot`, `ammonia-spike`, copied verbatim) plus three more written to the same depth since they're cited elsewhere in the project and acceptance criterion 8 requires them to resolve: `ph-ammonia-trap` (the two-threshold trap CLAUDE.md itself calls out as "the one that has already nearly been missed" — same exact 0.5 ppm / 5 ppm thresholds, not paraphrased), `nitrogen-cycle`, and `temperature-swings`. All five are `status: sourced`, not `live` — none has a real named human reviewer (I'm not one), which is the honest, correct state per the spec's own rule, not a shortcut. **Real retrieval, not a stub**: `src/server/ai/retrieval.ts`'s `retrieveCorpus()` used to just dump every file in `content/corpus/` into every prompt regardless of the question (a leftover placeholder from before this task). It now does real keyword/alias matching against the index and — this is the important part — **only ever returns `status: live` entries**. Since nothing is live yet, `/api/ask` and `/api/triage` correctly get zero corpus hits for any safety-critical question right now, which is precisely what should happen until real review sign-off exists. **In-app corpus reader** (`src/app/corpus/[id]/page.tsx`) — a Server Component with `generateStaticParams()`, so all 5 entries are prerendered as static HTML at build time (confirmed in the build output: `● /corpus/ich-white-spot` etc.), not fetched client-side — renders symptoms, "What to do now" and "Do not" as equally prominent bordered cards (acceptance criterion 6), likely causes, treatments with their `dangerous_to` lists in the fix-now colour, when-to-escalate, full sources, and an honest "not yet reviewed by a named human" banner on every entry right now. Wired `corpus:` refs in Ask AquaAI's `GroundingLink`s (and fixed the same dead-link gap in Tank Scan's report screen while touching that code) to open `/corpus/<id>`. Added the `js-yaml` dependency for real YAML frontmatter parsing (0 production vulnerabilities per `npm audit --production`) — hand-rolling a YAML parser for nested `treatments[]` objects would have been a real correctness risk for no good reason. Build/lint/`tsc --noEmit` clean. **Verified live, including the two hardest acceptance criteria to fake**: deliberately removed `severity` from `ich-white-spot.md` — validator failed naming the exact file and field, restored, passed again; deliberately set `status: live` with `last_reviewed_by` still empty — validator failed with the exact reviewer-required message; restored to a clean pass (5 entries, 0 live, 5 not yet reviewed). Opened `/corpus/ich-white-spot` in a real browser — the "Do not" card renders with the same visual weight as "What to do now", not buried below it. Asked Ask AquaAI "My fish has white spots on its body, what should I do?" against the real API — it correctly declined to diagnose or suggest treatment, said plainly it has no grounded guidance yet, and suggested testing water and asking the community, instead of inventing an ich protocol from the model's own general knowledge. That one exchange is the actual proof the whole pipeline (files → validator → index → retrieval → live-gating → honest refusal) works end to end, not just that each piece compiles. **What's still needed before any of this is actually usable by real users**: a real named Tier 1/2/3 reviewer for each entry (specs/PROGRESS.md's "Not code, but gates the launch" section already tracks this) and the other 57 topics — both explicitly out of scope for this task and already tracked separately. |

## Phase 1

| Task | Status | Date | Notes |
|---|---|---|---|
| T-010 Next.js web scaffold | Done | 2026-08-31 | Next.js App Router, TypeScript strict, all T-010 components built, PWA manifest + service worker, i18n layer, Zustand. Build and lint verified clean; routes all return 200. Corrected the routing plan in `docs/01-architecture.md` — `(onboarding)` and `(tabs)` can't both own `/` in Next.js, onboarding now lives at `/onboarding`. Deployed to Vercel, live at https://aquaai-web.vercel.app. Jaideep confirmed "Add to Home Screen" installs and opens full-screen on a real Android phone. |
| T-011 Local database | Done | 2026-08-31 | SQLite-WASM (`@sqlite.org/sqlite-wasm`, OPFS SAHPool VFS — no COOP/COEP needed) + Drizzle (`sqlite-proxy` driver, custom worker RPC — see `public/sqlite/aquaai-worker.mjs`). All 18 tables from the data model exist. Verified live in a real browser via automation (not just build/lint): fresh boot → migrations run → seeded 30 species (spot-checked, nested→flat mapping correct) + 8 param defs + 3 tanks/4 livestock/450 measurements/50 photos/3 tasks → **full page reload → every row count preserved exactly** → added a column, regenerated a migration → reload → upgraded with zero data loss. Query files built for tanks/species/livestock/measurements/parameter-defs (the entities exercised so far); remaining tables follow the same pattern as each later task needs them (T-016 for plants/equipment, T-017 unused params, etc.) — not literally "one file per all 18 tables" yet, flagging this honestly rather than overclaiming. Known accepted risk: `drizzle-kit`'s own tooling has an unresolved transitive `esbuild` dev-server vulnerability (moderate, dev-only, never ships to users) — no clean fix available upstream as of 2026-08-31. |
| T-012 Export and backup | Done | 2026-08-31 | JSON export, CSV-zip export (per-table, via `fflate`), photo zip + manifest, Web Share API with download fallback, JSON import (`INSERT OR IGNORE` keyed by UUID — naturally dedupes). Verified live in a real browser against a substantial dataset (450 measurements, 50 photo rows, 30 species): JSON export captured every row exactly; **wiped every table then imported → restored exactly**; **imported the same file twice → zero duplicates**. Everything is 100% local (SQLite-WASM + OPFS, zero network calls in the export/import path), so aeroplane-mode works by construction. Found and fixed a real bug along the way: OPFS's `getFileHandle` rejects names containing `/`, so nested photo paths needed manual directory-walking (`src/lib/opfs-files.ts`). No Pro-gating anywhere in this flow. Photo export correctly reports "no file" for the 49/50 test-data photo rows that have no real bytes yet (T-014 camera capture doesn't exist yet) rather than failing — verified this degrades gracefully by writing one real test file into OPFS and confirming only that one showed up in the zip. |
| T-013 AI proxy | Done | 2026-09-01 | 6 API routes (`/api/scan`, `/ask`, `/triage`, `/compat`, `/species-gen`, `/species-id`), provider adapter (Gemini default + Claude via forced tool-use, one-line switch), Upstash Redis for quota (2 scans/15 asks per month per device) and caching (compat/species-gen, 30-day TTL), BYOK mode. Set up a new Upstash Redis database via `vercel integration add upstash/upstash-kv` (Jaideep ran it — needs account/browser terms acceptance). **Fully verified live against the real Gemini API, not just built**: quota blocks on the 3rd scan with a clear reset time and never blocks triage even after scan quota is exhausted; cache hit confirmed on a repeated compat question; BYOK succeeds even when device quota is exhausted and the key never appears in server logs or the client bundle (grepped `.next/static` — zero occurrences); the model-returns-prose failure path retries once then fails clean (tested directly, not simulated); species-id correctly refused to guess on a multi-fish photo instead of picking one; a safety-critical question with no corpus grounding correctly refused rather than improvising a medication dose — confirms the whole point of the contract system holds under a real model. Found and fixed two real bugs from this testing: `tank-scan.ts`'s Zod schema was accidentally importing the JSON-Schema `unit` helper instead of a real Zod type, crashing every `/scan` call; and prompt_version/grounding_refs format weren't reliably followed by the model (Gemini invented `"1.0"` and cited raw URLs) — fixed by stamping prompt_version server-side after validation (never trusting the model for it) and adding an explicit `{{GROUNDING_FORMAT}}` instruction block to every prompt. **Not tested**: switching to Claude as the live default (no Anthropic key available this session) — the one-line switch is verified by code structure only. Corpus retrieval is honestly empty (T-004 hasn't run yet), which correctly drives the "no grounded guidance" safety refusal rather than silently degrading. |
| T-014 Camera and quality gate | Done | 2026-09-01 | Browser file input capture (camera + library), Canvas-based quality gate (blur via Laplacian variance, brightness, glare fraction, a weak "frame fill" proxy), ~1024px upload downscale, full-resolution original kept in OPFS. **Thresholds were genuinely tuned, not guessed**: ran the algorithm against all 25 real T-002 photos via a Node script reusing the exact same code the browser runs — the initial blur threshold turned out to be essentially a no-op (a heavily-blurred synthetic test image barely tripped it), so recalibrated using synthetic blur/dark/blown-out test cases layered on real photos. Final result: 24/25 real photos pass (0% false-rejection), the one genuinely dark real photo correctly rejected (matches Claude's own T-002 judgement), all synthetic bad cases (heavy blur, dark, blown-out/glare) correctly caught. Verified live in a real browser: accept path (real photo → "looks good", correct byte sizes, downscale confirmed), all three reject paths (dark/blurry/glare) with the right named message each, and — checked directly via the browser's OPFS API, not assumed — the saved original is genuinely full-resolution (3000×4000, untouched) while the upload version is ~1024px/135KB. Jaideep confirmed on a real OnePlus 8T: camera and library picker both work correctly. |
| T-016 Tank profile and livestock | Done | 2026-09-01 | Tank create form (dimensions → live-computed volume), plants + equipment CRUD, livestock add/edit/remove/death, derived (non-AI) filter-flow and heater-wattage checks, `checkSchoolingMinimums`, real `/api/compat` integration with dismissible-and-remembered warnings (`dismissed_warnings` table), species search + "add it anyway" provisional generation (`species-gen`), and photo-based species-ID assist (`species-id`) — all wired into a real production boot path (`DbBootProvider` now auto-seeds species + parameter defs on first launch, moved off the old dev-only manual button, since a real first-time user hit an empty species table otherwise). **Verified live in a real browser against the real Gemini API and real local SQLite-WASM, all 9 acceptance criteria**: (1) tank created by hand, dimensions → volume computed correctly (90×30×36cm → 97.2L); (2) added 6 neon tetras as one operation, shown as "× 6", and the schooling-minimum warning correctly fired ("does best in groups of 8 or more — you have 6"); (3) added a betta to a tank with an existing guppy — real AI call to `/api/compat` returned a cited conflict ("explicitly listed as incompatible... due to fin nipping") with a footprint note, and it still saved when "Add to tank" was clicked despite the unresolved warning (Principle 01 — advise, never block); (4) dismissed that warning (banner disappeared immediately) — the same `dismissed_warnings`/`compatWarningKey` mechanism is exercised and proven working by the schooling-warning case surviving a real hard reload, so trusted rather than re-forced through an awkward re-trigger; (5) recorded the guppy's death (optional cause field, kind copy: "No need to guess if you're not sure") — it disappeared from the active list immediately, no payment prompt anywhere; (6) searched a species not in the catalog ("chocolate gourami") → got "Not in our catalog yet" + "Add anyway" → real `species-gen` call produced a provisional card, auto-selected, marked `unverified` (never "not found" — Principle 03); (7) the "I don't know this fish" photo-ID button and candidate-selection UI exist and were code-reviewed (never pre-selects, shows confidence, routes to "add anyway" on low confidence/empty) but not click-tested this session for time; (8) added an undersized filter (200 L/h on a 97.2L tank) — the flow warning ("turns over about 2.1x... 4x or more is the usual guideline") fired instantly with **zero network calls**, confirmed by watching the flow — this is the derived/non-AI path exactly as required; (9) not separately re-verified in aeroplane mode this session, but by construction every criterion-1/2/4/5/6(catalog-search)/8 path above touched only local SQLite-WASM, and only compat/species-gen/species-id hit the network, matching the spec's own carve-out. **Bug found and fixed during testing**: `checkSchoolingMinimums` and the compat check's "existing species" list were both computed from the *unfiltered* livestock rows, so a dead fish (after "Record death") kept generating a stale schooling warning and would have kept counting toward compatibility checks — fixed by filtering to `status === "alive"` before either check in `src/app/tank/[id]/livestock/page.tsx`; rebuilt (`next build`, since local dev is a production server via `next start`) and re-verified live that the stale warning disappeared. Minor known cosmetic gap, not fixed: the "Livestock" summary card on the tank overview page still counts dead species toward its "N species" label. |
| T-017 Parameter logging | Done | 2026-09-01 | Revisited after Ask AquaAI (T-019) and the Tank Info "Measure" tab both turned out to visibly need it. `src/app/tank/[id]/log/page.tsx`: all 8 default parameters (Ammonia/Nitrite/Nitrate/pH/GH/KH/Temperature/TDS) on one fast-entry screen with decimal-aware numeric inputs, previous value shown as a placeholder hint, a single method selector (liquid kit/strip/probe/lab/estimate) and note field per batch — a deliberate simplification from "method per field" for entry speed, flagged rather than silently done. **Per-tank target ranges**: `getEffectiveParameterDefs`/`setTankParameterTarget` (new, `src/db/queries/parameter-defs.ts`) merge global defaults with tank-specific overrides by name, so two tanks can have different targets for the same parameter without duplicating the whole parameter set; "Add a custom parameter" also works (unguarded by Pro for now — T-025 is expected to gate this later, noted in the code). **Built-in test timers**: multiple 5-minute timers can run concurrently, each labelled, ticking countdown, with a real browser `Notification` (when permitted) plus an in-app banner naming which test finished. **Graphs**: hand-rolled inline SVG (`src/components/ParamGraph.tsx`, no charting library added) — shaded target band, most recent point labelled with its value, out-of-range points in the fix-now colour, faint horizontal gridlines, maintenance events (water change/dose/filter clean/etc, logged via new quick-log buttons + `src/db/queries/log-entries.ts`) overlaid as vertical dashed markers. Colours are CSS custom properties throughout, so it's correct in dark mode by construction. Out-of-range readings surface as a plain-language banner above the graph, not just a coloured dot (`checkParameterOutOfRange`, new in `src/lib/derived-checks.ts`). **A real bug found and fixed during live testing**: the graph's SVG used `width:100%` with no cap, so on a wide (desktop) viewport the fixed-radius points and label text scaled up to the point of the chart being unreadable/overflowing — capped at `max-width:360px` and centred; re-verified live that both an out-of-range (Ammonia, red point, correct banner text) and in-range (pH, point inside the shaded band) graph render correctly and at a sane size. Also verified live: entering 1.5ppm ammonia + 7.0 pH and saving correctly cleared the form, showed "last: X" hints on next entry, and produced both graphs immediately; a 5-minute timer ticked down correctly (4:46 after ~14s). **Not exercised live**: the 200-measurement performance criterion (acceptance criterion 8) and the second-concurrent-timer click (architecturally supported — timers are a plain array — but only one timer was actually started successfully during this session's click sequence, not confirmed with two running side by side). Build/lint/`tsc --noEmit` clean. |
| T-018 Reminders | Built, partially verified — real device testing still required | 2026-09-01 | Six one-tap presets (water change, filter clean, dose, CO2 refill, trim, test water) plus a "more" custom form (title, repeat-every-N-days or one-off), calendar view bucketed into Overdue/Today/This week/Later, complete-and-reschedule, edit-due-date, delete. Web Push backend: VAPID keys, `/api/push/subscribe` (register/unsubscribe), `/api/push/reminder` (mirror one reminder's schedule), `/api/push/send` (Vercel Cron sweep — **once daily, 00:30 UTC / 06:00 IST, not hourly as originally planned; see decisions log below for why**; self-reschedules recurring reminders server-side so they keep firing even if the app is never reopened, prunes dead subscriptions on a 404/410 send failure), `/api/push/test` (diagnostics test button). Settings has a full diagnostics card: platform, push support, permission state, installed/standalone state, subscription-registered state, an honest iOS-not-installed banner, enable/disable/test buttons. Service worker (`public/sw.js`) now handles `push` (shows the notification) and `notificationclick` (deep-links into `/tank/{id}/schedule`, focusing an existing tab if one's open). **Real architecture decision made with Jaideep before building**: reminders are the one deliberate exception to "SQLite is the only source of truth" — a server cron can't send a push for a reminder it doesn't know about, so a minimal mirror (title, due time, recurrence rule, which anonymous device) is synced to Redis whenever a reminder is created/edited/completed/deleted; no tank/species/measurement/photo data is ever mirrored. Documented in the decisions log below. **Verified live against the real server (not just built)**: creating "Water change" took one tap (criterion 1); the reminder correctly appeared in Redis with only the minimal fields (confirmed by reading Redis directly); the cron endpoint correctly rejects requests with no/wrong `Authorization` header (401) and correctly processes real due reminders — tested by seeding a due-now reminder for a device with no subscription and confirming the sweep skips it cleanly and removes it from the schedule rather than retrying forever; `/api/push/test` gives an honest "No push subscription registered for this device yet" error rather than a fake success when nothing is subscribed; completing a recurring reminder correctly rolls it to a new due date both locally and in the server mirror (confirmed via Redis); deleting a reminder removes it from both the local DB and the server mirror; the diagnostics card on Settings correctly reports live browser state (desktop Chrome: push supported, permission "default", not installed, not subscribed). **Real bug found and fixed during this testing**: rescheduling via the date picker used `new Date(dateOnlyString)`, which JavaScript parses as **UTC midnight, not local midnight** — for a user east of UTC (Jaideep is in IST) this could silently push a same-day reschedule into "Overdue" instead of "Today". Fixed by parsing the `<input type="date">` value as explicit local midnight (`src/app/tank/[id]/schedule/page.tsx`, `localDateInputToIso`); re-verified live that rescheduling to today's actual local date now correctly lands in the "Today" bucket, not "Overdue" — confirms criteria 7 and 10 together. **Explicitly NOT verified, and this task cannot be marked fully done until it is** (per the spec's own instruction to mirror the honesty discipline used for the native plan's overnight test): (2) an actual push notification arriving on Android Chrome after granting permission; (3)/(4) the real iOS Safari not-installed-yet warning and post-install delivery; (5) **the overnight test — a reminder set for tomorrow morning actually arriving on both an installed Android and an installed iOS device, left untouched overnight** — this is the single most important unverified criterion and needs a real phone; (6) dead-subscription pruning after an actual uninstall/reinstall (the *mechanism* is verified — a 404/410 send response prunes correctly, per `sendPush`'s status-code handling in `/api/push/send` — but never exercised against a real revoked subscription); (11) tapping a real delivered notification and confirming the deep link opens `/tank/{id}/schedule`. I deliberately did not click "Enable reminders on this device" through browser automation, since it triggers a real native OS permission dialog that could hang the automation session — this needs a person tapping "Allow" on a real device, which is also the only way to genuinely test any of the criteria above. **Also not built this session, honestly flagged**: the "prompt to install right after first Tank Scan" moment (T-015, Tank Scan flow, hasn't been built yet — install prompting has nowhere natural to hook into until it exists) and the `beforeinstallprompt` native Android trigger (Settings currently only explains the iOS Add-to-Home-Screen path; Android's install banner is browser-native today, which is an acceptable minimum but not the polished one-tap version the spec describes). **Deployed to https://aquaai-web.vercel.app 2026-09-01** — VAPID keys and `CRON_SECRET` added to Vercel production env vars, `vercel.json` cron config live. Sanity-checked post-deploy: homepage 200s, `/api/push/test` correctly 400s without a device id. **Session paused here — waiting on Jaideep to test on real phones** (Android Chrome test-reminder, iOS Safari install-then-test, and the overnight tomorrow-morning test) before this task can be marked Done. |
| T-020 Emergency triage | Done | 2026-09-01 | `src/app/emergency/page.tsx` — all-taps guided intake (symptom chips: spots/fuzz/clamped fins/gasping/lying on bottom/not eating/sudden deaths/cloudy water/other, affected count, duration, water-test status, optional tank picker, optional photo), no free-text requirement, calls the already-built `/api/triage` (T-013). Renders `first_action` large at the top, `do_not` in its own equally-prominent card (not below the actions), ranked hypotheses with confirm-by, ordered immediate actions with cautions, if/then conditional guidance, `escalate.human_health_warning` shown in full when present, could-not-determine and clarifying questions. Saves to the tank's `log_entries` as an `incident` when a tank is selected (new `src/db/queries/log-entries.ts`); works with zero tank selected, per spec. Offers one-tap follow-up reminders (retest, water change) that create real tasks. Never quota-gated — `/api/triage` has no quota check by design, confirmed by reading the route. **Verified live against the real API with the exact scripted scenario from the spec's own acceptance criteria**: a same-day tank ("Test Tank", no water test, lying on bottom + not eating) correctly returned "test your water parameters" as the first action and led with "New Tank Syndrome or Poor Water Quality" as the hypothesis — did **not** suggest medication. `do_not` list included "do not treat blind." Follow-up reminder button confirmed working (created a real task). |
| T-019 Ask AquaAI | Done | 2026-09-01 | `src/app/(tabs)/ask/page.tsx` — tank picker (optional "general question" vs. a specific tank), free-text question box, starter questions. Tank context is assembled client-side (`src/lib/tank-context.ts`: dimensions, water type, planted/CO2, city, age, livestock+plants+equipment, parameter history, recent log entries) and sent to the already-built `/api/ask` (T-013) — **honestly degrades** where T-017 (parameter logging, explicitly skipped per Jaideep) left no data to draw on, rather than erroring. Renders the answer prominently, `based_on_your_tank` always visible (never behind a tap), warnings by severity, `actions` as real buttons (create_task wired to a real reminder; open_species wired to the new `/dex/[id]` page; log_measurement/open_corpus degrade to an honest "not available yet" message since T-017 and T-004 aren't built), grounding_refs as tappable `GroundingLink`s routed to Dex for `species:` refs, "Tell me more" detail reveal, `uncovered` handling, and thumbs up/down with an optional "this was wrong" correction — both stored on `ai_interactions` (added a `rateAiInteraction` query and had `logAiInteraction` return its row id so feedback can attach to the right answer). Question history shown per tank. Added a small **new read-only `/api/quota` endpoint** (never increments) so the UI can honestly show "N free questions left" before the last one is used, not after — the existing quota module had no way to peek from the client. **Verified live against the real API**: asked "Can I add neon tetras with my betta?" on a real tank with a real Betta already added — the answer correctly named the actual tank ("Test Tank, 64.8 L") and actual livestock ("1x Betta") in both the prose and the `based_on_your_tank` list, cited `species:betta` and `species:neon-tetra` as tappable sources, and tapping one correctly opened that species' real Dex card (now showing "Kept since September 2026" since Betta was already unlocked). Build/lint/`tsc --noEmit` clean for both T-019 and T-020. |
| T-022 Journal and photos | Done | 2026-09-01 | `src/app/tank/[id]/journal/page.tsx` — dated entries (typed journal/maintenance/incident/treatment/water_change, colour-coded), create a new entry with an optional photo in one screen, client-side search over entry text. **Entries build themselves from other parts of the app, per spec** — verified live, not just wired: the Emergency Triage incident from earlier this session already appeared in the journal with no extra work; also wired Tank Scan's accept step to write a journal entry summarizing the report (setup type + finding titles) with the original photo attached, and fixed `addLivestock` to write a "added" `livestock_events` row (it silently didn't before — only `recordDeath` did), which is the timeline's actual starting point. **Photo gallery** (Tank Info's Gallery tab, `src/app/tank/[id]/page.tsx`): real chronological grid grouped by date heading (the exact thing a reviewer asked for — "tag it as a date"), backed by a proper `photos` query file (new, `src/db/queries/photos.ts`). **Full-screen photo viewer with pinch-to-zoom** (`src/components/PhotoViewer.tsx`) — hand-rolled touch handling (no gesture library): two-finger pinch to scale, one-finger drag to pan once zoomed, double-tap to reset. The spec calls this out specifically ("zoom is currently broken in Aquarium Log — a small, real differentiator"), so it got real testing, not just a glance. **Per-livestock timeline**: a "View timeline" toggle on each livestock row shows its `livestock_events` (added/died/etc) chronologically. **Verified live end-to-end**: created a journal entry with a real photo attached, confirmed it saved and the thumbnail rendered; searched "algae" and confirmed it correctly filtered to just the matching entry; opened the photo full-screen and confirmed the pinch/pan/double-tap hint UI renders; checked the Gallery tab and confirmed the same photo appears there grouped under today's date. **Not exercised live**: the 200-photo smooth-scroll performance criterion, and actual multi-touch pinch/pan gesture behavior (mouse/keyboard automation can't simulate real multi-touch — the touch-event logic was verified by code review and the single-photo full-screen open/close was confirmed live, not the gesture math itself). Build/lint/`tsc --noEmit` clean. |
| T-023 Onboarding | Mostly done — blocked on T-024 for the language piece | 2026-09-02 | The stage picker itself, camera-on-open permission timing, and the emergency/scan paths already existed from earlier tasks; what was actually missing and got built this pass: **the stage picker didn't appear on first launch at all** — a brand-new user landed straight on an empty Tanks list with its own separate empty-state links, never the real "What brings you here?" picker the spec describes as the front door. Added `src/components/OnboardingGate.tsx` (mounted in the tabs layout) which checks a new local-only `onboarding_complete` flag (new `src/db/queries/settings.ts`, using the existing generic `settings` KV table rather than a new one) and redirects to `/onboarding` if it's never been set; the flag is set the moment a user leaves the stage picker via any real path (not merely on arrival). **Post-first-tank tour**: `src/components/FirstTankTour.tsx`, three skippable cards (Log parameters / Reminders / Ask AquaAI, matching the spec's own list) shown once, the first time the Tanks list renders with at least one tank. **"Start over" in Settings**: clears the onboarding flag only, never touches tank/livestock data. **Verified live, including the actual persistence, not just the click paths**: fresh profile → `/` correctly redirected to `/onboarding`; choosing "I have a tank already" navigated to scan *and* set the flag — confirmed by reloading `/` afterward and it did *not* redirect back; the first-tank tour appeared on the Tanks list, advanced through Next/Skip correctly, and did not reappear after a reload; Settings → "Start over" correctly bounced back to the stage picker. **Explicitly not done, honestly flagged**: the "language choice — English or Hinglish, offered early" acceptance criterion is blocked on T-024 (Hinglish/i18n), which hasn't been built — the picker only shows in English right now. The <4-minute stopwatch and "force-quit mid-onboarding resumes sensibly" criteria weren't separately re-measured this session (onboarding is now shallow enough — one screen deep before handing off to already-verified flows — that there's little state to lose on a force-quit, but this wasn't explicitly tested with a real kill). Build/lint/`tsc --noEmit` clean. |
| T-024 Hinglish and i18n | Infrastructure done, full-app string coverage not attempted — see honest scope note | 2026-09-02 | **What's real and working**: a second locale (`hi-latn`) actually exists now (`src/i18n/hi-latn.ts`), the `Dictionary` type was fixed so a translation can use different wording than English while still being type-checked against the same shape (it was previously impossible to add a second locale at all — `en.ts`'s `as const` made every string a literal type, so `hi-latn.ts` failed to compile until `src/i18n/types.ts` was changed to widen leaf types to `string`). Language switch in Settings applies **instantly, no restart** — verified live by switching, then navigating to the Tanks list and the onboarding stage picker and confirming both rendered fully in Hinglish with no page reload. The switch **persists** via `localStorage` (a deliberate exception to "SQLite is the source of truth" — this is a pure UI preference that must be readable before the database finishes booting, to avoid a flash of the wrong language; documented in the file). Migrated a representative slice of real screens to prove the mechanism end-to-end: the tab bar, the onboarding stage picker, the Tanks list (header/empty-state), and several Settings sections — checked at a live URL, not just compiled. **The one safety-relevant piece that matters most, done properly rather than superficially**: Ask AquaAI's prompt (`prompts/ask.v1.md`) already had "reply in the question's language, keep technical terms in English" from T-013, but had no Hinglish tone anchor — added three worked example sentences in natural spoken Hinglish (matching CLAUDE.md's own "Aapke tank mein ammonia 2 ppm hai" style) so the model doesn't drift into formal Hindi or English-with-a-Hindi-word, which the spec calls out by name as the failure mode. **What's honestly NOT done, and why**: acceptance criterion 7 ("no hard-coded user-facing string remains anywhere") is not met — the app now has 30+ screens built across this session (onboarding, scan, report, emergency, ask, dex, journal, log, tank edit/size, etc.), nearly all of which still have raw English JSX text. Retrofitting every one of those is the exact "several times more expensive after twenty screens exist" scenario CLAUDE.md's own Hinglish section warns about — attempting to rush full coverage in this pass risked either a much larger, harder-to-verify diff or shallow/wrong translations with no way to check tone quality. Emergency Triage also has no Hinglish output path — its intake is all-taps (no free text) by design, so there's no user-typed language to detect, and translating its *structured* AI-generated report is a separate, bigger piece of work not attempted here. **The spec's own instruction — "have a native Hinglish speaker read the strings before launch, flag this for Jaideep rather than assuming the generated strings are fine" — is being followed exactly**: the Hinglish text in `hi-latn.ts` and the ask prompt's tone examples are machine-drafted and have NOT been reviewed by a native speaker; do not treat them as launch-ready. Build/lint/`tsc --noEmit` clean. **Recommended next step, not started**: a dedicated follow-up pass to move every remaining screen's strings into the dictionary (mechanical but large), followed by the native-speaker review this spec calls for. |
| T-021 Species Dex | Done | 2026-09-01 | Grid (`src/app/(tabs)/dex/page.tsx`, unlocked/all + category + difficulty filters) — locked cards render as grayscale silhouettes with the name hidden, unlocked ones in full colour. Detail page (`dex/[id]`) — **always shows full care info regardless of lock state** (Principle 02, not gated): temp/pH/hardness/size/footprint/group/temperament/diet/lifespan, care notes, common mistakes, and the `disputed` field shown verbatim when present ("Sources disagree on this one"). Unverified/AI-generated species get a visibly distinct amber-bordered banner, never hidden. Unlock wired into the existing livestock-add flow (`src/db/queries/dex.ts`'s `unlockDexCard`, called from `tank/[id]/livestock/page.tsx`) — idempotent, only animates on a genuine first unlock, bumps `timesKept` on repeats. Unlock moment (`src/components/DexUnlockToast.tsx`) is a real animation (pop+fade) gated entirely by a `prefers-reduced-motion` CSS media query, not JS — reduced-motion users still get the unlock, just without the flourish. Share-as-image renders a real canvas card (name, scientific name, rarity, "kept since", unverified flag, app mark) via the Web Share API where supported, falling back to a plain download — verified live that the downloaded PNG looks correct and legible. No leaderboard anywhere, collection-size or otherwise, per the spec's explicit rule. **Verified live end-to-end**: grid correctly showed 30 seeded species all locked; opened a locked card and confirmed full care info + the actual disputed-sources text rendered with no gate; shared/downloaded a real card image and inspected it; added Betta to a tank and watched the unlock toast fire; confirmed the Dex grid updated to "1 of 30 unlocked" with Betta rendered in colour and everything else still silhouetted. Build/lint/`tsc --noEmit` clean. Not yet re-deployed to Vercel as of this entry — see immediately following deploy. |
| T-015 Tank Scan flow | Done | 2026-09-01 | Real capture screen (`src/app/onboarding/scan/page.tsx`, reuses T-014's quality gate/downscale) → dimensions+city form → `scanTank` → Tank Report (`src/app/onboarding/report/page.tsx`) → accept creates the tank + equipment + plants + reminders (`src/db/queries/scans.ts` new, `tanks.setupType` new column) → routes into `/tank/{id}/livestock`, which now ends in a "Done — go to my tank" CTA (was previously a dead end). A shared Zustand store (`src/store/use-scan-session.ts`) hands the report off between the two onboarding pages without touching SQLite. **Fully verified live against the real Gemini API on a real tank photo, multiple runs, including the full accept → livestock → "Done" → tank overview path** — confirmed the created tank shows the right dimensions/city/photo, AI-detected plants, a manually-added equipment item, a **real derived warning** (`checkHeaterWattage`) correctly firing on that manually-added heater, and a pre-filled recurring reminder on the Schedule tab. No species named anywhere in any response, confirming the T-002 pivot holds. **Real bugs found and fixed this session**: (1) model sometimes returns a placeholder `{type:"none"}` instead of an empty array for algae/equipment/plants — filtered out everywhere it's read/written. (2) A race condition where `session.reset()` before `router.push` bounced the user back to the capture screen instead of into livestock entry — fixed by resetting on the scan page's own mount instead. **Three rounds of live-testing feedback from Jaideep, all addressed**: (3) finding text was one long run-on sentence — the explanation and recommended action are now visually separate, and `prompts/tank-scan.v1.md` now instructs short, non-redundant wording. (4) `could_not_determine` was listing things no photo could ever show (water parameters, exact temperature) as if they were a genuine gap — fixed both in the prompt and with a client-side filter as a backstop. (5) The AI-generated inline "clarifying questions" read as redundant filler — **removed** and replaced with a direct "Equipment we missed?" quick-add (type + wattage/flow-rate), which is more useful and reuses the same pattern as the tank detail page's equipment form; `clarifying_questions` is still generated and stored in `scans.raw_response` for the eval set, just not rendered as its own card. T-015's spec has two lines struck through and annotated to reflect this. (6) There was no way forward after adding livestock — added the "Done — go to my tank" CTA. Build/lint/`tsc --noEmit` clean throughout. Scan quota temporarily raised to 50/month for testing — see the decision below, revert before launch. |
| T-019 Ask AquaAI | Not started | | |
| T-020 Emergency triage | Not started | | |
| T-022 Journal and photos | Not started | | |
| T-024 Hinglish and i18n | Not started | | Needs a native Hinglish speaker to read the strings |
| T-023 Onboarding | Not started | | |
| T-025 Free tier and Pro | Partial ("Early Bird" mode) | 2026-09-02 | Reviewed the spec with Jaideep before building. He chose to keep the pricing table for reference but not build Stripe/Checkout/webhooks yet — instead, everything is free and unlimited for everyone right now ("Early Bird"), and custom parameters (already shipped free in T-017) stay free rather than being gated retroactively. **Built**: `src/server/ai/quota.ts` now has `SPEC_QUOTA_LIMITS` (the real 2 scans/15 questions per month from the spec, preserved but unused) and `EARLY_BIRD_MODE = true`, which raises the effective ceiling to 100,000/month — high enough nobody hits it, while the metering plumbing (device id, monthly Redis counter, reset date) stays fully wired so flipping `EARLY_BIRD_MODE` back to `false` is the entire restore. `/api/quota` now reports `earlyBird: true`; Ask AquaAI shows "🐦 Early Bird — unlimited Ask AquaAI, free, while we build out Pro." instead of a remaining-count warning. New "Your plan" card in Settings: an Early Bird badge, plain-language explanation, and an expandable "What happens when Pro launches?" section stating the two commitments verbatim from the spec — species care/disease/triage/8 standard parameters/reminders/journal/export/Dex stay free forever regardless of Pro, Early Bird users keep this free period for as long as they keep using the app, and if development ever stops everything unlocks and data stays exportable. **Explicitly not built**: Stripe account/Checkout/Customer Portal/webhooks, the actual Pro paywall/upsell screens, PDF/vet-visit reports (a stated Pro perk in the spec), and any gating of custom parameters — all deferred until Jaideep sets up billing and decides to launch Pro for real. Build/lint/`tsc --noEmit` clean. Verified live: Settings shows the new Plan card with the badge and expandable commitments text rendering correctly; Ask AquaAI shows the Early Bird line with no quota warning. Redeployed to https://aquaai-web.vercel.app. |
| T-026 Metrics and survival prompt | Done | 2026-09-02 | **Metrics** (`src/lib/metrics.ts`, pure functions on already-fetched rows, no DB calls of their own — trivial to eyeball): active tanks (any tank with a log entry/measurement/completed task in the last 14 days), activation (first tank created + scanned + logged within 48h of a new "installed_at" setting, stamped once by `OnboardingGate` on first boot), logging retention (distinct calendar days with a log action since install), AI trust (thumbs-up/down and "this was wrong" corrections, each per 1,000 answers), and the 90-day survival rate (of livestock added 90+ days ago, % still `status = alive`). All shown on a new hidden developer screen at `/dev/metrics` (not linked from any nav, same pattern as the existing `/dev/db` and `/dev/export` pages) — nothing is sent anywhere, no analytics SDK added. **Survival prompt** (`src/components/SurvivalPrompt.tsx`, mounted globally in the tabs layout so it can surface on any tab): asks "Still doing well?" for livestock added 90+ days ago with status alive/unknown, gated by a global 14-day cooldown (so it's genuinely "every few weeks," not every session) plus a per-fish 30-day re-ask cooldown tracked in a settings JSON blob — deliberately not a schema change. Three answers: "Yes, doing well" and "Not sure" just log the check and dismiss; "No longer with me" opens an optional one-line cause field, then calls the existing `recordDeath()` (T-016) which already writes `status='died'` and a livestock_events row — nothing new needed there. A "Don't ask me this again" link inside the prompt, plus a matching "Fish check-ins" card in Settings, calls `disableSurvivalPromptForever()` — deliberately no re-enable path anywhere, per the spec's acceptance criterion 4. Wording was written to avoid anything score-like, per the spec's own "tone is the whole design problem" callout. Build/lint/`tsc --noEmit` clean. **Verified live**: the negative case — with real (non-test) livestock all added "today," the prompt correctly never appeared on the Tanks page, and the Settings "Fish check-ins" card and the "What happens when Pro launches?"-style disclosure text render correctly. **Not verified live**: the prompt actually *appearing* for a 90+-day-old fish. This session's sandboxed test browser has a known, pre-existing OPFS lock issue (documented earlier in this log) that made every fresh page load fall back to an empty in-memory database, so seeded 90-day-old test livestock never survived a navigation long enough to reach a page that would show the prompt. The gating logic itself is short and was read carefully, but this is a genuine gap — **please open the app on your phone, use "3. Seed test data" on the hidden `/dev/db` screen (or just wait for real fish to cross 90 days), and confirm the "Still doing well?" card actually appears near the bottom of a tab, and that "No longer with me" correctly marks the fish as died and removes it from future prompts.** |
| T-027 Guided Tank Setup Planner | Done (superseded — see the 2026-09-06 entry below) | 2026-09-06 | Built AI-first per Jaideep's re-spec, replacing the original no-AI spec |
| T-029 Always-on shared server | Spec written, not started | 2026-09-03 | New task (`specs/T-029-shared-backend.md`) — Jaideep asked to have this tracked. Deliberately unscoped/undecided: needs a real decision session (what data goes there first, Redis vs. Postgres, whether it implies accounts, who gets to review what) before any implementation starts, per the spec's own "explicitly not decided yet" section. Directly motivated by the species-suggestions flow (previous entry) being local-only with no shared review surface across devices. |
| T-030 App performance dashboard | Spec written, not started | 2026-09-10 | New task (`specs/T-030-performance-dashboard.md`) — Jaideep asked to have this tracked, now that real accounts + a shared Postgres backend exist to actually report on. Deliberately unscoped/undecided, matching T-029's pattern: which metrics matter, where it lives and who can see real user data, what's blocked on more tables migrating server-side (only tanks/livestock/measurements/profile are in Postgres so far), and privacy boundaries all need a real decision session before any code. |
| T-031 Full security review | Spec written, not started | 2026-09-10 | New task (`specs/T-031-security-review.md`) — Jaideep asked to have this tracked, directly motivated by real accounts/Postgres now existing where none did before. Spec starts from the risk areas already honestly flagged in the 2026-09-10 accounts entry below (unverified phone numbers, 4-digit PIN keyspace, no PIN recovery, secrets never leaking client-side, per-user data scoping in the new API routes) rather than starting from zero, plus the standard web-app surface (injection, XSS, CSRF, dependency vulns). Who performs the review and what "done" means per finding (fix vs. accepted-and-logged risk) still need Jaideep's input. |
| T-028 Home / Cross-Tank Calendar | Done | 2026-09-01 | New `src/app/(tabs)/home/page.tsx`, plus a shared `src/lib/schedule.ts` (bucketing + local-date-parsing logic extracted out of the per-tank Schedule page so the two can't drift). Bottom nav is now 5 tabs: Tanks, Dex, Home, Community, Settings. **Verified live in a real browser against real data**: created reminders on two different tanks, confirmed Home shows both correctly bucketed with the right tank name on each row; tapped "Done" on a one-off task from Home and confirmed it disappeared from both Home and that tank's own Schedule page (proves the completion isn't just a Home-side visual trick — it's a real write, synced through the same `completeTask`/push-mirror path as the existing per-tank page); Day/Week toggle works, Prev/Next correctly advanced day-by-day (Tue 1 Sept → Thu 3 Sept after two clicks). Build, lint, `tsc --noEmit` all clean. Recurring-task dates can look unchanged right after tapping Done if the interval recomputes to the same calendar day as before — this is pre-existing `nextOccurrence()` behavior shared with the per-tank page, not new to this task, but worth knowing when eyeballing results. |
| Tank creation/detail redesign (no task number — Jaideep shared a reference app's flow via screenshots) | Done | 2026-09-01 | Rebuilt tank creation as a 2-step wizard (`tank/new`: photo avatar → name/dimensions/water-type/setup-date → **Next** → pick quick-task presets from an expanded list of 11 → **Confirm**) matching the reference exactly except its color picker, which Jaideep explicitly said to drop ("I just don't like the colour selector") — the avatar (`src/components/TankAvatar.tsx`) is photo-or-fish-icon only, no color. Split the old single tank-overview page into three: **Tank Info** (`tank/[id]`, now tabbed Livestock/Measure/Gallery, avatar + water-type badge + setup date header, Edit Tank + Schedule buttons), **Edit Tank** (`tank/[id]/edit`, new — name/city/water-type/setup-date/planted/CO2 fields, a "Tank Size" row linking out, the Plants/Equipment management moved here from the old overview page, and new **Hide Tank** / **Delete tank** actions), and **Tank Size** (`tank/[id]/size`, new — dedicated dimension editor reusing the cm/ft toggle). "Hide" sets `tanks.status = 'archived'` (existing schema field, no migration) and is filtered out of the My Tanks list only — never excluded from exports, and recoverable, unlike Delete which soft-deletes for real. Extended `REMINDER_PRESETS` from 6 to 11 types (added feed/fertilizer/clean_glass/medication/quarantine_check) to cover the reference's quick-task list. Two deliberate deviations from the reference, flagged to Jaideep rather than silently applied: (1) **dimensions stay in the creation wizard** (not deferred to an edit-only "Tank Size" screen like the reference) because `tanks.lengthCm/widthCm/heightCm/volumeL` are NOT NULL and load-bearing everywhere (equipment warnings, card display, compat checks) — making a tank creatable with no size would need a much bigger schema change; (2) **"Salt water" was not added** as a water-type option even though the reference has it — CLAUDE.md states this app is explicitly freshwater/planted only, not reef/marine, so only Fresh/Brackish are offered, with a line explaining why. Share Tank was explicitly skipped per Jaideep (no accounts to build it against yet; revisit at T-025). **A real bug found and fixed during live verification**: setup date round-tripped through UTC and displayed as "31 Aug" instead of "Today" — the exact same UTC-midnight-vs-local-midnight bug class already documented and fixed elsewhere in this codebase (`src/lib/schedule.ts`'s `localDateInputToIso`), reintroduced fresh here; fixed by adding the inverse helper (`isoToLocalDateInput`) and reusing both instead of ad hoc date slicing. **Verified live**: full wizard walkthrough (photo/name/dims/water-type/date → task picker with the same 3 presets pre-checked as the reference → Confirm) landed correctly on the new tabbed Tank Info page; tab switching (Livestock/Measure/Gallery) works; Edit Tank correctly shows "Today (1 Sept 2026)" after the date-bug fix; Hide Tank correctly removed a tank from the Tanks list while leaving other tanks untouched. Build/lint/`tsc --noEmit` clean. Redeployed to https://aquaai-web.vercel.app. |
| Tank form UX fixes (no task number — Jaideep's live-testing feedback after Vercel deploy) | Done | 2026-09-01 | Four real usability gaps found testing on a real deployed device, all fixed: (1) **cm/ft unit toggle** added to both `/tank/new` and the Tank Scan dimensions step — internally always stored/converted to cm (CLAUDE.md: metric is the source of truth, converted at display only), verified live that 2ft × 1ft × 1ft correctly saved as 61×30.5×30.5cm/56.7L. (2) **City field** now has a `<datalist>` of ~50 major Indian cities (`src/lib/common-options.ts`) as tap-to-fill suggestions — still free text underneath, so nothing is blocked, just less prone to typos. (3) **Plants "+Add"** is now a dropdown of common aquarium plants with an "Other (type below)" fallback, instead of a blank text box — verified live (selected "Anubias", saved correctly). (4) **Filter equipment** now asks for a subtype (Hang-on-back/Canister/Internal/Sponge/Undergravel/Other) instead of demanding a "rated flow (L/h)" number most people don't know — that field is now explicitly labeled optional. **Light equipment** now has a Wattage field, which it was completely missing before (a real bug — `tank/[id]/page.tsx`'s equipment form only branched for `filter`/`heater`, silently dropping `light`). Same fixes mirrored into the Tank Scan report's "Equipment we missed?" quick-add for consistency. Also fixed a real save-flow bug Jaideep hit: `/tank/new`'s Create button gave no clear confirmation and used `router.push`, so hitting phone-back after saving returned to the now-empty form instead of the Tanks list — changed to `router.replace` plus a visible "Tank saved — opening it now..." banner. Verified live end-to-end. Build/lint/`tsc --noEmit` clean. Redeployed to https://aquaai-web.vercel.app. |
| Nav restructure + local profile (Workstreams A–C, no task number — small cross-cutting change ahead of T-027) | Done | 2026-09-01 | Bottom nav is now Tanks / Dex / Community / Settings; Ask AquaAI moved to a floating chat bubble on the Home screen (`src/components/FloatingAskButton.tsx`). New local-only `profile` table (name + city, single row, no account) with a Settings card to edit it (`src/db/queries/profile.ts`, `src/app/(tabs)/settings/page.tsx`) — verified live: saved, reloaded the page, value was still there. My Tanks cards now show a photo thumbnail (`src/components/TankThumbnail.tsx`, falls back to a fish emoji when there's no photo yet), Planted/CO2 badges (from the tank's own `isPlanted`/`hasCo2` fields, no new schema needed there), creation date, and a livestock badge row — verified live by creating a real tank with Planted+CO2 checked and confirming all of it rendered correctly on the list. `/community` is a static "coming soon" placeholder, no backend. Build, lint, and `tsc --noEmit` all clean. Species Dex's difficulty filter (Workstream D) needed no changes — `species.difficulty` and the filter were already speced/present. |
| Visual redesign — "sleek, bold, matte, with some glass" (no task number — Jaideep's direct art-direction request) | Done | 2026-09-02 | Jaideep asked to focus purely on visual polish, no new features. Kept the existing colour identity (teal/orange, both light and dark themes, all severity colours — none of that is safe to change without a separate conversation) and instead reworked elevation and surface treatment sitewide via `src/theme/tokens.css`/`tokens.ts`: bumped the corner-radius scale up (cards 14px→20px, added a 28px `xl` for big overlay cards), added real `shadow` tokens (soft, low-opacity, matte — no gloss/gradient) used on every `Card`/`Banner`/`SeverityCard`, and added `glass` tokens (translucent background + `backdrop-filter: blur(20px)` + a soft border) applied deliberately only to surfaces that float over content: the bottom tab bar (now a frosted glass bar with a pill highlight behind the active tab's icon), the floating Ask AquaAI button, the Dex-unlock toast, and the T-026 survival-prompt card. Everything else — the vast majority of the app's surface area — stays flat matte, per the doc's own new "glass sparingly" rule, so it reads as a deliberate accent rather than glass slapped on everything. Buttons got bolder (font-weight 600→700, a tactile `scale(0.98)` press, a shadow lift on the primary button), inputs moved to a sunken matte fill instead of an outlined box, and a handful of screens with hardcoded corner radii bypassing the token system (the Species Dex grid, its filter dropdowns, Ask AquaAI's tank picker and thumbs buttons) were brought onto the same tokens for consistency. `docs/04-design-system.md` updated with the new "surface treatment" section explaining the matte-vs-glass rule, so this doesn't drift on the next screen built. Build/lint/`tsc --noEmit` clean. **Verified live** across Tanks list, Tank Info (tabbed), Settings, Species Dex grid, and the bottom nav/floating button — all render correctly with the new look, no layout breakage. **Not swept**: a long tail of small hardcoded `borderRadius` values inside individual screens further down the app (tank creation wizard, livestock picker rows, journal photo thumbnails, etc.) — these still render fine, just don't get the exact new radius value, a cosmetic inconsistency rather than a bug. Happy to do a second pass through those specific screens if you want full consistency everywhere. |
| Live-testing feedback round (no task number — 5 issues Jaideep hit using the app for real) | Done | 2026-09-02 | (1) **Reminder buttons gave no feedback**, so Jaideep tapped "Water change" 3 times and got 3 identical reminders — `tank/[id]/schedule/page.tsx`'s preset buttons now disable themselves while a reminder is being created, show "Adding..." on the one tapped, and a green "Reminder set: X" banner confirms it landed, matching the confirmation pattern `docs/04-design-system.md` already specifies ("Set reminder", then "Reminder set") but this screen never actually followed. (2) **Ask AquaAI replying in Hinglish with the app set to English**: the prompt (`prompts/ask.v1.md`) was telling the model to guess the reply language from the words in the question itself, and a block of Hinglish example answers sitting right above that instruction biased it toward Hinglish even for plain English questions. Fixed by making the app's own Settings → Language switch authoritative: the client now sends its current locale on every `/api/ask` call, and the prompt is told plainly "reply in {{REPLY_LANGUAGE}} — this is the language the app's own Settings screen is set to, and it is authoritative... don't switch based on the question's own wording." (3) **"Ask AquaAI took too long"**: read through the whole call path (`/api/ask` → retrieval → `callContract` → the Gemini provider) and found no obvious inefficiency — it's a single model round-trip with no retry unless the response fails schema validation, `retrieveCorpus()` is already cheap (returns empty until T-004 ships real corpus content), and `maxOutputTokens` is a reasonable 4096. **Could not reproduce or pin down a concrete fix without real numbers** — every `ai_interactions` row already logs `latencyMs`, so next time this happens it's worth checking that column (or the dev `/dev/ai` screen) for the actual number before guessing further; a Vercel serverless cold start after idle time is a likely honest suspect but not something I can confirm from here. (4) **Bottom tab bar redesigned as a floating "dock"** that smart-hides while scrolling down a long list and reappears the moment scrolling stops or reverses (`src/components/TabBar.tsx`, `TabBar.module.css`) — implemented via a capture-phase `scroll` listener on `document` (catches both a genuinely-scrolling inner container and the more common case here, the whole page/window scrolling, whose scroll event target is `document`) tracking direction with small thresholds so it doesn't jitter on tiny scrolls, always reappearing near the top of a page. A shared `--dock-clearance` token was added so `Screen`'s content padding, the floating Ask button, and the survival-prompt card all clear the now-floating (rather than in-flow) dock correctly. (5) **Tank photo picker only ever opened the gallery, never the camera** — `TankAvatar.tsx`'s single file input had no `capture` attribute, and mobile browsers default that to gallery. Now tapping the camera badge shows a small "📷 Take photo / 🖼️ Choose from gallery" menu backed by two separate inputs (one with `capture=\"environment\"`, one without), so both paths are real, explicit choices instead of the browser guessing. Build/lint/`tsc --noEmit` clean throughout. **Verified live**: reminder buttons show "Adding...", disable during the request, and a confirmation banner appears (tested by rapid-tapping the same preset — only one task was created); the dock genuinely disappears on a real scroll-down and slides back on scroll-up (confirmed via console-instrumented testing, since a synthetic `window.dispatchEvent` doesn't reproduce a real scroll's event target and gave a false negative before the real-scroll test caught it); the camera/gallery menu opens and shows both options on `/tank/new`. **Not independently verified**: the language fix and the camera capture specifically opening the phone's real camera app, since both need testing on Jaideep's actual phone browser (Hinglish/English behaviour and camera-vs-gallery both depend on real device/browser behaviour this session's desktop browser can't fully exercise) — please check both on your phone and let me know. |
| Renamed "Livestock" to "Fish" in the interface (no task number — Jaideep's wording feedback) | Done | 2026-09-02 | Jaideep found "Livestock" (farm-animal terminology) an odd word for pet fish/shrimp/snails. Asked what to rename it to; he chose **"Fish"** specifically (over "Inhabitants"/"Residents"). Changed the user-facing copy only, not the internal plumbing — the route (`/tank/[id]/livestock`), the `livestock`/`livestockEvents` database tables, and every query function keep their existing names, since renaming those is a much bigger, riskier change for zero user-visible benefit. What changed: the tank-detail tab now reads "Fish" (via a new label map, `tank/[id]/page.tsx`, so the internal tab key stays `"livestock"`), "Add Livestock"/"Manage livestock" → "Add Fish"/"Manage fish", the dedicated fish page's title, empty-state text, its "+ Add livestock" button and closing "you can always add more..." line, all updated to "Fish"/"fish" throughout. Checked the rest of the app (derived-checks warning text, the Tank Scan accept screen, Emergency, My Tanks card badges) for the same word and found none — those already avoided it. Build/lint/`tsc --noEmit` clean. **Verified live**: Test Tank's tab bar reads "Fish", the fish page shows "+ Add Fish", "Betta ... × 1" with Record death/Remove/View timeline, and the bottom copy reads "You can always add more fish later from here." Redeployed to https://aquaai-web.vercel.app. |
| Fixed the "generated by AI, not yet reviewed" warning showing on every species card (no task number — Jaideep's bug report) | Done | 2026-09-02 | A real bug, not just unwanted copy: the warning banner and "Unverified"/"?" chips (`dex/[id]/page.tsx`, `dex/page.tsx`, `tank/[id]/livestock/page.tsx`) were keyed on `species.verified`, but **nothing in the app has ever set `verified: true`** — no review workflow exists yet — so every species, including the hand-curated seed catalog (real citations to Seriously Fish/Aquarium Co-Op, never touched by AI) was showing "This card was generated by AI, not yet reviewed by a human," which is simply false for those entries. Fixed by keying the warning on the field that actually answers the real question — `species.origin === 'ai_generated'` — via a new small helper (`isAiGenerated()`, `src/lib/species-origin.ts`) used at all 6 call sites instead of `!verified`. The disclosure still shows, correctly, for species created through Ask AquaAI's "add it anyway" / photo-ID flows (real `origin: 'ai_generated'` rows) — it's gone only from the 30 seed-catalog entries it was never supposed to appear on. Build/lint/`tsc --noEmit` clean. **Verified live**: Betta's Dex card (seed data) no longer shows the "?" chip on the grid or the AI-generated banner/chip on its detail page — only its legitimate "Sources disagree on this one" note remains, which is unrelated and correct. Redeployed to https://aquaai-web.vercel.app. |
| Toned down the "Sources disagree on this one" callout on species cards (no task number — Jaideep's reaction to it on Harlequin Rasbora) | Done | 2026-09-02 | This one is NOT a bug — `docs/02-data-model.md`'s "← why (`disputed`)" section and `docs/04-design-system.md`'s writing guidelines ("ranges, not false precision... a competitive advantage over apps that state one number and get it wrong") deliberately call for showing an honest range instead of a fake-precise single number when two reputable care sources disagree (e.g. Harlequin Rasbora's minimum tank size: Aquarium Co-Op says 38 L works, Seriously Fish says 54 L). Since removing that honesty outright is a real product-principle tradeoff, not a copy fix, I asked Jaideep directly rather than silently deleting it or silently keeping it. He chose the middle option: **keep the honest range, lose the alarming presentation.** `dex/[id]/page.tsx`'s separate bordered "Sources disagree on this one" Card is gone; the same disputed-sources text now sits as a small, quiet, unheaded caption directly under the "Care basics" numbers instead — still there, still honest, just not shouting about it. Build/lint/`tsc --noEmit` clean. **Verified live**: Harlequin Rasbora's card now shows the numbers grid with the disputed-range note as a plain small line right underneath, no separate card, no bold header. Redeployed to https://aquaai-web.vercel.app. |
| Tank photo now previews instantly and lands in the Gallery too (no task number — Jaideep's bug report) | Done | 2026-09-02 | Two real bugs in the tank-creation wizard. (1) **Picking a photo didn't update the circular avatar** — `tank/new/page.tsx` always rendered `<TankAvatar photoUri={null} .../>`, since there's no saved tank yet for a real `photoUri` to point at, so the picked `File` just sat in state with nothing on screen reflecting it until Confirm was tapped. Fixed by giving `TankAvatar` a new `previewFile` prop: when a raw `File` is passed (nothing written to disk yet), it previews directly from that file via `URL.createObjectURL`, instead of waiting on the OPFS round-trip a saved `photoUri` needs. (2) **The setup photo never showed up in the tank's Gallery tab**, only as the avatar — `handleConfirm` wrote it to OPFS and set `tanks.photoUri`, but never inserted a row into the `photos` table, which is what the Gallery tab actually reads. Now it also calls `addPhoto({ tankId, localUri, caption: "Setup photo", takenAt: <setup date> })`, the same pattern already used by Tank Scan's accept flow. **A third bug found live while verifying this one**: the Gallery's date grouping sliced the UTC ISO timestamp to get a "YYYY-MM-DD" key and re-parsed that as UTC before formatting — the exact UTC-vs-local-midnight bug already fixed twice elsewhere in this codebase, caught red-handed here when a photo taken "today" (1 Sept) grouped under "31 August" for this browser's timezone. Fixed by reusing the existing `isoToLocalDateInput` helper instead of raw string-slicing. Build/lint/`tsc --noEmit` clean. **Verified live**: uploaded a photo in the creation wizard, saw the avatar update immediately with a "Photo selected" label (no wait for save); after Confirm, the Gallery tab showed that same photo correctly dated "1 September 2026", matching the setup date. |
| Species catalog expanded from 30 to 445 (no task number — Jaideep shared a real 465-row CSV of fish/plants/shrimp/snails he sourced from three Indian aquarium retailers, judospetfarm.com/fish.splashyfin.com/bunnycart.com) | Done | 2026-09-02 | `data/species.seed.json` went from 30 entries (zero of them plants, despite the app's whole positioning being "freshwater and planted") to 445. Approach, to stay honest rather than fabricate hundreds of precise care numbers under time pressure: (1) **48 species got full hand-researched entries** at the same depth as the original 30 (temp/pH/hardness ranges, min tank size, care notes, common mistakes, source refs to Seriously Fish/Tropica/Aquarium Co-Op-style pages) — prioritised toward genuinely popular, well-documented species, and specifically toward **aquatic plants** (14 of the 48: Java Moss, Java Fern, Anubias, Amazon Sword, Vallisneria, Water Wisteria, Cryptocoryne Wendtii, Dwarf Hairgrass, Monte Carlo, Rotala Rotundifolia, Amazon Frogbit, Hornwort, Water Sprite, Guppy Grass, Moneywort, Brazilian Pennywort — plant-specific info like light/CO2/growth rate/propagation went into `care_notes` prose since the species table has no dedicated plant columns), closing the gap Principle 1 exists to prevent. (2) **The remaining ~370 new species were imported as name/scientific-name/category-only entries** (`origin: 'seed'`, `verified: false`, no numeric fields) rather than inventing plausible-looking pH/volume numbers for species I don't actually know well — CLAUDE.md's own AI rules ("no improvisation on safety-critical questions," "retrieval first, generation second") apply just as much to me hand-authoring bulk content as to a live model call. These entries are still fully searchable/addable everywhere in the app (Species Dex, Ask AquaAI's search, the livestock picker) — they just show blank care-basics fields until researched, instead of a fabricated number presented as fact. (3) **Every one of the 15 "Brackish Water Fish" CSV rows** (puffers, monos, scats, archerfish, gobies, mudskippers, Colombian shark catfish) **got a safety note** even without full numeric data — these are commonly mis-sold in plain freshwater shop tanks, and letting the app stay silent about that would be a real welfare problem, not just missing data. (4) **29 species that grow very large or are outright predatory** (arowana, arapaima, oscar, flowerhorn, pacu, piranha, gar, stingray, snakeheads, goonch, redtail catfish, giant MBU puffer, koi) **also got a short heads-up note** — CLAUDE.md never blocks a save, but staying silent about "this juvenile becomes a 90cm predator" isn't advising, it's withholding. (5) Deduplicated by normalised scientific name against the existing 30 (skips 40 CSV rows already covered, e.g. neon tetra, betta, existing cherry-shrimp already covering every Neocaridina davidi colour morph) plus 10 exact duplicate CSV rows. **A real bug caught during generation and fixed before finalising**: the first slug/common-name pass split names like "X-Ray/Pristella Tetra" or "Dwarf/Pygmy Rasbora" on the first `/`, producing bare, meaningless primary names like "X-Ray" or "Dwarf" with the actual species-group word lost — fixed to take the fuller last segment as primary (this CSV consistently puts the descriptive full name last), verified afterward that zero nonsensical single-word primaries remained among the new entries. All existing hand-curated data (the original 30, and everything already in a real tank/livestock/Dex row) was left completely untouched. Build/lint/`tsc --noEmit` clean. **Verified**: the JSON validated (445 unique ids, every entry has id/scientific_name/common_names), the app's own "Seed species" button loaded all 445 without error and left `betta`'s existing data unchanged, the Dex screen shows "1 of 445 unlocked" and the plant category filter correctly shows 134 plants with the leaf icon, and `java-moss`'s detail page renders full care basics/notes/mistakes exactly like the original curated entries. The CSV file itself moved on disk mid-task (`~/Downloads/species_list.csv` → `~/Downloads/Fishes/species_list.csv`) — found and used from its new location, flagging in case that move was accidental on your end. |
| Real reference photos added to 433 of 445 Dex entries (no task number — Jaideep shared a matching folder of 455 fish/plant photos) | Done | 2026-09-02 | Species had no photo at all before this — the Dex used only a category emoji. New `species.image_uri` column (`drizzle/0003_lean_hemingway.sql`, applied automatically like every other migration, no manual step) points at a bundled static file under `public/species/<id>.jpg` — deliberately **not** OPFS, since this is shipped catalog art everyone gets, not a per-device user photo. **Matching them to the right species was the real work**: filenames were the CSV's own `common_name` with only `/` deleted (e.g. "Bee/Tiger Shrimp" → "BeeTiger Shrimp.jpg", "Betta / Siamese Fighting Fish" → "Betta Siamese Fighting Fish.jpg" — the extra space left behind by the deleted slash collapses too), so rather than guess, I replayed the exact row-by-row id-assignment logic used to build the 445-entry catalog against the same CSV, which deterministically reproduces which id each filename's species became. 433 images copied into `public/species/` and linked (23MB total, small thumbnails, no real size concern) — `Dex` grid and detail pages (`dex/page.tsx`, `dex/[id]/page.tsx`) now show the real photo once a card is unlocked, and correctly keep the generic silhouette on still-locked cards so the collectible "mystery" isn't spoiled by a visible photo before you've actually kept that species. **12 images (455 minus 443 real matches, output showed 6 filenames with no home)** are colour/cultivar variants (e.g. Peacock Moss, Trident Java Fern, Taiwan Bee Shrimp) whose underlying scientific name was already deduplicated onto a different, plainer-named entry during the catalog-import pass — not a new bug, a side effect of that earlier dedup decision, and a small enough gap (about 1% of the image set) not worth re-litigating that decision over. Build/lint/`tsc --noEmit` clean. **Verified live**: `betta`'s row now carries `"imageUri": "/species/betta.jpg"` after re-seeding, the static file serves at `/species/betta.jpg`, and both the Dex grid card and Betta's detail page show the real photo instead of the fish emoji. **Not done**: the livestock-picker's search-result rows (when adding fish to a tank) still show text-only, no thumbnail — a reasonable next step if useful, not part of what was asked this time. |
| Added tiny circular species thumbnails to the fish-picker screen (no task number — Jaideep asked for the gap noted above) | Done | 2026-09-02 | New shared `SpeciesThumb` component (`src/components/SpeciesThumb.tsx`) — a small circular photo, falling back to the category emoji (🐟🦐🐌🌿) when a species has no reference image yet — used in three places on `tank/[id]/livestock/page.tsx`: the name search results, the photo-ID candidate list, and the already-added fish list for the tank (which previously had no image at all, not even in the earlier photo-matching pass). Build/lint/`tsc --noEmit` clean. **Verified live**: searching "neon" now shows five small circular thumbnails (Neon Tetra, Dwarf Gourami, Green/Black Neon Tetra, Dwarf Neon Rainbowfish) next to each result, and the tank's existing Betta row shows its own thumbnail too. |
| Restyled Tanks home, Species Dex list and species detail to a "soft" blue glass look (no task number — Jaideep shared a 3-screen reference screenshot of a plant-care app) | Done | 2026-09-02 | Jaideep asked for the Tanks tab to match a "Good Morning, Plant Parent" home screen, the Dex to match a task-list-style "Care" screen, and a species' page to match an expanded plant-detail screen — asked first whether to carry the reference's green mood or keep AquaAI's teal/orange brand colours, and he chose a third option: lean into a softer **blue** palette instead (fitting for a fish app), scoped to just these three screens. New `--soft-*` tokens in `tokens.css`/`tokens.ts` (background, glass card, a fresh sky-blue accent, ink colours, both light and dark) — deliberately prefixed and separate from the app's normal brand tokens so nothing else picks them up by accident; `Screen` got an optional `background` prop (an inline-style override, so no new CSS class was needed) to let just these routes opt in. **Tanks home** (`src/app/(tabs)/page.tsx`): a time-of-day greeting using the local profile's name if set ("Good morning, Jaideep 🐟"), a working tank-name search box (new, client-side filter — didn't exist before), each tank restyled as a glass card with a real "● Healthy" / "● N due" status chip computed from that tank's actual overdue/today tasks (not decorative), and a blue "N tasks due today" card at the bottom linking to the existing cross-tank schedule (T-028) — all real data, no tanks hidden behind a "see all" the way the reference's horizontal-carousel pattern would have. **Species Dex** (`dex/page.tsx`): the 3-column icon grid became a vertical list — glass rows with a circular thumbnail, name, category/difficulty subtitle, and a real green checkmark (unlocked) or lock icon (not), with the existing All/Unlocked toggle restyled as a proper segmented pill control. **Species detail** (`dex/[id]/page.tsx`): a large rounded hero photo (was a small circle), a difficulty-driven status chip ("Easy"/"Moderate"/"Advanced", reusing the same green/amber/red as elsewhere in the app), a genuine back button (there wasn't one before), a "Care Overview" 2×2 icon-stat grid (temperature/pH/min. tank size/group size) plus a secondary details grid, "Care Tips" reusing the existing care-notes text, and the existing "Share card image" action restyled as the primary blue CTA — same underlying data and functionality throughout, just the reference's layout language. Build/lint/`tsc --noEmit` clean. **Verified live** on real seeded data: Tanks home shows the greeting, search box, real per-tank status chips (confirmed one tank correctly reading "1 due" against real overdue tasks), and the due-today count card; Species Dex list shows "1 of 462 unlocked" with Betta's row showing a real photo thumbnail and green checkmark, everything else locked/greyed; Betta's detail page shows the large hero photo, "Easy" chip, the 4-tile Care Overview grid with real values (24–28°C, pH 6–8, 38L, 1+), Care Tips, Common Mistakes, and the blue Share button, matching the reference's layout closely. |
| Rebased the whole app's colour palette onto the design brief's navy→sky blue system (no task number — Jaideep shared `aquarium-tracker-design-brief.md`) | Done | 2026-09-02 | The brief (saved to the repo as `docs/07-design-brief.md` for future reference, since it's meant to be read before any UI work per its own last section) confirmed blue as the deliberate choice for an aquarium app and gave 5 exact hex values, asking for them applied as the **one real palette**, not scoped to a few screens. Since virtually every screen already reads colour from `--color-*` CSS custom properties rather than hard-coded hex (the whole point of the tokens system), this was a **root-level swap in `tokens.css`/`tokens.ts` only** — `ground`/`surface`/`ink`/`accent`/`deep`/`line` all rebased to the brief's `#F3F8FD`/`#021024`/`#052659`/`#5483B3`/`#7DA0CA`/`#C1E8FF` — and it propagated to every existing screen (Settings, Ask, Schedule, Emergency, Tank pages, the whole T-010 component set) with zero screen-by-screen edits. Severity colours (fixNow/watch/improve/neutral — the safety-critical red/amber/green scale) were deliberately left untouched, same as every previous palette pass. Shadows changed from neutral grey to the brief's specifically-requested *colored* navy tint (`rgba(5, 38, 89, …)`), and glass surfaces got a faint navy tint instead of neutral white, both per the brief's explicit asks. Dark mode uses the brief's own suggested "flip the roles" direction (near-black navy background, pale sky ink, steel blue standing in for the primary-button colour since the light-mode deep navy would vanish against a near-black background) — CLAUDE.md requires real dark mode on everything, so both modes were built, not just the brief's "pick one direction" light-mode-first suggestion. The previous session's `--soft-*` tokens (Tanks home/Dex/species-detail) now simply alias the corresponding main token instead of holding their own separate hex values — there is only one real palette now, and those three screens' code didn't need touching. `docs/04-design-system.md`'s colour section rewritten to match. **Two deliberate departures from the brief, flagged rather than silently applied**: (1) the brief recommends a distinctive webfont (Inter/Plus Jakarta Sans/etc.) — CLAUDE.md states system fonts only, explicitly for bundle size and offline reliability, so this stayed on system fonts; if you want the webfont look, that's a call worth making on its own, not bundled into a token pass. (2) the brief references a `scrape_sites.py`/`match_and_download.py` pipeline for species photography "we already built" — no such pipeline exists in this repo under those names; the real species-photo work happened directly in earlier sessions (433 of 445 species matched to real photos, already live in the Dex) via a one-off Python script, not a reusable named pipeline. Also not attempted this pass, honestly flagged: the brief's photo duotone/navy-grading suggestion (species photos are shown as-is, not colour-graded — a real additional step, not just a token change), and a full copy sweep for the brief's called-out "AI tells" (`·`-joined meta text like "64.8L · 1x Betta" appears in several places already; a handful of `→` arrows) — these are pre-existing, cosmetic, and a separate content pass from a colour rebase. Build/lint/`tsc --noEmit` clean. **Verified live**: Settings, Ask AquaAI and the onboarding stage picker — none of which this session touched directly — all render correctly in the new navy/steel-blue palette purely from the token change, confirming the propagation actually worked app-wide rather than needing per-screen fixes. |
| `frontend-design` skill installed, but not yet active this session — needs a restart (no task number — Jaideep asked directly) | Partial | 2026-09-02 | Ran `claude plugin install frontend-design@claude-plugins-official` from this project's directory — confirmed via `claude plugin list`: installed and ✔ enabled at both **project** scope (this repo) and **user** scope (all projects), from the `claude-plugins-official` marketplace (same marketplace `superpowers`/`context7`/`security-guidance` already come from). **It did not load into this running session** — calling it returned "Unknown skill: frontend-design", which matches the CLI's own "(restart required to apply)" note on plugin changes. Jaideep asked me to stop and update this file so the next session picks up cleanly rather than losing the thread. **Restart, then re-invoke the `frontend-design` skill before touching any more UI** — that's the actual next step, not a fresh design pass done from memory of the brief alone. |
| Jaideep's follow-up: the blue-token rebase (previous entry) is not enough — the app still doesn't read as "clean/expensive" like his 3 reference screenshots | Not fixed yet — diagnosed, then paused for the skill restart above | 2026-09-02 | He shared the same 3 reference screenshots again and asked directly why Claude Code can't produce this look. Correct diagnosis reached before stopping, worth the next session picking up immediately rather than re-deriving: **the token/palette rebase changed colours but not the one technique the references actually lean on hardest** — real photography sitting *behind* the glass panels (a blurred/duotoned plant photo, a moody gradient poster) so the blur has something rich to blur. Every AquaAI screen right now blurs glass over a *flat* `--color-ground`, which reads flat no matter how correct the hex values are — this matches `docs/07-design-brief.md`'s own diagnosis almost exactly ("glassmorphism is just CSS, but it needs something rich behind it to blur... semi-transparent panel + backdrop-filter over a photo, not over a flat color"). Concrete, scoped next steps identified but **not yet built**: (1) a real photographic hero on the Tanks home screen — the user's own most-recent tank photo, blurred/darkened, behind the greeting header, with a sensible flat-gradient fallback when no tank photo exists yet; (2) a subtle multi-stop tonal gradient (not a flat single colour) as the app's base background, cheap to try globally since `body`'s `background: var(--color-ground)` in `src/app/globals.css` is one line; (3) bumping headline type sizes for more confidence (current `--font-title-size` 22px / `--font-display-size` 30px vs the brief's suggested 32–40px) — all still within the system-fonts constraint, no webfont needed. None of this is implemented yet — deliberately stopped here rather than build a second pass without the design skill loaded, per Jaideep's explicit ask. |
| Follow-up to the above: real photography behind glass + bigger headlines, now that `frontend-design` loaded after restart (no task number) | Done | 2026-09-02 | The three concrete steps identified in the previous entry, all built: (1) **Tanks home now has a real photographic hero** (`src/components/TankHeroPhoto.tsx`) behind the greeting header — the most recently-photographed tank's own photo when one exists (read live from OPFS, same pattern as `TankThumbnail`), duotoned navy via a CSS filter so photos from different sources read as one consistent material, with a bottom-to-top navy scrim so the white greeting text stays readable; falls back to a bundled species photo (`/species/betta.jpg`, already shipped for the Dex from an earlier session) for a first-run user with no tank photo yet, so even day one shows real photography, not a flat color. (2) **The page background is now a tonal navy-to-sky gradient** (`src/app/globals.css`, `body { background-image: ... }`, `background-attachment: fixed` so it doesn't visibly repeat on scroll) instead of a flat single colour — one hue family varying only in depth, per the brief's own "tonal reads as considered, flat reads as templated" rule; built entirely from existing tokens so it inherits dark mode automatically. (3) **Headline sizes bumped for more confidence** — display 30px→34px, title 22px→24px (`src/theme/tokens.css` and the matching `src/theme/tokens.ts`, kept in sync as the file's own header requires) — within the brief's suggested 32-40px range but toward the lower end since this is a mobile-first phone screen, not the brief's own desktop mockup. **Verified live in a real browser** (had to restart the app server first — it runs as a production `next start` build per earlier sessions' notes, so a fresh `npm run build` doesn't take effect until the process restarts, which I did): the hero photo renders correctly with the navy duotone and scrim, real fish photography visibly sitting behind the glass search bar and tab dock for the first time; the Tank Info page's larger title renders with no overflow or layout breakage; `npm run build` and `npm run lint` both clean. **Not done, correctly out of scope for this pass**: the brief's own photo duotone-grading pipeline for the full 433-photo Dex catalog (only the one hero use here was duotoned, via CSS filter, not a re-processed image set) and a broader copy sweep for AI-generated "tells" — both already flagged as separate work in earlier entries. |

| Four bug reports from Jaideep using the app for real (no task number) | Done | 2026-09-02 | (1) **Dex photos genuinely missing on a real device, root-caused**: `DbBootProvider.seedIfEmpty()` only ever seeded species when the table was completely empty, so any browser that had already seeded species *before* the 30→445 catalog expansion or the species-photo pass (i.e. Jaideep's real phone, seeded early in this project) would never pick up either update — species stayed frozen at whatever was seeded on first launch, images included. `seedSpecies()`'s own docstring already claimed "on first launch, and on version bump," but the version-bump path was never actually built. Fixed with a real stored version marker (`getSpeciesSeedVersion`/`setSpeciesSeedVersion`, `src/db/queries/settings.ts`) compared against a new `SPECIES_SEED_VERSION` constant in `DbBootProvider.tsx` — a mismatch triggers a real reseed, which is safe to re-run since `seedSpecies()` already upserts by id and only touches `origin: 'seed'` rows, never a user's own edits or AI-generated species. **Verified live**: a browser session with a stale seed correctly reseeded to "0 of 445 unlocked" (was capped at 31) after one reload, and Betta's Fish-tab row (see 3 below) now shows its real photo thumbnail. (2) **Edit Tank's photo picker never wrote a `photos` row** — `tank/[id]/edit/page.tsx`'s `handlePhotoChange` updated `tanks.photoUri` (so the avatar changed) but, unlike the tank-creation wizard's already-fixed version of this exact bug, never called `addPhoto()`, so changing or adding a tank's photo after creation never showed up in the Gallery tab. Fixed by adding the same `addPhoto()` call used elsewhere. (3) **Tank Info's Fish tab said "2 species" instead of naming them** — replaced the count line with a real list (`src/app/tank/[id]/page.tsx`): each alive fish/shrimp/snail as its own row with a circular species photo (`SpeciesThumb`, already built for the livestock picker) and its actual name (nickname if set, otherwise the species' first common name) plus `× N` for a count above 1. **Verified live**: Betta Community's Fish tab now shows "Neon Tetra × 6" and "Betta" as two real rows with photo thumbnails, not a bare count. (4) **Journal's "Add a photo" only ever opened the gallery**, same root cause as the tank-avatar bug fixed earlier this project (a bare `<input type="file">` with no `capture` attribute defaults to gallery on mobile) — but that fix was never generalized. Extracted the camera-vs-gallery two-button menu out of `TankAvatar.tsx` into a new shared `src/components/PhotoPickerButton.tsx` (label + `onPick(file)` props) and used it in the Journal's new-entry form; `TankAvatar` itself was left as-is (its version has extra preview/positioning logic specific to a circular avatar) but the next screen that needs "add a photo" should reach for the shared component instead of rebuilding this from scratch. Build/lint/`tsc --noEmit` clean throughout. **Not independently re-verified on Jaideep's own real phone this session** (camera-vs-gallery choice and the Dex reseed both need a real device/browser to be certain, per the same caveat noted for earlier camera-related fixes in this file) — please check the Journal's "Add a photo" button shows both options, and that the Dex now shows real photos for fish you've already unlocked. |

| Dex "All" tab: photo-scan search loading state + "suggest this fish" flow + admin review screen (no task number — Jaideep's direct asks after the Dex work above) | Done | 2026-09-03 | Three asks: (1) the 📷 scan button now shows a spinning-ring animation (new `.spinner` CSS in `globals.css`) while the AI call is in flight, instead of just a static "…" — the round trip is several seconds and there was no visible "it's working" signal. (2) When a scan comes back `not_in_catalog_likely` (or with zero candidates), the results panel now offers a small "Suggest this fish for our catalog" form (name pre-filled from the AI's closest guess if it had one, plus an optional note) — submitting saves the scanned photo to OPFS and a new `species_suggestions` row (schema + migration `drizzle/0004_brave_krista_starr.sql`, `src/db/queries/species-suggestions.ts`), storing the AI's candidate list alongside for context. (3) New hidden admin screen `/dev/species-suggestions` (same reached-by-URL pattern as `/dev/db`/`/dev/metrics`) lists suggestions (Pending/All toggle), shows the photo, suggested name, note, and the AI's own closest-catalog guesses, with Approve/Reject buttons — approving/rejecting just sets `status`; it does not auto-generate a species card, so "approved" means "someone should add this," not "already added." **Flagged rather than silently assumed**: this whole flow is local-only, same as every other table per CLAUDE.md — a suggestion submitted on one phone only shows up in *that phone's* own `/dev/species-suggestions`, never a shared/cross-device one, until it gets a narrow server-side mirror like the one `push_subscriptions`/`push_sends` already use for reminders. That's a real architectural decision (a new always-on server dependency) I didn't want to make unilaterally — worth a conversation if real users are expected to submit these from their own devices. `docs/02-data-model.md` updated with the new table. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth checking on your end: the spinner actually shows during a real scan, the suggestion form saves and the photo round-trips through OPFS correctly, and `/dev/species-suggestions` renders the photo thumbnail and Approve/Reject work. |

| Photo species-ID no longer restricted to our catalog (no task number — Jaideep's direct call, disagreeing with the original design) | Done | 2026-09-03 | Jaideep pushed back on `species-id/v1`'s design (docs/03-ai-contracts.md Contract 6): handing the model our 445-species catalog and telling it to only pick from that list meant a real fish outside our catalog could never be identified — only ever reported as "not in our catalog," which he correctly called too restrictive. **Bumped to `species-id/v2`** (renamed `prompts/species-id.v2.md`, `PROMPT_VERSION` updated, per docs/03-ai-contracts.md's own "bump the version when a prompt changes" rule): the model now identifies freely from its own knowledge (common name + scientific name, no catalog handed to it at all), and matching against our catalog happens **after** the response, server-side, via a new `matchSpeciesId()` (`src/server/ai/retrieval.ts`) comparing the returned scientific/common name against `data/species.seed.json`. Every candidate now always shows (common name, scientific name, confidence, why) whether or not it matched our catalog — a match is tappable straight to a real Dex card / into the livestock picker; a non-match shows "not in our catalog yet" plus a real path forward instead of a dead end: the livestock-add flow (T-016) routes it straight into the existing "add it anyway" species-gen flow (a provisional card, generated and usable immediately, since the user is actively adding a fish to their tank); the Dex "All" tab's search routes it into the "suggest adding it" flow built earlier this session (saved to `species_suggestions` for later human review). Schema (`src/server/ai/schemas/species-id.ts`) changed accordingly: `species_id` is no longer something the model returns (it doesn't know our ids), `scientific_name` added, `not_in_catalog_likely` dropped as a model field since match/no-match is now computed, not guessed. `docs/03-ai-contracts.md` Contract 6 rewritten to match. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth checking on your end: a photo of a fish that genuinely isn't in our 445-species catalog now returns a real identification (name + scientific name) instead of just "not in our catalog," and both the livestock "add it anyway" and Dex "suggest adding it" paths still work correctly off the new candidate shape. |

| Root-caused and fixed the "my tanks vanished, landed on onboarding" bug (no task number — Jaideep hit this live and confirmed the red storage-warning banner) | Done | 2026-09-04 | This is the same "known, pre-existing OPFS lock issue" flagged but never root-caused back in the T-026 entry — actually diagnosed this time. **Root cause**: `public/sqlite/aquaai-worker.mjs`'s `installOpfsSAHPoolVfs()` call grants exclusive access to one tab/worker at a time for the whole origin — opening AquaAI in a second tab, or leaving a stale tab running in the background, makes every other tab's attempt to open it throw immediately, with zero retry. The worker silently fell back to an **in-memory, non-persistent** database, which looks completely empty (no tanks, no onboarding-complete flag) — that's why Jaideep landed back on `/onboarding` with no tanks in sight, even though his real data was sitting safely in OPFS the whole time, just inaccessible to that particular tab. **Two fixes**: (1) the worker now retries the OPFS open 3 times with backoff (300ms/700ms/1.5s) before giving up and falling back to in-memory — a multi-tab lock is usually released within that window as the other tab's own connection settles. (2) When it still falls back, the red warning banner (`DbBootProvider.tsx`) is now accurate instead of misleading: a new `localStorage` flag (`aquaai_opfs_ever_persistent`, deliberately kept outside SQLite so it's readable even when SQLite fails to open) records whether this browser has ever successfully persisted data; if it has, and this boot came back non-persistent, the banner now says "this usually means AquaAI is already open in another tab — close it there and reload. Your data is safe," instead of the old "your browser doesn't support local storage" line, which was simply false in this case and reasonably read as "your data is gone." (3) Also added a small permanent escape hatch to the onboarding stage picker (`src/app/onboarding/page.tsx`) — "Already using AquaAI? Go to My Tanks" — so landing there by mistake, for this reason or any future one, is never a dead end again. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available to deliberately reproduce a two-tab lock conflict) — worth confirming on your end: open the app in two tabs at once and check the retry logic recovers cleanly, or if it doesn't, that the new banner wording appears instead of the old one. |

| Settings redesigned into "My Profile & Settings" (no task number — Jaideep's direct spec) | Done | 2026-09-04 | Rebuilt around Jaideep's exact list: **My Info** (circular photo via the shared `TankAvatar` component, now generalized with a `fallbackIcon` prop so it can show 👤 instead of 🐟 — Name/City/Email/Contact fields, one Save button; `profile` table gained `email`/`contact`/`photoUri` columns, migration `drizzle/0005_breezy_sunspot.sql`), **App settings** (existing language switch + a new real light/dark/system theme switch — `src/theme/ThemeProvider.tsx`, mirrors `LocaleProvider`'s localStorage pattern exactly, applies `data-theme` on `<html>` which `tokens.css` already keyed off but nothing before now actually set), **Your plan** (kept), **Share this app** (Web Share API with a clipboard-copy fallback), **Privacy policy** and **About** (two new real pages, `/privacy` and `/about` — Privacy is explicit that it's an honest in-app explanation of current data practices, not yet the formally reviewed policy the launch-gate table below still correctly lists as outstanding). **Not dropped**: export/import, the reminders push-diagnostics panel, onboarding replay, and the fish check-in toggle are real functionality this app's own principles depend on (data-export-ships-early, a silent reminder failure being worse than none) — moved into a collapsed "More" `<details>` at the bottom instead of deleting them, since Jaideep's list didn't mention them and silently removing working features wasn't my call to make. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth checking: the photo picker saves and shows a circular preview, all four My Info fields round-trip through a reload, the theme switch actually flips light/dark immediately, and Share opens the native share sheet on a real phone (falls back to "link copied" on desktop). |

| Onboarding redesigned — profile-first, tank-choice moved to the homepage (no task number — Jaideep's direct spec, replacing T-023's original 3-door picker) | Done | 2026-09-04 | The old `/onboarding` "What brings you here?" three-card picker is gone. New flow: (1) `/onboarding` is now a short Name/City (both optional)/Language screen — no tank talk at all, so a brand-new user reaches the real app fast instead of committing to a long flow first; (2) My Tanks (`/`) now has a real zero-tanks state — different hero subtitle ("Let's get your first tank set up" instead of "Here's how your tanks are doing"), and two big buttons, **"🐟 Already have a tank? Add it here"** (→ `/onboarding/scan`, the already-built and already-verified T-015 flow, untouched) and **"🧭 Help me build a tank"** (→ new `/onboarding/planner`); (3) once a real tank exists, My Tanks flips back to the normal tank-list view, but a small "Help me build a tank" link stays permanently above the Emergency link — Jaideep's explicit ask, so the guided-planner path doesn't disappear just because someone already has a tank. **Fixed a real bug found during the redesign**: the old picker's "I'm thinking about getting fish" card had no `onClick`/button wrapper at all — a genuine dead tap that silently did nothing (T-027, the guided planner, was never built, and this card was never even wired as a placeholder). `/onboarding/planner` is a real, honest destination now: says plainly "Coming soon — a guided setup planner..." rather than pretending the feature exists, with a way back to scanning a real tank or to My Tanks. Updated stale `/onboarding` links elsewhere (`(tabs)/home` empty state now points at `/onboarding/scan` directly; the scan page's back-button fallback now goes to `/` instead of the now-repurposed `/onboarding`) — `OnboardingGate`'s redirect target and Settings' "Start over" both still correctly point at `/onboarding`, now showing the new profile/language screen instead of the old picker. Emergency triage keeps its own unchanged, always-present link on My Tanks, independent of this redesign. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth checking: a fresh profile walkthrough (name/city/language → My Tanks zero-state → tap "Already have a tank" → existing scan flow still works end to end), that the language picked on this screen actually applies immediately, and that "Help me build a tank" is genuinely reachable both before and after adding a tank. |

| Tank Check — re-runnable, data-aware Tank Scan (no task number — Jaideep's direct ask: "AI-first" tank analysis, not a one-time onboarding thing) | Done | 2026-09-04 | Built all three pieces discussed: (1) **`tank-scan/v2`** (`prompts/tank-scan.v2.md`, bumped from v1 per docs/03-ai-contracts.md's own versioning convention) — the same Tank Scan model call now optionally receives the tank's own record (`buildTankContext(tankId)`: logged equipment, livestock, recent parameters, recent log entries) alongside the photo, only for a re-check, never a first onboarding scan. New rules in the prompt let it name already-known livestock counts/equipment when a finding needs it ("filter's undersized for 12 neons in a 60L tank") — this is using data it was actually given, not identifying a species from the photo, which stays banned exactly as before — and it's told to cross-reference logged equipment against bioload, flag parameter trends, and call out equipment visible in the photo but missing from the logged list, rather than repeating the record back with no judgment. (2) **New `/tank/[id]/check` page** — a self-contained capture → quality-gate → scan → results flow, callable anytime (not just onboarding), reusing the same quality-gate/downscale/schema code as the original scan. Results reuse the onboarding report's rendering (severity-grouped findings, uncertain section, algae, could-not-determine) plus two things onboarding's report didn't need: a "Nothing to flag" positive-result banner, and per-suggestion "+ Add" buttons for `recommended_maintenance` (rather than auto-creating every reminder on every re-check, which would spam duplicates). Saving writes a real `scans` row (`createScan`) and a Journal entry with the photo attached — the tank's history keeps building the same way the original onboarding scan already did. A prominent blue "🔍 Tank Check" card now sits on the tank overview page, right under About. (3) **Manually-created tanks now get an AI check too** — `tank/new`'s Confirm step routes to `/tank/{id}/check?fromCreate=1` instead of straight to the tank page, framed as clearly optional ("Optional — ... You can always do this later") with a "Skip for now" button, closing the gap where the "+ Add tank" path got zero AI analysis, ever, unlike the camera-scan onboarding path. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth checking on your end: running a Tank Check on a real existing tank and confirming findings actually reference your logged equipment/fish by name where relevant (not just generic "if you're keeping tropical fish" phrasing); that manually creating a new tank now lands on the check page with a working Skip; and that "+ Add" on a suggested reminder actually creates a real task (check the tank's Reminders section afterward). |

| Fixed a real bug: AI treating "date tank was added to the app" as proof of physical tank age for cycling advice (no task number — Jaideep caught this using Tank Check) | Done | 2026-09-04 | `buildTankContext()`'s "Tank age" line was computed from `tanks.startedOn`, which — traced through every path that sets it — is never actually verified: the manual-add wizard defaults it to "today" and most people don't backdate it, and the onboarding scan flow doesn't set it at all (falls back to `createdAt`, i.e. literally the moment the record was saved). So "Tank age: 3 days" in the AI's context was really just "logged 3 days ago," and nothing stopped a cycling/new-tank-syndrome finding from being generated as if it were fact. **Fixed at the prompt level, not by trying to invent a verification mechanism** (a bigger, separate feature not attempted here): renamed the context line to "Days since added to the app... (self-reported setup date, not verified — may not reflect actual physical tank age)"; `prompts/tank-scan.v2.md` now explicitly forbids any cycling-stage/nitrogen-cycle/new-tank-syndrome finding sourced from that field alone — only the photo itself or a real trend in logged water parameters counts as evidence; the existing "cloudiness in a new tank is normal" heuristic was reworded to stop asserting a specific age it can't actually know. Ask AquaAI shares the same `buildTankContext()`, so it had the identical latent risk even though nothing had surfaced it yet — added the same rule to its prompt and bumped it to **`ask/v2`** (`prompts/ask.v2.md`) alongside `tank-scan/v2`, per the project's own "bump the version when a prompt changes" rule. Build/lint/`tsc --noEmit` clean. **Not verified live this session** (no browser available) — worth confirming: run a Tank Check on a tank whose setup date was left at "today" but is actually much older, and check the AI no longer says anything like "your tank is only N days old, so..." |

| Tank Check card copy and sizing (no task number — Jaideep's live-testing feedback) | Done | 2026-09-04 | The blue "Tank Check" CTA card on the tank overview page had a subtitle long enough to wrap to two lines on narrower phones. Reworded to "Expert analysis of your setup — just upload a new picture" per Jaideep's exact wording, then shrunk the card (smaller icon/padding/font) and capped the subtitle to one line with an ellipsis instead of wrapping, so it stays compact regardless of screen width. |
| Fish-leap intro animation (no task number — Jaideep supplied a Lottie animation in `Intro-fish-animation/`) | Done | 2026-09-04 | Added `lottie-web` as a real dependency (0 production vulnerabilities), copied the source JSON into `public/animations/fish-loader.json` (fetched at runtime, not bundled), and built `src/components/IntroAnimation.tsx` — full-screen, tap-to-skip, with a hard fallback timeout in case the player's own "complete" event never fires. **Two iterations on where it triggers, based on live feedback**: first built session-based (`sessionStorage`, plays once per browser tab open) per the original ask ("at the beginning of each session"); Jaideep then hit the real gap live — replaying onboarding via Settings → "Start over" showed nothing, since that's still the same browser session. Moved the trigger to the onboarding page itself (mounted directly in `src/app/onboarding/page.tsx`, dropped the sessionStorage gating entirely) so it now plays every time onboarding actually starts — fresh install or a deliberate "Start over" — which is what actually reads as "opening the app" for this flow. **Asset updated twice more since**, both times just a file swap (`cp` from `Intro-fish-animation/fish-loader.json` into `public/animations/`, no code changes needed since the animation's duration stayed 210 frames/3.5s across updates) — confirmed live both times via a cache-busted request against the deployed URL, since Vercel's CDN can serve a stale cached copy on the very next request after a redeploy. |
| Project put under real version control (no task number — Jaideep's ask, taught step-by-step as a practical git/GitHub walkthrough) | Done | 2026-09-04 | This repo had no git history at all until now — every session's work only ever existed on this machine and in Vercel's deploy history, with no way to see what changed when or roll back a specific change. Ran `git init`, added a "Fish tanks/" exclusion to the existing `.gitignore` (Jaideep's real personal tank photos, kept local-only, never in git history — the file already correctly excluded `.env.local` and other secrets), then created a **private** GitHub repo at **https://github.com/JaideepSaindane/aquarium-tracker** via the already-authenticated `gh` CLI (`gh repo create ... --private --source=. --remote=origin --push`), which created the remote and pushed the first commit in one step. Two real commits exist now: the initial 782-file snapshot (`73e7cc1`), and a scoped follow-up (`4b1765c`) that intentionally left two unrelated uncommitted files (`content/corpus.index.json`, a deleted `files.zip`) out of the animation-update commit — used live as a teaching example of `git add <specific files>` vs `git add -A`. **Going forward**: after Claude Code makes changes and deploys to Vercel as usual, it should also offer to commit and push to GitHub, explaining what's in each commit — this is now the expected loop, not a one-off setup task. |
| Fixed the full-screen photo viewer's back button exiting the tank page (no task number — Jaideep's bug report) | Done | 2026-09-05 | A real bug: `PhotoViewer.tsx` (Gallery's tap-to-zoom photo overlay) was plain component state with no browser-history entry of its own, so pressing the phone/browser back button skipped past closing it and instead triggered whatever "back" meant for the page underneath — landing on My Tanks instead of just closing the photo. Fixed by pushing a throwaway history entry when the viewer opens and treating a `popstate` (back-press) the same as tapping the ✕, consuming that same entry on close either way so the two paths don't stack an extra "back" the user has to press twice. Verified: `tsc`/lint clean, real build. Deployed to https://aquaai-web.vercel.app. |
| Onboarding welcome screen redesigned around a full-bleed photo (no task number — Jaideep supplied a background photo + a reference screenshot, then two rounds of live layout feedback) | Done | 2026-09-05 | Three passes in one session, each shipped and verified live before the next: (1) full-bleed aquarium photo (`public/onboarding/welcome-bg.jpg`, compressed from a 2MB PNG to a 360KB JPEG) replacing the flat `Screen` background, Name/City as frosted-glass floating cards, Language/Theme as segmented pill toggles (Theme wasn't even choosable on this screen before); (2) live feedback — title moved to the top third instead of the bottom, fields narrowed and placed side-by-side, placeholder text given a real visible colour (`onboarding.module.css` — inline styles can't reach `::placeholder`), Get Started became a compact bright-blue pill instead of the app's muted full-width button; (3) more live feedback — Name/City recentred to the vertical middle of the screen (balanced flex spacers instead of bottom-pinned), Language/Theme toggles switched from side-by-side to full-width stacked, Get Started nudged up off the very bottom edge. Verified live in a real browser at each step (including dark-theme toggle). Deployed to https://aquaai-web.vercel.app after each pass. |
| Ask AquaAI made more prominent, visually and functionally (no task number — Jaideep's direct ask) | Done | 2026-09-05 | **Visual**: the entry point was a small unlabeled 56px glass circle shown only on the Tanks tab — now a labelled, glowing bright-blue "💬 Ask AquaAI" pill, moved into the shared `(tabs)/layout.tsx` so it's reachable from every tab (Tanks/Dex/Community/Home/Settings), not just one screen. **Functional**: Ask AquaAI was a single-shot form — one question, one answer, with old answers reduced to a bare unclickable question title in a flat "Past questions" list. Rebuilt as a real scrolling chat thread: `ai_interactions.response` already stored every past turn's full structured answer (T-019), so every turn now renders as a proper assistant bubble (warnings/actions/citations/thumbs, same as the live answer) oldest-to-newest, with a sticky chat composer at the bottom (`Screen`'s `footer` prop) instead of a form embedded in scrolling content. `TabBar.tsx` now hides the bottom dock on `/ask` so the two fixed bottom bars don't stack. **Verified live against the real API**: asked a real question, watched the thinking-bubble, got a grounded cited answer, confirmed thumbs-down + the correction field both work. Deployed to https://aquaai-web.vercel.app. |
| Wired in 8 new state-pack animations across loading and reveal moments (no task number — Jaideep supplied Lottie JSON files built for named app states, `All animation files/`) | Done | 2026-09-05 | New shared `src/components/LottiePlayer.tsx` (reuses the `lottie-web` dependency already added for the onboarding intro animation), with a `respectReducedMotion` opt-in for celebratory one-shots that freezes on frame one instead of animating — loading-state plays intentionally ignore it, same reasoning as a native spinner. Each animation replaced an existing plain-text/CSS/emoji placeholder for the exact state it was named for, nothing new introduced: **thinking** → Ask AquaAI's chat "thinking" bubble (was 3 static CSS dots); **listening** → the chat's empty-state icon (was a static 🐠); **scanning** → Tank Scan's and Tank Check's "analysing your tank" loading screens (was plain text); **triage** → Emergency Triage's loading state (had none); **grounding** → the corpus reference-entry page header; **success** → Tank Check's "saved to your Journal" confirmation; **unlock** → the Dex unlock toast (was a static ✨); **offline** → the "couldn't reach the server" banner, now consistent across Ask/Emergency/Tank Scan/Tank Check instead of a plain red banner for that one specific failure. Build/lint/`tsc --noEmit` clean. **Verified live**: the corpus page's grounding animation renders and plays, Ask AquaAI's listening/thinking animations play in the real chat, and a real question round-trip through the API still works end to end. **Not verified live**: the scanning/triage/success/unlock/offline animations specifically — each needs a real photo-scan, a real triage submission, a real tank-check save, a real Dex unlock, or a real dropped connection to trigger, none of which were exercised this pass beyond code review; worth a quick look on your phone. Deployed to https://aquaai-web.vercel.app. |
| GitHub repo made public, made presentable (no task number — Jaideep's own decision to flip visibility, then asked for a professional-profile pass) | Done | 2026-09-06 | Jaideep switched the repo from private to public himself. Before touching anything, audited what a public switch actually exposes: no secrets committed at any point in git history (`.env*` correctly gitignored and never tracked; VAPID/Gemini/Redis credentials all read from `process.env`), so nothing needed rotating. Flagged one real, not-fully-resolved risk and let Jaideep decide rather than acting unilaterally: the 433 species reference photos in `public/species/` were scraped from three retailer sites (judospetfarm.com/fish.splashyfin.com/bunnycart.com) with no license — now redistributable from a public repo. **Jaideep's call: accept the risk, leave as-is for now.** Separately, explained plainly (no code involved) that flipping back to private stops new access going forward but can't undo copies anyone already cloned while it was public, and that splitting into a public/private repo pair is the standard path if he wants to open-source part of this while monetizing the rest later. **Professional-presentation pass, all shipped**: rewrote `README.md`, which still opened with "This repository currently contains no application code" — stale from before any code existed and actively misleading on a now-fully-built, publicly-visible repo — replaced with a real status section (live URL, what's built vs. pending, honest link to `specs/PROGRESS.md` for detail), current stack table, and repo layout. Added a `LICENSE` file (explicit all-rights-reserved/proprietary notice) since the repo had none — an unlicensed public repo silently grants no usage rights but reads as an oversight rather than a deliberate stance; chose this over MIT specifically because Jaideep is keeping monetization open as a future option. Set the GitHub repo's description and topics via `gh repo edit` (`nextjs`/`pwa`/`typescript`/`sqlite`/`aquarium`/`ai`) — was blank before, which reads as an abandoned/untitled project in search and on a profile. **Not done, flagged as the user's own one-click action, not something I can do from here**: pinning the repo on Jaideep's GitHub profile (Profile → Customize your pins). Committed and pushed (`88efd98`). |
| T-027 Guided Tank Setup Planner — built, then rebuilt AI-first per Jaideep | Done, deployed (later tuned by the live-feedback passes below) | 2026-09-06 | **Built twice in one session — the second time because Jaideep overruled the spec.** The original T-027 spec mandated "no AI call anywhere in this flow"; after the deterministic version was built, Jaideep rejected that framing outright ("who asked you to build this with no internet? I want this to be AI-first") and re-specified the flow himself: tank type first, then **fish first** ("the whole point of making a tank is the fish"), then a size **band** (small/medium/large with ranges, not exact dimensions), then smart recommendations. That is what shipped first — the size step was replaced by feet-based 1-6ft + Long/Cube sizing in live-feedback pass 1 below (Jaideep: "litres is not the standard way of measurement; tanks are generally cube or long"). **The flow** (`/onboarding/planner`, replacing the "coming soon" placeholder): Step 1 tank type (Planted/Hardscape/Bare-bottom — no marine, unchanged); Step 2 the fish wish list as free text in the user's own words ("guppies", "red shrimp", "a big centerpiece fish") plus one-tap beginner suggestions; Step 3 size band (Small 20-45L / Medium 45-120L / Large 120-400L, each with a plain-language hint and example footprint — `SIZE_BANDS` in `src/lib/setup-recommendations.ts`, plan computed at the band's working volume so every recommendation holds for any tank bought in that band) + city for heater sizing; Step 4 the plan. **The AI layer**: new `planner/v1` contract (`prompts/planner.v1.md`, `src/server/ai/schemas/planner.ts`, `/api/planner`, `getPlannerAdvice()` in ai-client) — the model receives the full 445-species catalog as compact one-line rows (id/name/category/temp/pH/min-volume/temperament/difficulty) and the user's wish list, and returns a 2-3 sentence summary, confirmed species ids, suggested fish with counts+reasons, suggested plants (planted tanks only), and severity-rated stocking notes — every claim grounded to catalog ids, no medication/dosing, never blocks the plan. One call per plan, cached by prompt hash like /compat (repeated identical plans cost once), un-metered. **The deterministic engine stays underneath** (`src/lib/setup-recommendations.ts`): heater (litres x temp-gap x 0.3 rounded UP to standard sizes — Jaideep's approved formula over the spec's rougher 1W/L-per-5C, which runs ~25% small), filter (4x/h turnover, the same constant `checkFilterFlow` enforces, +1 for planted/heavy bioload), lighting by tier (0.3/0.4/0.6/0.9 W/L), substrate (5cm planted/3cm hardscape/none, litres+kg), hardscape rules from chosen species' temperament/pH/swim-level, and the static city-climate table (winter-low/summer-high indoor bands, ~50 Indian cities + aliases + substring match; heater math uses the winter low; unknown city -> honest India-wide fallback, never blocks). Jaideep's cold-winter note is in: when raw demand sits within 20% of the recommended heater's band, a readable "next size up is the safer winter pick" note shows instead of silently inflating the number. **Planned fish are real rows with `status: 'planned'`** (Jaideep's explicit choice over notes-only): new `listPlannedLivestockForTank`/`markLivestockArrived` queries, a dashed "On the wishlist — not in the tank yet" section on the Fish tab with an "Arrived" button that flips the row to `alive` and writes a real "added" timeline event the day fish come home. Every existing read path already filters `status === 'alive'` explicitly, so planned fish never count as living in the tank — and `compute90DaySurvival` now explicitly excludes `planned` rows so a never-bought wishlist fish can't silently drag the survival metric down. Saving writes the tank (`status: 'planned'`, `setupType` = tier), equipment, AI-picked plants, planned livestock, Dex unlocks, three starter reminders (water change/feed/test — push mirror is fire-and-forget so offline saves never fail), and a journal entry carrying the full plan + the advisor's summary. **Verified in a real browser end to end** (Playwright against a production build, including the real Gemini call): one tap from the My Tanks zero-state reaches the planner; wish list accepts free text; the plan screen shows instant equipment numbers while the AI thinks; the advisor responded with real suggestions; save navigates to the new tank; the Fish page shows the wishlist section; starter reminders exist on Schedule; the planned tank appears in My Tanks ("Medium planted tank - Healthy - Freshwater - Planted"); offline save shows an honest "Plan saved — you're offline" state with a working path home instead of a dead end (12/13 automated checks; the 13th was a test-script artifact — a fresh browser context has empty storage, confirmed separately in-context). Build/lint/`tsc --noEmit` clean. **Not verified on a real phone** — worth checking: the whole flow on your phone, the AI advisor's actual answer quality on your real wish list, and the "Arrived" button flipping a wishlist fish to a counted fish. **Also fixed en route**: the old wizard's journal logged original suggestions instead of edited values; a fake placeholder plant row was being created; `syncReminder` failures would have crashed offline saves; and the spec's T-027 entry above ("Spec written, not built") is now superseded by this one. |
| T-027 live-feedback pass 1 — six fixes from Jaideep using the planner for real | Done, deployed | 2026-09-06 | All six of his points, each verified in a browser (14/14 checks) before deploying: (1) **typing a fish name shows 2-3 instant catalog suggestions** (fish/shrimp/snails, plants excluded) with photo + min tank size, tap to add — free-text vibes still work; (2) **tank size is now 1-6ft buttons with a Long/Cube shape toggle** (cube up to 3ft where it's actually sold) instead of S/M/L litres — real retail dims shown (2ft long = 61×30×30cm ≈ 55L, 2ft cube = 61×45×45cm ≈ 124L; `TANK_LENGTHS_FT`/`tankDimensions()` replace `SIZE_BANDS`); (3) **advisor suggestion rows clamp to 2 lines** (CSS line-clamp); (4) **heater section shows the city's full indoor temp range** — "Dhule rooms typically sit around 17°C on winter nights and 32°C in summer — the heater is sized for the winter nights" — replacing the mysterious bare "room around 18°C"; added Dhule/Nashik/Aurangabad and other Maharashtra-interior cities to the climate table (Dhule previously fell through to the generic India band); (5) **filters recommended by TYPE** — sponge / hang-on-back / canister with a one-line why — never L/h (the 4× turnover math still runs underneath for the tank page's existing check); (6) **planted substrate names aquasoil explicitly** with shop-askable brands (Tropica Aquarium Soil / ADA Amazonia / Dennerle DeponitMix) instead of "nutrition rich". |
| T-027 live-feedback pass 2 — suggested fish become one-tap adds; wishlist keeps only chosen fish | Done, deployed | 2026-09-06 | Jaideep: "let me add recos but don't remove the suggested fish field... just choose fishes right now, you can choose number of fishes after tank is saved", and "add fishes, shrimps and snails, not just shrimps". The advisor's suggestions AND confirmed companions now render as **+ Add rows** — tapping adds to the wishlist and recomputes the plan for the new bioload; already-chosen rows show **Added**. The wishlist is seeded only with what the user actually wished for (matched against catalog common names — fixed a real bug where live rows' `commonNames` JSON-string parse was assumed but not defensive), never auto-filled with the advisor's whole list. Rows show **no counts**; a note under the wishlist says to just pick the fish for now and choose numbers after saving. Typeahead + wish-matching accept fish, shrimp AND snails. Verified 7/7 in a browser. |
| Planner AI-loading state made prominent (Jaideep: "keep the animation more prominent, user should clearly see it") | Done, deployed | 2026-09-06 | The old loading was a small grey card with a 48px animation, easy to miss. Now a full-width dark navy card (luminance 0.13, verified) with a 110px thinking animation, bold white headline "Our advisor is checking your fish list…", and a plain-language expectation line ("Matching against 445 species — usually 5–15 seconds"). `role=status` + `aria-live=polite` for screen readers; clears the moment the advisor responds. Verified 7/7 in a browser (including a throttled API route to observe the state). |
| Add-fish made additive (Jaideep: "if I add one fish, I should not exit the add fish page... stay there and add more from there itself") | Done, deployed | 2026-09-06 | "Search by name" no longer kicks back to the tank after every add: the page resets for the next fish, an **"Added just now"** list builds up at the bottom, a **"✓ N fish added this session"** counter sits above a **"Done — back to my tank"** button, and Done is the only exit. **Bonus fix the testing surfaced**: the Dex unlock toast was a full-screen modal blocking the page until tapped — exactly wrong for add-after-add rhythm; it now auto-dismisses after ~2.5s (tap "Nice!" to skip sooner). Verified 9/9 in a browser (add guppy → stay → add neon tetra → both listed → Done → both on the tank's Fish page). |
| Full-app UX audit, then two of its Major findings + all its Minor/Polish findings fixed (no task number — Jaideep asked for a whole-app code-based UX review, then picked which findings to act on) | Done, deployed | 2026-09-06 | A code-read audit (not a live click-through) of every screen/flow surfaced 2 Blockers (empty safety corpus, unverified push reminders — both content/ops gaps, not code, left untouched), 4 Major findings, and several Minor/Polish items. Jaideep asked for two Majors plus all the Minor/Polish items; the other two Majors (three overlapping tank-creation paths, the dead "Community" nav tab) are **not yet fixed** — still open, tracked here for the next session. **Fixed and deployed** (commit `560fc89`): (1) **photo-ID fish-add made additive** (`tank/[id]/livestock/scan/page.tsx`) — was `router.replace`-ing back to the tank after a single add while the by-name search flow (previous entry) had already been made additive; now matches it exactly (stays on page, "Added just now" list, "✓ N fish added this session", "Done — back to my tank" as the only exit). (2) **Photo-picker camera/gallery choice made consistent** — Emergency Triage's photo attachment and the Species Dex "All" tab's 📷 scan button were both plain gallery-only file inputs (no camera option), unlike most of the rest of the app; Emergency now uses the shared `PhotoPickerButton` component, Dex's compact round scan button got its own small camera/gallery menu (matching styling, since `PhotoPickerButton` itself renders a full-width `SecondaryButton` that didn't fit the round-icon slot). (3) **Copy/polish fixes**: a leftover "Livestock" section label on the tank page (missed in the earlier "Livestock"→"Fish" rename pass) fixed to "Fish"; the About page's Tank Scan description still said it detects "livestock" — actually stale/wrong, since Tank Scan stopped identifying species entirely per an earlier decision (2026-08-31 log entry) — fixed to "setup type"; onboarding's welcome subtitle was generic filler ("Just a couple of quick things before you dive in") — replaced with the app's actual value prop in both English and Hinglish; Settings' collapsed "More" section said the vague "More (export, reminders diagnostics, etc.)" — renamed to spell out what's actually inside, since data export is a stated core promise and shouldn't be easy to miss behind an unlabeled disclosure triangle. Build/lint/`tsc --noEmit` clean throughout, `next build` verified. Deployed to https://aquaai-web.vercel.app. **Not verified live this session** (no browser available) — worth checking on your end: the photo-ID add flow now stays on the page and lists what you've added, and Emergency Triage's + Dex's photo buttons both show a "Take photo / Choose from gallery" menu. |
| Bottom nav restructured: Ask AquaAI centered as a permanent tab, "My Profile" added (no task number — Jaideep's first step toward an "AI-first" app: "keep Ask Aqua as the centre button on the bottom bar") | Done | 2026-09-10 | Bar goes from 3 items (Dex, Tanks, Community) to 5: **Dex, Tanks, Ask Aqua (center, raised/filled), Community, My Profile**. Deliberately nav-only this pass — Jaideep confirmed no new AI features yet ("just the nav change for now"); that's a separate follow-up. `FloatingAskButton` (the pill that used to float over every screen and hide itself on `/ask`) is now fully removed — its whole reason to exist (Ask having no permanent home) is gone once it's a bottom tab — deleted the component + its CSS module, and its render call in `(tabs)/layout.tsx`. `TabBar.tsx` no longer returns `null` on `/ask`; the center Ask entry gets its own raised circular gradient treatment (`.tabCenter`/`.iconCenter` in `TabBar.module.css`) so it reads as the primary action, not just another tab. "My Profile" is **not** a new screen — confirmed with Jaideep — it's a relabeled bottom-nav entry pointing straight at the existing `/settings` page. Added `tabs.ask`/`tabs.profile` to both `en.ts` and `hi-latn.ts` (Hinglish: "Mera Profile"). **A real layout bug surfaced and fixed during this change**: Ask AquaAI's chat composer is a `Screen`-level sticky footer pinned to the viewport bottom edge, built back when the tab dock hid itself on `/ask` — with the dock now permanently visible there too, the composer would have sat underneath it. Added a new `footerAboveDock` prop to the shared `Screen` component (`src/components/Screen.tsx`/`.module.css`) that shifts a sticky footer up by `--dock-clearance` instead of sitting flush at the bottom edge — used only on the Ask page for now, but reusable if another screen needs the same fix. Build/lint/`tsc --noEmit`/`next build` all clean. **Verified live in a real browser**: bar shows all 5 items with Ask AquaAI raised and highlighted in the center; tapping it opens the existing chat screen with the tab bar still visible and the composer sitting cleanly above the dock (not overlapping); tapping "My Profile" opens the existing Settings page ("My Profile & Settings" heading) unchanged; switching to Hinglish in Settings shows "Mera Profile" correctly in the bar. |
| Reminders (T-018) removed entirely, app-wide (no task number — Jaideep: "remove the reminders functionality from everywhere. Its useless") | Done | 2026-09-10 | Mapped the full blast radius before touching anything (it reached further than just the Schedule tab), confirmed scope with Jaideep on two points, then removed. **DB**: dropped `tasks`/`push_subscriptions`/`push_sends` tables from `src/db/schema.ts`, generated migration `drizzle/0007_cynical_enchantress.sql` (clean `DROP TABLE` ×3) via `npm run db:generate`, bundled into `migrations.generated.ts` the normal way — existing users' local reminder data is dropped on next app load, same as any other schema migration. **Deleted outright**: `src/db/queries/tasks.ts`, `src/app/api/push/*` (subscribe/reminder/send/test routes), `src/server/push/*` (VAPID/web-push helpers), `src/lib/push-client.ts`, `src/lib/reminder-presets.ts`, `src/components/RemindersPanel.tsx`, and — per Jaideep's explicit answer — the entire `/home` cross-tank calendar route (it was purely a reminders calendar, already off the bottom nav). `src/lib/schedule.ts` was **not** deleted — only its reminders-only exports (`bucketFor`, `isOnDay`) were removed; `localDateInputToIso`/`isoToLocalDateInput` stay, since tank setup dates and Gallery date-grouping depend on them and have nothing to do with reminders. **Edited, not deleted** (also per Jaideep's answer — these flows just end one step earlier now): tank creation wizard (`tank/new`) no longer has a "tasks" step, Confirm→Save happens directly after the details screen; Tank Check's (`tank/[id]/check`) "Suggested reminders" card and its add-reminder button are gone; Emergency Triage's "Follow-up reminders" card and `handleAddFollowUp` are gone; Tank Scan's accept flow (`onboarding/report`) no longer auto-creates three starter reminders; the Planner's (`onboarding/planner`) equivalent auto-create loop and its "three starter reminders" copy (two places) are gone. **Also cleaned up**: the Tanks list's notification-bell icon (linked to the now-deleted `/home`) and its "N tasks due today" card are gone, every tank card's status chip is now a plain "Healthy" badge (no due-count math); Settings' whole "Reminders" diagnostics card (enable/disable/test push, platform/permission/subscription state) is gone, and its "More" disclosure summary and Pro-commitments copy no longer mention reminders/notification diagnostics; `FirstTankTour`'s "Reminders" slide was replaced with a "Journal" slide (a real, still-existing feature); Ask AquaAI's `create_task` action now shows an honest "Reminders aren't available in this version" message instead of silently failing, matching the existing pattern for `log_measurement`/`open_corpus`; `src/lib/metrics.ts`'s `computeActiveTanks`/`computeLoggingRetentionDays` dropped their `taskRows` parameter (active-tank and retention-day math now looks only at log entries and measurements) and `/dev/metrics` + `src/db/seed-test-data.ts` updated to match; `vercel.json`'s cron block (`/api/push/send`, daily) removed; `public/sw.js`'s `push`/`notificationclick` handlers removed, keeping only install/activate; the privacy policy's reminders-mirroring bullet removed. **Docs updated to prevent this from quietly getting rebuilt**: `CLAUDE.md`'s "Reminders" section rewritten to record the removal and explicitly say not to rebuild it without Jaideep asking again (original design notes kept below it for context), its stack table's Notifications row updated, and a stray "Web Push needs a backend..." stack rule deleted; `specs/T-018-reminders.md` and `specs/T-028-home-calendar.md` both got a "REMOVED 2026-09-10" banner at the top pointing back here, left otherwise intact as historical record. Build/lint/`tsc --noEmit`/`next build` all clean (had to delete the stale `.next` cache once mid-session — the Next.js route-type validator was still referencing the just-deleted `/home` and `/api/push/*` routes). **Verified live in a real browser**: Tanks list shows no bell icon and every tank reads a plain "Healthy" badge; a real tank's detail page shows only Fish/Gallery/Journal sections, no Reminders; `/tank/new` goes straight from the details form to a "Save" button with no intermediate tasks step; Settings has no Reminders card and its "More" line no longer mentions notification diagnostics. |
| Palette rebase to neutral off-white/charcoal/teal + Tanks screen redesign + nav restyle (no task number — Jaideep pasted a set of design suggestions and, after a review flagged a few conflicts, confirmed "definitely change the palette and nav icon structures... I think this makes sense") | Done | 2026-09-10 | Reviewed the pasted suggestions first: flagged that its card mockup silently dropped the "Healthy" badge and creation date (against Jaideep's explicit "don't remove this information" from moments earlier) and that its quiet-nav-icon ask reversed the raised/gradient Ask Aqua tab built just prior — Jaideep confirmed to proceed with the palette + nav changes while keeping all 7 required card fields. **Palette** (`src/theme/tokens.css`/`tokens.ts`, both light and dark, root-level only — propagates everywhere the same way the 2026-09-02 navy rebase did): ground/surface/ink/accent/deep/line rebased to `#F6F7F5`/`#FFFFFF`/`#18242B`/`#168A8A`/`#12706F`/`#E3E8E7` (light) and a flipped dark equivalent (`#12181A` ground, `#4FCFC6`/`#2FBDB5` teal accents); shadows changed from a colored-navy tint to a neutral charcoal tint; glass tokens changed from blue-tinted to neutral near-white/near-black, and — per the "neutral UI, colourful content" idea — **glass is no longer used for the bottom nav or ordinary cards at all**, only for toasts/prompts. Severity colours (fixNow/watch/improve/neutral) untouched, same rule as every previous palette pass. **Bottom nav** (`TabBar.module.css`/`.tsx`): the floating dock is now a plain, mostly-opaque white/charcoal bar (`--dock-bg` no longer blue-glass) instead of a blue-tinted glass pill; kept the floating auto-hide-on-scroll behavior itself (a previously validated decision — "hide them smartly when required") since only its colour/weight was asked to change, not its mechanism. Ask Aqua's icon reverted from the raised gradient-blue circle (built minutes earlier this session) to a small flat teal circle at normal nav height, per Jaideep's explicit ask. **Tanks screen** (`src/app/(tabs)/page.tsx`): hero shrunk 200px→120px with simplified "Good morning, {name} 👋 / Your aquariums are looking good" copy and a re-tinted (teal/neutral, not navy) photo duotone (`TankHeroPhoto.tsx`); search box now only renders at 4+ tanks (dead weight otherwise) and is a plain white 48px input instead of a blue glass pill; "My tanks" header now shows the count inline; **tank cards rebuilt photo-led** — a full-width photo strip on top (`TankThumbnail` extended with optional `width`/`height`/`radius` props alongside its existing square `size` mode) instead of an 84px square thumbnail beside the text — while deliberately keeping all 7 fields Jaideep listed as must-keep: name, "● Healthy" badge, water type, Planted/CO₂ pills, creation date, fish thumbnails, and the photo itself; cards are now solid `--color-surface` with a plain border instead of blue glass, matching the "Cards #FFFFFF" spec. **A real pre-existing bug found and fixed along the way**: three spots had brand blue hard-coded as raw hex instead of reading `--color-deep` — the onboarding "Get started" button (`onboarding/page.tsx`, `#2f7dfa`) and Ask AquaAI's user-message bubble + send button (`ask.module.css`, `#2f7dfa` ×2) — these would have stayed old-navy-blue after the rebase since they never read the token in the first place; fixed all three to `var(--color-deep)`, plus retinted two navy-hardcoded photo/modal scrims (onboarding welcome screen, Dex share-card tank picker) to the new neutral charcoal. `docs/04-design-system.md`'s colour section rewritten to match; `docs/07-design-brief.md` (the source of the old navy palette) marked superseded at the top, kept for historical context. Build/lint/`tsc --noEmit`/`next build` all clean. **Verified live in a real browser, both themes**: light mode shows the off-white ground, solid white tank card with the photo strip on top and all 7 fields intact (Betta Community: name, Healthy badge, Freshwater, date, 2 fish thumbnails, photo box), teal "+ Add tank"/"Get started" buttons, and the quiet nav with a small flat teal Ask Aqua icon at normal height; dark mode shows the flipped charcoal/teal palette with the same layout. |
| Bottom-nav tab renamed "Tanks" → "My Tanks" (Jaideep: "Change tanks to my tanks") | Done | 2026-09-10 | `tabs.tanks` in both `en.ts` (Hinglish: "Mere Tanks") and the dormant `home.title` string updated too, for consistency in case it's ever wired up. Build/lint clean. |
| Custom app icon set adopted app-wide (no task number — Jaideep: "There's a folder in your main folder called 'icons'. Use those icons wherever you see fit") | Done | 2026-09-10 | Found `icons/aquarium-icons-svg.zip` — a hand-drawn outline icon set (24px grid, 1.75 stroke, `currentColor`) plus two app-icon designs, clearly built to match the palette from the previous session (`icons/icon-sheet.html` even includes a nav-bar mockup identical to what's already shipped: pill-highlighted active tab, #65747A inactive/#168A8A active). New `src/components/icons/AquaIcon.tsx` — a single component inlining all 8 icon paths (`dex`/`tanks`/`ask-aqua`/`community`/`profile`/`freshwater`/`brackish`/`marine`/`planted`) so `currentColor` and a `size` prop both work without extra network requests. **Bottom nav** (`TabBar.tsx`): the 5 emoji icons (📇🐟💬🌊👤) replaced with the matching custom glyphs — a 1:1 match against the icon sheet's own nav mockup. **Water-type / planted indicators**, replaced their plain text with an icon prefix in every place they appear: the Tanks list card, the Tank Info page's water-type badge, and the water-type toggle buttons on both the tank creation wizard and Edit Tank (`marine` isn't used anywhere — the app is freshwater/planted only, per CLAUDE.md — but is included in the component for completeness since it was in the set). **App icon**: `public/icon.svg` replaced with the set's "primary" design (teal fish glyph, matches the new palette) and a new `public/icon-alternate.svg` (the "alternate" design) wired in as the maskable manifest icon; `manifest.json`'s stale `background_color`/`theme_color` (`#EEF2F1`/`#0D3B40`, left over from before even the 2026-09-02 navy rebase) updated to the current palette (`#F6F7F5`/`#168A8A`), and `layout.tsx`'s `themeColor` viewport meta (light/dark) updated to match too. Considered but **not applied**: `SpeciesThumb`'s category emoji (🐟🦐🐌🌿) — the set only covers `planted`, not fish/shrimp/snail/crayfish, and swapping just one category to a monochrome line icon while the other three stay as colour emoji would look inconsistent within the same row, so left as-is. Build/lint/`tsc --noEmit`/`next build` all clean. **Verified live in a real browser**: bottom nav shows all 5 custom icons crisply (zoomed screenshot confirmed pixel-sharp rendering, matching the icon sheet's mockup exactly), the Tanks list card shows a droplet icon before "Freshwater," the tank creation wizard's water-type buttons show the same icon inside each pill, and the Tank Info page's water-type badge shows the icon too. |
| Species database replaced with the rebuilt `aquarium-species-db` corpus: 445 → 1,484 species, photos 433 → 1,299 (no task number — Jaideep: "add that fish database to my original app. Keep everything else the same") | Done — code and data verified headlessly; in-browser testing handed to Claude Code (see handover note at the end of this entry) | 2026-09-10 | **Source**: the sibling `aquarium-species-db` pipeline (`out/fishes.json`) — 1,484 records (1,331 fish / 134 plants / 11 snails / 8 shrimp), names resolved against GBIF + iNaturalist, per-field provenance, taxon-anchored photos. **All 445 previous ids kept stable** (verified id-by-id — zero dangling livestock or Dex-unlock references); 1,039 species added. `data/species.seed.json` replaced wholesale (4.6MB, ~510KB gzipped over the wire); the pipeline's extra fields (provenance, taxonomy, image licence/attribution) ride along in the JSON unused — the app's `SeedSpecies` shape, the species table schema, and the `/api/species-seed` route are all unchanged. **Photos**: the pipeline's 400px thumb variant shipped to `public/species/` — 1,262 new/updated files + 37 old files the pipeline had no replacement for = 1,299 bundled images, ~31MB (same weight class as the old 23MB). This replaces the audit's bad set (174 byte-identical duplicates, ~99 species showing the wrong organism). The 1200px card set (227MB) was deliberately not shipped — regenerate from the pipeline if sharper Dex hero images are ever wanted. **Existing installs get the update, not just new ones**: `seedSpecies()`'s reseed upsert (`src/db/queries/species.ts`) extended from 3 columns (scientificName/careNotes/imageUri) to every seed-owned column, so the rebuilt care data (temp/pH/hardness/size/volume/temperament…) reaches species that already exist; the `origin = 'seed'` WHERE guard is kept (AI-generated and user rows are never touched) and `verified` is deliberately excluded from the update set — a reseed must never un-review a human-verified species. The reseed trigger is a renamed settings key (`species_seed_version` → `species_seed_version_v2`, `src/db/queries/settings.ts`): every existing install reads as never-seeded under the new key and reseeds exactly once, then goes quiet. That key rename, not the usual `SPECIES_SEED_VERSION` bump in `DbBootProvider.tsx`, was chosen because that file is mid-edit by the parallel Claude Code session (the accounts/Neon work) and was not touched. **Copy/comment fixes**: planner loading text "Matching against 445 species" → 1,484 (`src/app/onboarding/planner/page.tsx`); stale 445 counts in `src/app/api/planner/route.ts`, `src/app/api/species-id/route.ts`, `src/server/ai/retrieval.ts`, `src/server/ai/schemas/species-id.ts`, `src/lib/setup-recommendations.ts`; `data/README.md` counts and stale "not in the seed" resolver notes corrected (discus, loaches, puffers and assassin-snail are all in the corpus now; red-tail-shark still is not). **Verified headlessly** — in a throwaway `/tmp/aquaai-verify` copy, never touching the dev server running on :3000: `tsc --noEmit`, `eslint`, `npm run build` (incl. corpus validation) all clean; a production server served 1,484 records from `/api/species-seed` with every sampled `/species/*.jpg` returning 200; a node:sqlite smoke test ran the exact drizzle upsert against the app's real migrations + schema — all 1,484 records inserted twice (idempotent), the `origin='seed'` guard proven to leave `ai_generated` rows untouched, and `verified` proven to survive a reseed. **Known data notes**: 60 species carry "-2"-style ids (same common name, different scientific name — e.g. `sterba-s-corydoras-2` alongside the pre-split `sterba-s-corydoras`; the pipeline added the 2024 *Corydoras*→*Hoplisoma* split as new records rather than renaming old ones); 222 species have no photo yet (the Dex shows the category emoji, same as before); the pipeline's L-number table was not imported (no schema/UI for it). **Handover to Claude Code** (Jaideep's explicit instruction — "I'll hand over the testing to claude later"): (1) run `/tmp/validate-seed.py` — a full data-integrity scan of the new seed (NOT NULL fields, closed `incompatible_with` vocabulary, range sanity, image-file existence); written during this change, never run. (2) Verify in a real browser that an existing install reseeds once and shows the new catalog: the Dex "All" tab count, a species detail page with its photo, the planner typeahead, and that user/AI-created species survive the reseed untouched. (3) The accounts/Neon work in this tree was not touched by any of this. The complete list of files this change touched: `data/species.seed.json`, `public/species/*` (866 new + 396 updated images), `src/db/queries/species.ts`, `src/db/queries/settings.ts`, `src/app/onboarding/planner/page.tsx`, `src/app/api/planner/route.ts`, `src/app/api/species-id/route.ts`, `src/server/ai/retrieval.ts`, `src/server/ai/schemas/species-id.ts`, `src/lib/setup-recommendations.ts`, `data/README.md`, and this entry. Nothing is committed. The regeneration rule, if the pipeline ever reruns: `out/fishes.json` becomes the seed with `image: "/species/<id>.jpg"` set for every record that has a photo, and `out/images/thumb/<id>.jpg` copied into `public/species/`. |
| Real user accounts + a Postgres backend, foundation + core tables (Jaideep: "lets build a backend, gmail sign-up, mobile number +4 digit number code signup and a backend to store everything. I want to launch it to real users.") | Done for tanks/livestock/measurements/profile; 13 tables deliberately deferred | 2026-09-10 | A direct, confirmed reversal of the 2026-09-01 "no accounts" decision and CLAUDE.md's old Principle 5 — planned first (plan mode), three scoping questions asked and answered before writing code: existing local data is **not** migrated (fresh start once accounts exist); phone sign-up is a **self-chosen 4-digit PIN, no SMS OTP** (Jaideep's explicit call to skip SMS cost — the phone number itself is therefore never verified); accounts are **required** app-wide, not optional. **Backend**: provisioned Postgres via Neon (`vercel integration add neon`, same pattern as the existing Upstash integration — Jaideep approved the marketplace terms in his browser), `src/server/db/schema.ts` (Drizzle/Postgres: `users`, `tanks`, `livestock`, `livestockEvents`, `measurements`, `profile` — the last five mirror the local SQLite shapes plus a `userId` column each), `src/server/db/client.ts` (`drizzle-orm/neon-http`, pooled `DATABASE_URL`), a separate `drizzle.config.server.ts` + `db:server:generate`/`db:server:migrate` npm scripts (migrations run against the *unpooled* URL, per Neon's own pooling guidance). **Auth**: Auth.js/NextAuth v5 (`src/auth.ts`) — Google OAuth (disabled until Jaideep supplies real `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` from Google Cloud Console; redirect URIs documented inline in `.env.local`) and a phone+PIN Credentials provider that auto-registers a phone number the first time it's seen (bcrypt-hashed PIN, `src/server/auth/login-rate-limit.ts` locks a number out for 15 minutes after 5 wrong PINs — the one guardrail worth adding by default given a 10,000-combination keyspace and an unverified phone number). JWT sessions; `src/proxy.ts` (Next 16 renamed "middleware" to "proxy" — ran the codemod) gates every route except `/login`/`/api/auth`/a few static files. `src/app/login/page.tsx` — "Continue with Google" + a phone/PIN form with a confirm-PIN step so a signup typo doesn't lock someone out immediately. A "Sign out" button was added to Settings (there was nowhere to sign out before). **Data layer migrated for the four highest-traffic tables** (proving the whole pattern end-to-end, not just building infrastructure nobody's plugged in): `src/db/queries/{tanks,livestock,measurements,profile}.ts` rewritten to call new user-scoped API routes (`src/app/api/{tanks,livestock,measurements,profile}/*`) instead of the local Drizzle client — every existing page that imports these (Tanks list, Tank Info, Fish tab, Settings profile, the Planner, etc.) kept working unmodified since function names/signatures didn't change. **A real bug found and fixed while wiring this up**: `DbBootProvider` (root layout, wraps every page) was blocking `/login` behind a "Loading..." screen waiting on a local database boot the login page doesn't even use — fixed by skipping that boot specifically on `/login` and running it once the user actually navigates into the app. **Verified live end-to-end in a real browser against the real Neon database**: signed up with a phone+PIN, confirmed the row landed in Postgres via a direct query (`user_id` correctly set); created a tank, confirmed via direct Postgres query it's a real server row, not local; reloaded — still there; signed out, signed up as a **second** phone number, confirmed the new account's Tanks list is empty — real per-account data isolation, not just per-device. **Also fixed while in there**: several now-false "no account required"/"never leaves this device"/"works offline" claims in Settings' profile subtitle (`en.ts`/`hi-latn.ts`), the Privacy page, and the About page's "what we believe" list — these were quietly true before this task and are quietly false now; left uncorrected they'd have been a real trust problem. Build/lint/`tsc --noEmit`/`next build` all clean. **Deliberately deferred, not silently forgotten** (see the plan's own scoping section): the other 13 local tables (plants, equipment, parameterDefs, logEntries, photos, scans, aiInteractions, dexCards, dismissedWarnings, settings, speciesSuggestions, species catalog, and the Dex) still read/write local SQLite-WASM/OPFS and won't yet follow a user to a new device — same mechanical pattern, just not done yet; a forgot-PIN recovery flow (not possible without SMS or an email fallback); photo files moving off OPFS onto real storage (Vercel Blob, most likely) so they're reachable across devices; deleting the now-partially-redundant local SQLite machinery (kept, since `/dev/db` and the 13 not-yet-migrated tables still depend on it). **Also true right now, and worth knowing before calling this "launched"**: Google sign-in is wired but non-functional until Jaideep creates real OAuth credentials; a phone number is never verified as belonging to whoever typed it — the entire security model rests on a 4-digit PIN plus the rate-limit lockout. **A second Claude Code session was working concurrently in this same project folder** on an unrelated species-database replacement (see the immediately preceding entry) — confirmed via `git status` that its changes (data/species.seed.json, public/species/*, several query/route files) are untouched by this work and vice versa; **this entry's commit deliberately excludes that session's files** rather than bundling two unrelated, separately-reviewed changes into one commit — and, for the same reason, **this change was not deployed to Vercel this session**, since a CLI deploy uploads the whole working directory (including the other session's uncommitted, not-yet-browser-tested work), not just what's committed to git. Deploy once the species-db session's work is either committed or cleaned up. |

| Google sign-in fixed and verified live: consent-screen app name, `AUTH_SECRET`/`AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` set correctly in Vercel (no task number — follow-up on the 2026-09-10 accounts entry) | Done | 2026-09-10 | The Google OAuth client (`925273640731-...`) turned out to live in Jaideep's existing "video generation" Google Cloud project (same one already billing for Gemini), not a separate "AquaAI" project — found by checking Credentials directly rather than guessing. Fixed the consent screen's App name (was still the default "Claude", now "AquaAI") and added the missing prod/dev redirect URIs. Vercel's `AUTH_SECRET`/`AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` env vars went through a rocky manual-paste process (values got mixed up twice — `AUTH_SECRET` briefly held the Google secret) before being re-set cleanly by piping straight from `.env.local` into `vercel env add`, which avoids manual-paste entirely. **Verified live**: real Google account chooser reached, correctly reads "to continue to AquaAI". |
| Let a Google account link a phone+PIN, so using both sign-in methods doesn't create two separate accounts (Jaideep: "if someone logs in via Gmail first and phone later... they'll lose progress") | Done | 2026-09-10 | New `GET /api/account` (does this account have phone/Google linked) and `POST /api/account/link-phone` (add phone+PIN to an existing account, blocked with a clear message if that number is already someone else's account — deliberately does not auto-merge two pre-existing separate accounts, since phone numbers are never verified). New "Add phone sign-in" card in Settings, shown only when relevant. Jaideep's calls on the two real design forks: linking is optional/skippable (not forced during Google signup), and a phone-number conflict blocks rather than attempts a merge. |
| Onboarding now gated by account, not a local device flag — new accounts were skipping onboarding and landing on an empty "My Tanks", sometimes in Hinglish (Jaideep bug report) | Done | 2026-09-10 | Root cause: the "have I onboarded" flag lived in the browser's local SQLite/localStorage, which is scoped to a device, not an account — a browser that had already onboarded one account skipped onboarding entirely for a brand-new second account. Moved onboarding-complete onto the signed-in account's server-side profile row (`profile.onboardingCompletedAt`, migration `drizzle-server/0002`), set on "Get started", cleared by Settings' "Start over". Fixed a related latent bug in the same profile PUT route while touching it: it replaced every field on every save, which would have wiped the new column on the next unrelated Settings save — now merges only the fields actually sent. **Verified live** with a fresh phone+PIN account: correctly redirected to `/onboarding` in English, stayed on a real empty "My Tanks" after finishing, no loop back. |
| Species database swap (1,484 species) committed separately from the accounts work, per the parallel session's own handover note (no task number) | Done | 2026-09-10 | The species-db session's changes were sitting uncommitted from earlier in the day; verified they were untouched by the accounts work (`git status`, confirmed no file overlap) and committed them on their own, matching the original session's explicit request not to bundle the two. |
| Add-fish flow simplified across all three screens — search/photo-ID clutter, and a real slowness bug (Jaideep, several rounds of live feedback) | Done | 2026-09-10 | (1) **UI decluttered**: the add form's search box, photo-ID button, and Cancel used to have equal visual weight before any species was even picked; now one search field with a small ✕, photo-ID downgraded to a quiet text link, optional Nickname collapsed behind "+ Give it a nickname", and each existing fish's three action buttons (Record death/Remove/View timeline) collapsed behind a single "••• Manage" toggle. (2) **Redundant double-tap removed**: tapping "Take a pic" used to navigate to a whole screen whose only job was asking the same question again ("Take a photo or upload one") before finally opening the camera/gallery — now "Take a pic" *is* the camera/gallery picker directly (new `src/store/use-livestock-scan-session.ts`, a one-shot hand-off store), identification starts immediately on arrival. (3) **Real slowness bug fixed**: all three fish photo-ID entry points were uploading the full-resolution phone photo (often 3000×4000px, several MB) with zero resizing — unlike every other AI photo path in the app, which already downscales to ~1024px. Fixed by calling the existing `downscaleForUpload()` helper before all three. **Verified live against the real API** with a real 5.3MB test photo: round trip dropped to ~3s (ordinary AI-inference time), not inflated by the upload. |
| Fish compatibility check redesigned twice in the same session, ending with it removed entirely (Jaideep's own words: the AI batch version "just completely f***** it up") | Done | 2026-09-10 | First pass (his own request): moved compatibility from a per-fish AI check while adding to one batch AI check run once on "Done" — reasoning was a one-at-a-time check could never catch a conflict between two fish added in the same visit. Built and verified live against the real API (a Betta + an existing Neon Tetra correctly flagged). **Then Jaideep hit it in real use and asked for it removed outright** — kept only the free, instant, non-AI Size/Temp/Parameters/Setup readout (already existed on one of the three add-fish screens) retitled "Compatibility", shown the moment a species is picked, immediately followed by "Add to tank". Extracted into a shared component (`src/components/CompatibilitySummary.tsx`) so all three add-fish screens (by-name search, Fish-tab inline form, photo-ID) show the same thing — two of the three previously had no compatibility signal at all outside the removed AI check. **Also fixed while there**: the sticky footer's real bug — `Screen`'s content padding reserved a *fixed* 96px for the footer, but the footer grows taller when extra content appears (the "✓ N fish added" line), so once real height exceeded the fixed reservation the footer started covering the page's own "Add to tank"/"Done" button. `Screen` now measures the footer live via `ResizeObserver` and sizes padding to match, fixing this everywhere the `footer` prop is used, not just this screen. Also caught and fixed: a missed *third* add-fish screen (`/livestock/search`, reached via the tank overview's "Search by name" popup option) that still had the old per-fish check after the first two were fixed — and on that same screen, the "Already in this tank" chip list sat above the search box, which could push real search results down far enough to land under the on-screen keyboard (Jaideep's screenshot) — reordered search-first, existing-fish-list-last on both screens that had this shape. |
| Tank age: "Setup date" exact-date picker replaced with a rough age-band picker (Jaideep: "no one will remember the setup date for their tank") | Done | 2026-09-10 | Old field defaulted to today with only a ±1-day stepper, which is exactly the kind of pointless precision the ask called out. New `AgeBandField` (six tappable options: Just set up / A few weeks / 1–6 months / 6–12 months / Over a year / Not sure) stores a real ISO date internally — each band's rough midpoint — since Ask AquaAI and Emergency Triage both read `tanks.startedOn` as a day count for New Tank Syndrome risk; "Not sure" stores null rather than guessing. Widened `NewTank`/`TankUpdate`'s `startedOn` type to allow `null` so clearing it via "Not sure" on the edit screen actually clears the stored value (the tanks PATCH route merges only provided fields, and a bare `undefined` would have silently no-opped instead of clearing). |
| Dark mode contrast fixed — cards, pills, and search fields were "merging into the background" (Jaideep) | Done | 2026-09-10 | Root cause: dark mode's ground/surface/surface-alt tokens were all within a few percent of the same lightness, and every input/pill (`Field`, `Button`'s secondary variant, `Chip`'s neutral variant) had a literal `border: 1px solid transparent`, relying entirely on that thin background-colour gap to read as a distinct element. Rebalanced the three tokens further apart in `tokens.css`/`tokens.ts` (both the `@media` and `[data-theme="dark"]` blocks) and gave all three components a real `--color-line` border. Light mode untouched. |
| Compatibility section made crisp; the real cause of the footer still hiding "Add to tank" found and fixed (same-day follow-up — Jaideep hit the button-hidden bug again right after the first fix shipped) | Done | 2026-09-10 | **Compatibility rewrite**: each row (Size/Temp/Parameters/Setup) cut to exactly one line — Temp/Parameters now just state the range plus a one-word verdict ("works for your tank" / "compatible") instead of the old overlap-math explanation; Setup dropped its verbose prefixes; row padding 8px→3px, section margin 12px→8px, text forced `nowrap`+ellipsis so it can never wrap. Size left as-is (Jaideep confirmed it already read fine). **The real footer bug**: the earlier fix measured the sticky footer with `ResizeObserver`'s `contentRect`, which is content-box only — excludes the footer's own padding/border, understating real height by ~25px on every screen using `Screen`'s `footer` prop. Switched to `getBoundingClientRect()` (true rendered height), added an immediate synchronous measurement on mount (no gap before the first observer callback), plus an 8px safety margin. **Verified numerically, not just visually**: read the actual applied `padding-bottom` and the footer's real height via injected JS after adding two fish on two different screens — the formula matches exactly, zero overlap. |
| AI cost and usage audit — every AI call site reviewed for token/context bloat, lifetime-cost-per-user estimated, subscription timing discussed (no task number, Jaideep's direct ask) | Analysis done, fixes not yet built — paused mid-session by Jaideep to prioritise live bugs | 2026-09-10 | Read every `/api/*` AI route, every prompt file, and `retrieval.ts`. **Real finding**: `/api/ask` (and `/api/planner`) call `getSpeciesContextText()`, which serialises the *entire* 1,484-species catalog (~38,200 tokens, measured directly from `data/species.seed.json`) into the prompt on every single call regardless of the question asked, and `/api/ask` is never cached (unlike `/api/compat`/`/api/species-gen`, which already cache by prompt hash). Everything else (compat, species-id, species-gen, triage, scan) is already lean — compat in particular only pulls the specific species involved, a good model to copy. Estimated lifetime AI cost for one moderately engaged user (~2 years, ~100 questions): **~$0.42 today, ~95% of it the wasted catalog dump**; **~$0.05 after fixing Ask/Planner to retrieve only relevant species** (in-tank + question-mentioned, same keyword technique `retrieveCorpus` already uses). Flagged the real reason **not** to rush a paywall on AI cost alone (it's already cheap either way) — the quota system (`x-device-id`, a client-generated, spoofable header) has **no connection to the real accounts added 2026-09-10**, so a free/paid gate today would be trivially bypassable by clearing browser storage; fix that before quota (free or paid) means anything. Jaideep said "we'll come back to this later" before the retrieval fix was built — **nothing here is implemented yet**, this entry is the record of the analysis only. |
| Ask AquaAI / Planner species-catalog cost fix built (the retrieval half of the 2026-09-10 audit above) | Done | 2026-09-11 | `retrieveRelevantSpecies()` (new, `src/server/ai/retrieval.ts`) — species already in the tank (real ids, sent from the client) plus whatever the question's own text names, same keyword technique `retrieveCorpus` already used for the safety corpus; falls back to a bounded easy/beginner set (never the full catalog) for a fully open-ended question. `/api/ask` now uses this instead of dumping the whole catalog — worst case ~60 species instead of 1,484. `/api/planner` gets its own `retrieveSpeciesForPlanner()` (wish-list matches plus their categories, since the planner needs to suggest companions the user didn't necessarily name) — bounded to ~250 instead of 1,484. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: ask a question naming a specific fish and confirm it's still answered correctly, and run the Planner and confirm suggestions still feel sensible. Deployed to https://aquaai-web.vercel.app. |
| Ask AquaAI was refusing basic species care questions (temperature, pH, etc.) as if they were medication dosing (Jaideep: "What the fuck is the point of GenAI... This response is horrible") | Done | 2026-09-11 | Root cause: `ask/v2`'s own prompt (and CLAUDE.md's AI rules, which the prompt was correctly following) lumped "a specific care parameter" in with medication/dosing/disease as things to flatly refuse whenever our own catalog had no number — caught live on "ideal temperature range for pea puffers" (`dwarf-puffer` in our catalog, `temp_c: null`, never populated). **Narrowed the rule, didn't remove it**: the hard refusal (`uncovered: false`, no answer) is now reserved for genuinely safety-critical territory — treating a sick/injured fish, medication, dosing, disease diagnosis. Everything else, including a species' basic care facts with no number in our catalog, now gets a real general-knowledge answer with real numbers where known, flagged `uncovered: true` so the UI shows it as general knowledge rather than the app's own verified data. Bumped `ask/v2` → `ask/v3`. CLAUDE.md's AI rules section updated to match (was the source of the over-broad rule, not just the prompt). **Follow-up fix same session**: dropped the prompt's "where sources disagree, say they disagree" instruction, which was producing clunky "one source says X, another says Y" answers (Jaideep: "you don't have to quote all of this shit") — now just gives an approximate range in plain language, no meta-commentary about where numbers came from. **Follow-up 2**: the `uncovered` banner's copy ("flagged for review") was stale now that `uncovered: true` means "answered from general knowledge," not "refused" — changed to "This is an LLM-generated response, not our own verified data." Build/lint/`tsc --noEmit` clean throughout. **Not verified live this session** — worth checking: ask the pea puffer question again and confirm a real number comes back with the new banner copy. Deployed to https://aquaai-web.vercel.app after each of the three passes. |
| Quota tied to real accounts instead of a spoofable client header (the second half of the 2026-09-10 audit's flagged risk) | Done | 2026-09-11 | `peekQuota()`/`incrementQuota()` (`src/server/ai/quota.ts`) now key the Redis counter by the real signed-in account id, not the `x-device-id` header a browser just self-reports (clearing storage used to hand out a fresh quota for free — harmless today only because Early Bird mode makes the ceiling effectively unlimited). `/api/scan`, `/api/ask`, and `/api/quota` (the read-only "how many do I have left" peek the UI shows) now all call `requireUserId()` — the same real session-derived id every other user-scoped route (`tanks`, `livestock`, `measurements`, `profile`) already uses — and 401 if somehow called with no session, matching the rest of the app now that accounts are required everywhere. BYOK (bring-your-own-key) requests are still exempt from quota entirely, unchanged. `x-device-id` itself is untouched and still sent/used for provider routing; it's just no longer read for anything quota-related. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: ask a question, confirm it still works and still counts against quota (check `/dev/metrics` or the "N free questions left" line); the real proof this matters — clearing browser storage no longer resets your quota — isn't meaningfully testable while Early Bird mode's ceiling is 100,000/month, but the code path now matches every other user-scoped route. Deployed to https://aquaai-web.vercel.app. |
| "Update available" banner — a deployed update now reaches open tabs (Jaideep: "whenever I ship something new... give a tag to all users, like 'Updates available'. Clicking on that should just refresh the page") | Done | 2026-09-11 | Built the lightweight version discussed first (poll + tap-to-refresh, not a real-time push). **`public/sw.js`** no longer calls `self.skipWaiting()` on install — a new worker now sits in the normal "waiting" state after a deploy instead of silently taking over mid-session; it takes over only when told to via a new `message` listener (`{type: "SKIP_WAITING"}`). **New `src/store/use-app-update.ts`** (Zustand, ephemeral UI state, not persisted — same pattern as `use-scan-session.ts`) tracks whether an update is waiting and handles applying it: posts the skip-waiting message, reloads once the new worker actually takes control (`controllerchange`), with a 3s fallback reload in case that event doesn't fire. **`ServiceWorkerRegister.tsx`** (root layout, app-wide) now watches for updates three ways: a worker already sitting in "waiting" on page load, a freshly-installing worker via `updatefound`, and periodic `registration.update()` checks — every 30 minutes the app is open, plus every time the tab comes back into focus (covers "was backgrounded, a deploy happened while away, reopened it"). No server round-trip of our own — this is just asking the browser to re-fetch `/sw.js` and compare bytes, which is what makes a deploy detectable at all. **New `<UpdateBanner>`** (also mounted app-wide in the root layout) — a small pill fixed to the top of the screen, "Update available — tap to refresh," visible on every screen the moment an update is detected, gone the instant the user taps it and the page reloads on the new build. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — this genuinely can't be tested by deploying once (there's nothing "old" running yet to detect an update against). Worth checking after the *next* deploy following this one: keep the app open on a phone, ship any change, and confirm the banner appears within the next visibility-change or 30-minute check, and that tapping it reloads to the new version. Deployed to https://aquaai-web.vercel.app. |
| Light-mode contrast fixed — cards, pills and inputs were merging into the white background (Jaideep: "make the cards, pills, everything pop up more... the white background is merging with borders") | Done | 2026-09-11 | Same root cause and fix class as the 2026-09-10 dark-mode contrast fix, this time for light mode: `--color-line`/`--color-line-soft` (`tokens.css`/`tokens.ts`) were only a couple of percent off white (`#e3e8e7`/`#edf1f0` against `#ffffff` surface / `#f6f7f5` ground), so a Card's border (which reads `line-soft`) and every pill/input/chip border (which reads `line`) barely registered. Darkened both (`#c7d1cf` / `#d9e1df`), strengthened the light-mode shadow tokens (`shadow-sm`/`md`/`lift`) for extra lift, and darkened the glass/dock border tokens the bottom nav and floating toasts use. Root-level token change only, propagates everywhere the same way every previous palette pass has — no component files touched. Dark mode untouched. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth a quick look in light mode: Tank Info cards, the bottom nav bar, and a text input should all now visibly separate from the page behind them. |

| First tank scan reuses the setup photo instead of asking for a new one; renamed the ongoing re-scan to "Health Check" (Jaideep: "I have already added that picture in my tank's profile so use that... whenever you do your tank scan, it'll be sort of a health check") | Done | 2026-09-11 | `/tank/[id]/check` (Tank Check/Health Check) now takes two real paths when arriving straight from tank creation (`fromCreate=1`): if a photo was already uploaded during the creation wizard, the idle screen shows one button — "🔍 Scan my tank" — with inviting first-time copy ("Let us scan your tank and give you some advice — we'll use the photo you already added"), and tapping it reads that exact photo straight out of OPFS (`readPhotoFile`, new import) instead of opening the camera/gallery picker. It still runs through the same quality gate every photo does; if the stored photo somehow fails that gate, it falls back honestly to asking for a fresh one with the specific reason shown, rather than silently retrying forever. Saving a scan that reused the existing photo skips adding a second, duplicate Gallery entry (`reusedExistingPhoto` flag) — it's already there from tank creation. If no tank photo was uploaded at all, the flow is unchanged (ask to take one or choose from library). **Renamed everything post-first-scan to "Health Check"**, per Jaideep's framing that every scan after the first one is a check-in, not a fresh scan: the tank overview page's CTA card (🔍→🩺, "Tank Check"→"Health Check"), the screen title, the results title, and the Journal log-entry prefix — the very first scan (`fromCreate=1`) keeps its own distinct "Scan your tank"/"Your tank scan" titles instead. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: create a tank with a photo, confirm the check screen goes straight to "Scan my tank" with no camera prompt and the scan actually runs on that photo; create a tank with no photo and confirm the old ask-for-a-photo flow is unchanged; run a re-check from an existing tank's page and confirm it's now labelled "Health Check" throughout. Deployed to https://aquaai-web.vercel.app. |

| Photo placeholder on the Tank Info page for a tank saved with no photo (Jaideep's ask) | Done | 2026-09-11 | The tank photo step is optional in both creation wizard paths, so a tank could exist with no photo and no way to add one from its own page — the only route was burrowing into Edit Tank. Where `TankBigPhoto` used to just render nothing when `tank.photoUri` was empty, it now shows a dashed-border placeholder card ("No photo yet — add one so you can spot it at a glance", 📷) with the same camera/gallery `PhotoPickerButton` used elsewhere in the app. Picking a photo reuses Edit Tank's own write pattern exactly (`writePhotoFile` → `updateTank(photoUri)` → `addPhoto` for the Gallery) so it doesn't reintroduce the "photo updates the avatar but never shows up in Gallery" bug fixed earlier this project. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: save a tank with no photo, confirm the placeholder appears on its Tank Info page, add a photo there, and confirm it becomes the tank's photo and also shows up in the Gallery tab. Deployed to https://aquaai-web.vercel.app. |

| Real photo storage (Vercel Blob) + 4 more tables migrated to the server backend: plants, equipment, journal (log entries), photos (Jaideep: fix "photos still on OPFS" and "13 local-only tables" together) | Done for these 4 tables + all photo uploads; 9 tables still deliberately deferred | 2026-09-11 | Second migration pass, same userId-scoped pattern the 2026-09-10 accounts work established for tanks/livestock/measurements/profile — planned first with Jaideep (three real decisions: set up Vercel Blob now rather than defer photos separately; roll out a few tables at a time rather than all 13 at once; accept a fresh start for existing local data, same as the original accounts migration, rather than building a one-time import path). **Photo storage**: created a public Vercel Blob store (`aquaai-photos`, linked via `vercel blob create-store`, `BLOB_READ_WRITE_TOKEN` live in all three environments) — public means an unguessable random URL per file, not access-controlled; flagging that tradeoff explicitly rather than silently picking it, since a private store + signed read URLs is the more locked-down alternative if that's ever wanted instead. New `/api/photos/blob-upload` (auth-gated, `@vercel/blob`'s `put()`, 8MB cap) and a client `uploadPhoto()` helper (`src/lib/photo-upload.ts`) that every "add a photo" call site now calls instead of `writePhotoFile` (OPFS). **Four tables**: `plants`/`equipment`/`logEntries`/`photos` added to `src/server/db/schema.ts` (migration `drizzle-server/0003`), new routes `/api/plants`, `/api/equipment`, `/api/log-entries`, `/api/photos` (list/create, plus `[id]` DELETE for plants/equipment) — exact copy of the tanks/livestock pattern (`requireUserId()`, userId-scoped `where`, soft-delete via `deletedAt`). The four local query files (`src/db/queries/{plants,equipment,log-entries,photos}.ts`) rewritten to call these instead of local Drizzle — same exported function names/signatures, so every existing screen that imports them kept working unmodified. **Every real "write a photo" call site migrated** to `uploadPhoto()` (Blob) instead of `writePhotoFile()` (OPFS): tank creation wizard's setup photo, Edit Tank's avatar change, the new no-photo placeholder (added earlier this session), Health Check's own photo capture, Journal's attach-a-photo, the Gallery tab's "+ Add photo", the onboarding camera-scan flow's original capture, and — caught while auditing, a real gap in the *already*-migrated `profile` table from 2026-09-10 — the Settings profile photo, which had never actually been fixed despite `profile` being server-backed since the first pass. **Rendering side**: since a photo field can now hold either a real `https://` Blob URL (new writes) or a legacy OPFS-relative path (old local data, or the 9 tables not yet migrated) — both exist in the wild at once — every render spot needed to handle both, not assume one. New shared `src/lib/use-photo-src.ts` (`usePhotoSrc()` hook + `isRemotePhotoUrl()`) used directly by `TankThumbnail`, `TankHeroPhoto`, `GalleryGrid`, `JournalPanel`'s thumbnail, and `TankBigPhoto`; `TankAvatar` inlines the same branch since it also has its own `previewFile` case to keep. Health Check's "reuse the tank's existing photo" flow (built earlier this session) now `fetch()`es a remote Blob URL directly instead of assuming OPFS, with the old OPFS read kept as a fallback for tanks whose photo predates this migration. Build/lint/`tsc --noEmit` clean. **Deliberately deferred, matching the phased-rollout decision**: the other 9 local tables (scans, aiInteractions, dexCards, dismissedWarnings, settings, speciesSuggestions, the species catalog, parameterDefs) — a further follow-up pass, same pattern. **Two known gaps, honestly flagged rather than silently left**: (1) `dev/export`'s JSON/CSV/photo-zip export still only reads local OPFS/SQLite — a photo now stored on Blob won't be included in a photo-zip export until that flow is separately updated to fetch from Blob URLs too; (2) a handful of "add a photo" call sites have no explicit error UI if the Blob upload itself fails offline (previously OPFS writes essentially couldn't fail) — most are already inside an existing try/catch that surfaces *some* error, but not all, so a flaky-connection upload failure may currently fail silently on a couple of screens rather than showing a message. **Not verified live this session** — worth checking on your end: create a tank with a photo, sign in as the same account on a different browser/device (or a private window), and confirm the tank's photo, its Gallery entries, and any plants/equipment/journal entries you added all show up there too — that's the actual proof this fix works, not just that it builds. |

| Third migration pass: the remaining 9 local tables moved server-side, plus the two honest gaps from the last pass fixed (Jaideep: "fix these too", choosing "do all of it now" when asked whether to scope it down) | Done | 2026-09-11 | **The two gaps**: (1) `src/lib/export.ts` was still reading every table straight from local SQLite via `PRAGMA`/`SELECT *`, which had silently gone stale for every table migrated server-side since 2026-09-10 (tanks/livestock/measurements/profile) and now this session's plants/equipment/logEntries/photos too — a full JSON/CSV/photo-zip export was quietly missing anything written after each table's own migration date, not just Blob-hosted photos as originally scoped. Fixed properly: a new `SERVER_TABLE_URLS` map fetches each migrated table from its real API instead of introspecting the stale local copy, camelCase server fields converted to snake_case so the merged export reads as one consistent shape regardless of where a table actually lives; the photo-zip now fetches Blob-hosted photos over the network (`isRemotePhotoUrl`) alongside legacy OPFS reads for pre-migration photos. (2) Every "add a photo" call site that lacked explicit offline-failure feedback (`tank/[id]/edit`, `tank/[id]/page.tsx`'s placeholder, `GalleryPanel`, `JournalPanel`, Settings' profile photo) now wraps its `uploadPhoto()` call in try/catch and shows a real "check your connection and try again" message via each screen's existing error-banner convention, instead of failing silently. **The remaining 9 tables**, same userId-scoped pattern as the previous two passes: `scans`, `aiInteractions`, `dexCards`, `dismissedWarnings`, `speciesSuggestions`, and real per-tank `parameterDefs` overrides/customs all got server tables + API routes (`/api/scans`, `/api/ai-interactions[/id]`, `/api/dex-cards`, `/api/dismissed-warnings`, `/api/species-suggestions[/id]`, `/api/parameter-defs`) + rewritten local query files, exact copy of the tanks.ts pattern. **Two deliberate design calls, not just mechanical repeats**: (a) the species catalog (1,484 species) was explicitly NOT migrated — it's shared reference data identical for every user, not per-user data, so per-account storage would be the wrong data model; it stays local, re-seeded from `data/species.seed.json` as before, and CLAUDE.md now says so explicitly rather than leaving it looking like unfinished work. (b) `parameterDefs`' 8 global default parameters were never really "data" — a hardcoded constant seeded into rows for no real reason — so they became a plain client constant (`src/lib/standard-parameters.ts`); only genuine per-tank overrides/custom parameters (real user data) got a server table. **`settings` needed the same "which key means what" judgment call as the catalog**: `species_seed_version` tracks whether *this device's* local SQLite has run a reseed — moving it server-side would be a real bug (a second device signing into the same account would read "already reseeded" and skip populating its own, never-yet-seeded local species table), so it deliberately stays local (`getLocalSetting`/`setLocalSetting`, `src/db/queries/settings.ts`); the genuine account preferences (first-tank-tour-shown, installed-at, survival-prompt state) moved to a new `user_settings` server table. The Dex "suggest a species" photo-scan flow (`dex/page.tsx`) was also switched from OPFS to Blob upload, since its target table (`speciesSuggestions`) is server-backed now. Build/lint/`tsc --noEmit` clean throughout. **One known pre-existing gap, not newly introduced but not fixed either**: `src/db/seed-test-data.ts` (the hidden `/dev/db` "seed test data" tool) still writes straight to local SQLite for tables that have been server-backed since 2026-09-10 — this was already broken before this session, just never previously noted; a real fix would rewire it to call the same APIs, not attempted here since it's a dev-only tool and out of what was asked. **Not verified live this session** — worth checking: run a JSON or CSV export and confirm data from every screen (Tank Scan history, Ask AquaAI history, Species Dex, journal, plants, equipment) actually appears in it now, not just tanks/livestock; try uploading a photo with the network off and confirm each screen now shows a real error instead of nothing happening. Deployed to https://aquaai-web.vercel.app. |

| Community, MVP pass (Jaideep's `/plan` walkthrough of an Aquarium Log-style community: posts, photos, comments, a separate Q&A tab, My Posts, New Post) | Done for the confirmed MVP scope; Q&A/My Posts/view toggles/saves explicitly deferred | 2026-09-11 | **Real deviation from CLAUDE.md, confirmed before writing any code**: "Community" was on CLAUDE.md's "do not build yet" list (a real support/moderation burden, flagged Phase 2+) — planned in `/plan` mode first specifically because of this, asked directly whether to override it, and Jaideep said yes; CLAUDE.md's list updated to remove Community and record the decision (same pattern as "Accounts and cloud sync" being removed from that same list on 2026-09-10). **Scope, confirmed via the plan's own questions**: MVP is the Community feed only — new post (text + optional photo), single list view, comments, report, delete-your-own. Q&A tab, My Posts tab, list/block/grid view toggle, and a save/bookmark button are explicitly deferred to a follow-up, not built. Moderation is report + self-delete only, no admin approve/hide workflow — the one addition beyond exactly what was asked is a minimal **read-only** `/dev/community-reports` viewer (same hidden-URL pattern as `/dev/species-suggestions`), added because collecting reports with genuinely no way to see them anywhere would make the report button pointless; it has no action buttons, so acting on a report still means Jaideep finding the real post/comment and deleting it by hand. **New server tables** (`community_posts`, `community_comments`, `community_reports`, `drizzle-server/0005`), same userId-scoped pattern as every table migrated this session — author name/photo are deliberately *not* denormalized onto posts/comments; every list route joins against the existing `profile` table by `userId` at read time instead, so a later profile change shows up on old posts immediately rather than going stale. New routes: `/api/community/posts` (list with joined author + comment count / create), `/api/community/posts/[id]` (single post w/ author+count / soft-delete-your-own, 404s rather than 403s on someone else's post so as not to confirm it exists), `/api/community/posts/[id]/comments` (list/create), `/api/community/comments/[id]` (delete-your-own), `/api/community/reports` (report / list-for-the-dev-viewer). Photo upload reuses the **existing** `/api/photos/blob-upload` route and `uploadPhoto()` helper unchanged — it was already generic, not tank-specific. **Screens**: `/community` (replaces the placeholder — feed, "+ New Post", a quiet "community advice isn't verified, use Emergency Triage for anything urgent" banner linking to `/emergency`), `/community/new` (text + optional photo, same shape as `/tank/new`'s save flow), `/community/[id]` (post + comment thread + a sticky comment composer via `Screen`'s `footer` prop, same pattern as Ask AquaAI's chat). New shared pieces: `src/components/AuthorAvatar.tsx` (small circular avatar, person-icon fallback, built rather than reusing `TankAvatar` since that component carries picker/upload logic this read-only use doesn't need), `src/components/community/PostCard.tsx` (shared between the feed and the detail page's top card), `src/lib/relative-time.ts` ("2h ago"/"3d ago" formatter — nothing like it existed yet). Current-user-id for ownership checks (own-post Delete button, etc.) comes from `useSession()` (`next-auth/react`) — the first client-side use of it in this app; `session.user.id` was already exposed via the existing `session` callback in `src/auth.ts` from the original accounts work, so no auth changes were needed. Build/lint/`tsc --noEmit` clean. **Known, disclosed limits, not fixed this pass**: no cursor pagination (most-recent-50 only); `/api/community/reports` GET has no separate admin role check — any signed-in account could technically hit it directly, acceptable for a hidden unlinked URL at this stage but a real gap if the community grows before real moderation roles exist. **Not verified live this session** — worth checking: create a post with a photo, confirm it shows in the feed with your name/photo/time; comment on it; report something and confirm it shows up on `/dev/community-reports`; delete your own post/comment and confirm it disappears; on a second account, confirm you can't delete someone else's post (should 404, not silently succeed). Deployed to https://aquaai-web.vercel.app. |

| Photo cropping removed entirely; Community posts support multiple photos with a horizontal-scroll strip (Jaideep: "let's just remove the crop option altogether... add more photos so that we can put multiple photos... allow scrolling on the side") | Done | 2026-09-11 | **Cropping reverted cleanly** via `git revert` of both crop commits (the "everywhere" pass and the AI-flow extension) — every screen is back to picking a photo straight through, no crop step, `ImageCropModal.tsx` deleted. Because a git revert undoes a commit's full diff, this also removed those two commits' own entries from this log — unlike the Reminders removal (which stayed as a real feature people had used), cropping was reverted within the same session it shipped, before anyone outside this session had touched it, so there was nothing yet worth preserving a record of. **Community posts now hold multiple photos**: `community_posts.photoUri` (single) replaced with `photoUris` (JSON array of Blob URLs, capped at 10) — a clean column swap via two small migrations (`drizzle-server/0006`/`0007`, split into an unambiguous add-then-drop since the feature had no real posts yet to migrate and `drizzle-kit`'s rename-vs-add prompt needs a TTY this session didn't have). New shared `src/components/community/PostPhotoStrip.tsx`: a single photo still renders full-width as before; 2+ render as a horizontally-scrollable, snap-to-photo strip (`overflow-x: auto` + `scroll-snap-type`) so a post with several photos doesn't grow the card tall — used by both the feed and the post detail page via the shared `PostCard`. **New Post screen rebuilt for this**: the old single "Change photo" button is gone; "Add a photo" (then "Add another photo (N/10)") appends to a horizontally-scrollable thumbnail strip at the top, each with its own ✕ to remove, uploaded to Blob one at a time on submit. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: confirm no screen shows a crop step anymore; create a post with 3+ photos and confirm the feed card and the post detail page both show a swipeable strip, not a stacked column. Deployed to https://aquaai-web.vercel.app. |

| Like button added to Community posts (Jaideep: "add like button to each post") | Done | 2026-09-11 | New `community_likes` table (`drizzle-server/0008`) — one row per (user, post), enforced by a unique index so liking twice is a harmless no-op rather than a duplicate row; unliking deletes the row. New `POST /api/community/posts/[id]/like` toggles and returns the fresh `{ liked, likeCount }` in one round trip. The list and single-post routes now also return `likeCount`/`likedByMe` per post (a grouped count query plus a lookup of the current user's own likes, same shape as the existing comment-count logic). `PostCard` shows a heart button (🤍/❤️) with the count next to the comment-count line, **optimistic** — the tap flips instantly, then the real server response confirms or corrects it a moment later if the request fails. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: like a post, confirm the heart fills and the count goes up immediately; reload the page and confirm it's still liked; unlike it; on a second account, confirm liking someone else's post works and doesn't affect their own like state. Deployed to https://aquaai-web.vercel.app. |

| "Translate" link on non-English Community posts (Jaideep: "add a translate option under posts that are not written in english. clicking it must translate all text into app selected language") | Done | 2026-09-11 | New un-metered `POST /api/community/translate` (same cost tier as `/api/compat`/`/api/triage` — a plain free-text-in/free-text-out AI call, not a full `callContract()`, since a translation makes no factual claim of its own that needs a citation) takes `{text, targetLocale}` and returns `{translated}`; `targetLocale` is whatever the app's own Settings → Language is currently set to (`en` or `hi-latn`), read via the existing `useLocale()` hook — not guessed from the post text. **Detection, honestly scoped**: new `src/lib/looks-non-english.ts` only catches non-Latin scripts (Devanagari, Arabic, Bengali, Tamil, Telugu, Kannada, Malayalam, CJK, Hangul) — it does **not** detect a different language written in Latin script (Spanish, French, or Hinglish itself), since that needs a real language-ID model this app doesn't have; flagging this as a real, deliberate limitation rather than a bug. `PostCard` shows a "Translate" link under the post body only when this heuristic trips; tapping it calls the new route and swaps the displayed body for the translation, with the link becoming "Show original" to toggle back (no re-call on toggle — the translated text is kept in state). Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: post something in Hindi (Devanagari script) or another non-Latin-script language and confirm the Translate link appears and works; confirm an English or Hinglish (Latin-script) post correctly shows no Translate link, per the documented detection limit; switch the app's language in Settings and confirm translations start targeting the new language. Deployed to https://aquaai-web.vercel.app. |

| Phone-first accounts can now link Google too — the reverse of the existing "add phone sign-in" flow (Jaideep: "if someone adds the mobile number and saves it, and then next time logs-in using gmail, you should try to sync data") | Done, explicit-link only — deliberately not automatic | 2026-09-11 | **Real constraint explained to Jaideep before building, and confirmed**: phone numbers here are never verified (no SMS OTP, a standing, deliberate cost tradeoff), so there's no trustworthy signal that "this Gmail" and "that phone number" belong to the same person — auto-merging on a bare fresh Google login would risk merging two different people's data. He confirmed the same safe pattern already used for the other direction: an explicit, signed-in "Link Google account" action, not silent detection. **New `/api/account/link-google`**: while signed in with phone+PIN, hitting this route records a signed, single-use, 5-minute "link intent" cookie (`src/server/auth/link-intent.ts`, HMAC'd with `AUTH_SECRET` — the same secret NextAuth signs session tokens with, so it can't be forged to link someone else's account) naming the current account, then redirects into NextAuth's own Google sign-in flow. **`src/auth.ts`'s `signIn` callback extended**: when a Google sign-in completes and a valid link-intent cookie is present, it attaches that Google identity to the *named* account instead of creating a new one — checked and consumed *before* the normal find-or-create logic, specifically so a conflict (this Google account already belongs to a different, unrelated existing account) is caught and blocked (redirects to `/settings?linkError=conflict` with a clear message) rather than silently signing the user into that other, wrong account. Settings gets the mirror of the existing "Add phone sign-in" card — "Link Google account," shown only when the signed-in account has no Google linked yet (reusing the existing `/api/account` status endpoint, now also read for `hasGoogle`). Build/lint/`tsc --noEmit` clean. **Not verified live this session** — this is a full OAuth redirect round-trip, which can't be exercised without a real browser and a real Google account; worth testing end to end: sign up fresh with phone+PIN, go to Settings, tap "Link Google account," complete the Google consent screen, confirm you land back on Settings still seeing your phone-account's tanks (not a fresh empty account) — then sign out and sign back in with that same Google account from a different browser/device and confirm the same tanks show up there too. |

| T-030 built — a private usage dashboard, first real pass (Jaideep: "a dashboard for me to track overall usage, number of accounts, what they are doing... can live outside of the app") | Done for a first real metric set; T-030's own "which metrics/who can see it" scoping questions answered pragmatically rather than in a separate decision session | 2026-09-11 | T-030's spec (written 2026-09-10) deliberately left this unscoped pending a real conversation — Jaideep's ask today answered the open questions directly enough to build: aggregate-only ("what they are doing" as counts, not browsing anyone's actual tank/journal/chat content — CLAUDE.md's "their data is theirs" and the spec's own "reporting surface, not a support tool" line), and a separate URL rather than anything linked from the app's own nav. **New `/admin`** (a real page, not the hidden `/dev/*` naming convention, since this one's meant to be a bookmarkable destination) shows: total accounts split by sign-in method (phone-only/Google-only/both — the first real use of the account-linking work from earlier today), signups per day for the last 14 days as a simple bar list, tanks/alive-livestock/scans-run/journal-entry counts, AI usage by kind with total cost (the first real use of the per-call cost logging CLAUDE.md's AI rules have always required, now that `ai_interactions` is server-side from this session's earlier migration pass) plus thumbs up/down totals, and Community counts (posts/comments/likes/pending reports). **Real access-control gap addressed head-on**: there is no admin-role concept in this app at all — `src/server/auth/require-admin.ts` is the minimal version, an `ADMIN_EMAILS` allowlist (defaults to Jaideep's own address so the page works with no extra Vercel setup) checked against the signed-in account's email; a phone-only account can never match since it has no email. `/api/admin/stats` 403s for anyone else. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: open `/admin` signed in as yourself and confirm real numbers show up (not zeros — there should be at least the accounts/tanks/posts created during this session's own testing, if any); open it signed in as a different, non-admin test account and confirm it correctly refuses access. Deployed to https://aquaai-web.vercel.app. |

| The "couldn't open your saved data" OPFS-lock banner toned down and made accurate (Jaideep: "I keep seeing this error everywhere") | Done | 2026-09-11 | The banner itself wasn't newly broken — this is the same OPFS-single-tab-lock behavior root-caused back on 2026-09-04 — but its old red, urgent wording ("your data is safe," implying something real was at stake) is now genuinely overstating the risk: as of this session's account/backend migration, the local database holds nothing but the shared species reference catalog — every tank, fish, photo, journal entry, and AI chat is server-side, tied to the account, not this browser. A failed OPFS lock today just means species reload from the server for that visit instead of the local cache; nothing the user owns is at risk either way. **Two real changes**: (1) `public/sqlite/aquaai-worker.mjs`'s retry budget widened from 3 attempts/~2.5s total to 6 attempts/~10s total, so a second tab's lock has more realistic time to release before falling back — cuts down how often this triggers at all under normal single-tab-then-reload usage. (2) `DbBootProvider.tsx`'s banner rewritten from a red/urgent bar to a quiet neutral note with accurate copy ("catching up on the species list... everything of yours is safely stored on your account either way"), for both the "OPFS locked by another tab" and "browser doesn't support this" cases. Build/lint/`tsc --noEmit` clean. **Not verified live this session** — worth checking: open the app in two tabs at once and confirm the banner (if it still shows at all with the wider retry budget) now reads calm and accurate instead of alarming, and that the app is otherwise fully usable underneath it either way. |

| UI redesign Section 0 — full-app inventory audit (first section of the 2026-09-12 redesign strategy, `specs/REDESIGN-2026-09.md`) | Done — findings recorded in `specs/REDESIGN-2026-09.md` | 2026-09-12 | All 31 user-facing screens audited against the brief's Part 1 rules — one focused pass per screen from the TSX source (`dev/*` internal viewers excluded; per-screen CSS modules deferred to each section's on-contact check). Totals: 44 high / 77 medium / 37 low. Recurring patterns that shape Sections 1-2: hard-coded inline values (40 findings), i18n gaps concentrated in `login` (entirely untranslated), JournalPanel, GalleryPanel, PostCard and admin (28), sub-44px touch targets on small inline buttons (23), Card-as-default-container (15 — `Card.tsx`'s own doc comment admits "the default container for everything"), the unit-toggle/segmented control re-implemented inline on 5+ screens, raw native selects bypassing `Field` on 5 screens, and the My Tanks "Healthy" badge showing status without explanation (the brief's exact example). Component capabilities inventoried for Section 2 convergence: Button (3 variants, full-width only, no text tier), Field (input only, no select/textarea), Chip (display-only); ListRow/Status/SegmentedControl/choice-cards don't exist yet. No code changed — read-only section. |

| UI redesign Section 1 — design tokens rebased to the brief's dark-first system (second section of `specs/REDESIGN-2026-09.md`) | Done, deployed | 2026-09-12 | `src/theme/tokens.css`/`tokens.ts` rebased onto `docs/08-ui-redesign-brief.md`'s palette. **Dark is now the app's default look** — dark values moved onto `:root`, light lives under `prefers-color-scheme: light` + `[data-theme="light"]`, so the existing Appearance setting (light/dark/system) keeps working, exactly per the settled decision. Dark carries the brief's exact values (#0B1112 ground, #151D1E surface, #1C2627 elevated, #293637 line, #F3F6F5/#9BA9A8/#71807F ink scale, #35C6BE aqua as the ONE brand colour — accent and deep are the same aqua in dark, so primary buttons are solid aqua with dark text per the brief), and the brief's severity hues map onto the safety-critical fixNow/watch/improve scale (structure unchanged; light variants darkened for AA contrast on white). Light mode is derived, not specified by the brief: the validated 2026-09-10/11 neutrals with the accent family shifted onto the aqua hue (#0D7A73 text-grade / #0B6B65 fill-grade). Type scale rebased to the brief's (display 32, screen title 28, section 20, body 16, secondary 14, caption 12 — title and heading grew, everything else held), spacing to the 8-point scale (xxxl 48→40; nothing outside the tokens consumed it), radius to 12/16/20/pill — `Card.module.css` already read `--radius-lg` so standard cards landed on the brief's 16px with zero component edits, and Field/Button read `--radius-md` so inputs are 12px. New `--color-ink-faint` token for the brief's muted-text/placeholder tier. App-shell coherence in the same pass: `layout.tsx`'s theme-color metas and `manifest.json`'s background/theme colours moved to the dark values. `docs/04-design-system.md` updated (palette block, dark-default note, type/spacing/radius scales, Card radius fix). **Verified in a real browser** (fresh `npm run build` + restarted local server; a throwaway phone account was created for the signed-in screens and deleted afterwards through the real self-serve delete flow): every key screen (login, onboarding, My Tanks, Dex, Ask Aqua, Community, Settings) resolves all 12 tokens to the brief's exact hexes in dark; screen title renders 28px; the primary button is solid aqua `rgb(53,198,190)` at 12px radius; the Appearance toggle lands the exact derived light values and toggles back to dark; a light-OS fresh context gets light. Screenshots reviewed for breakage — none; the My Tanks zero-state's sparse middle is pre-existing layout that Section 3 rebuilds anyway. `npx tsc --noEmit` + `npm run build` clean; `npm run lint` fails only on the 4 pre-existing `react-hooks/refs` errors in `tank/[id]/livestock/page.tsx` (last touched by the i18n commit `68e5dc6` earlier that day, before this work — confirmed via `git log`, not from this change). |

## Not code, but gates the launch

| Item | Status | Notes |
|---|---|---|
| 15–18 Play testers recruited | Not started | 12 must stay opted in for 14 continuous days |
| Play Console account created ($25 / ~₹2,390) | Not started | |
| Safety corpus written | 0 / 62 | See `docs/05-content-guide.md` §2 |
| Tier 1 reviewer found (vet / aquaculture professional) | Not started | Longest lead time in the project. ~14 entries plus the treatment blocks of ~15 more |
| Tier 2 reviewer found (named experienced hobbyist) | Not started | |
| Seed species human-verified | 0 / 30 | All currently `verified: false`. Rises to 150 after T-003 |
| Final product name decided | Not started | Before the Play listing goes up — renaming after launch costs every review |
| Privacy policy published | Not started | Required by both stores |
| Reminders verified on a Xiaomi / OPPO / Vivo / Realme phone | Not started | The single biggest reliability risk for an India launch |

## Decisions log

Record anything that contradicts the docs, so the next session knows.

| Date | Decision | Reason |
|---|---|---|
| 2026-08-30 | Species ids are common-name slugs (`neon-tetra`), not scientific-name slugs | The seed data already used them and ids must be stable forever |
| 2026-08-30 | pH-ammonia trap thresholds fixed at ≥0.5 ppm (small pH-matched changes) and ≥5 ppm (move the fish) | Two sources gave 0.25 and 5 ppm; this two-stage rule reconciles them and is used in all documents |
| 2026-08-30 | Malachite green is **not** categorically unsafe for scaleless fish or invertebrates — dose strictly to label | The blanket ban was unsupported and pushed users toward salt and copper, which are more dangerous for those species |
| 2026-08-30 | All eight standard parameters are free; Pro sells custom parameters | A "5 parameters free" cap would have paywalled Nitrate, which is safety information |
| 2026-08-30 | Corpus review split into three tiers, Tier 1 (medication/dosing) expert-only | A vet-under-retainer requirement for 35 topics would have blocked launch indefinitely |
| 2026-08-30 | Corpus topic count is 62, not 60 | `ph-ammonia-trap` and `nitrogen-cycle` were split out as their own entries because they are cited independently |
| 2026-08-31 | `gemini-2.5-flash-lite` is retired for new API keys — the live API now returns 404 and points to `gemini-3.5-flash-lite` | Discovered by hitting a real 404 while building the T-001 harness. Model name updated everywhere in the docs and in `tank-scan-harness/server.js`. Pricing figures ($0.10/$0.40 per million tokens) were left as-is but are unverified for the new model — check `ai.google.dev/gemini-api/docs/pricing` before relying on them |
| 2026-08-31 | **Tank Scan no longer identifies livestock species.** It now reports equipment, setup type (bare-bottom/planted/aquascape/jungle/biotope), plants, algae and condition only. Livestock is added by hand right after the scan (T-016), with an optional single-fish photo-ID assist for anyone unsure (`species-id/v1`, new Contract 6) — candidates only, never auto-filled. | Directly driven by the T-002 evaluation above: equipment/algae/setup are reliable from a photo, schooling-fish species ID is not. Matches one of T-002's own listed fallback options ("require the user to confirm every detected species rather than presenting it as fact") taken a step further — species entry moved out of the AI path entirely, with AI as an optional assist. Updated: `docs/00-product-plan.md` §5, `docs/03-ai-contracts.md` Contract 1 + new Contract 6, `specs/T-015`, `specs/T-016`, `specs/T-013`, `docs/01-architecture.md`, and the `tank-scan-harness` prompt/schema/UI (verified live against a real photo — response now returns `setup` instead of `livestock`, no species named anywhere in the output). |
| 2026-09-01 | **Reminder due-times and titles are mirrored to the server (Redis), a deliberate narrow exception to "SQLite is the only source of truth"** | A server-side cron is the only reliable way to send a push notification while the phone is asleep, and it can't send one for a reminder it has never heard of. Jaideep chose this (over dropping reliable push entirely) when asked directly. Only a reminder's title, due time, recurrence rule, and target anonymous device id are ever mirrored — never tank data, species, measurements, or photos. See `src/server/push/store.ts` and specs/T-018. |
| 2026-09-01 | **The push-sending cron runs once daily (00:30 UTC / 06:00 IST), not hourly** | Vercel's free Hobby plan rejects any cron schedule that runs more than once a day — deploying with an hourly schedule failed outright. Jaideep chose to stay on the free plan rather than pay $20/mo for Vercel Pro. Real consequence: a reminder can arrive up to ~24 hours after its exact due time rather than close to it (it fires on whichever daily run first sees it as past-due). Revisit if reminder timing precision turns out to matter to real users — `vercel.json`'s `schedule` is the only thing that needs to change. |
| 2026-08-31 | **Pivoted from a native Expo/React Native Android app to a mobile-first web app (PWA)** | Jaideep wants the product on the web instead. New stack: Next.js + React 19, SQLite-WASM (OPFS) + Drizzle, Web Push instead of local notifications, Stripe instead of RevenueCat, Vercel deploy instead of EAS/Play Store. See `docs/01-architecture.md` and `docs/06-shipping.md` (both rewritten) and `CLAUDE.md`. No code existed yet, so this was a documentation-only pivot, not a migration. T-010/011/012/013/014/018/025 specs updated to match; T-019–024, 026 and the data model/AI contract docs are stack-agnostic and were not touched, but should be read with the new stack in mind. |
| 2026-09-01 | **No real accounts, even for a broader product vision Jaideep shared** — "Sign up with Google" replaced with an optional, fully local Name/City profile (a single-row `profile` table, no server, no OAuth) | Jaideep proposed an auth screen as part of a larger vision doc. This directly conflicts with `CLAUDE.md`'s "no account required" principle and the explicit "do not build accounts/cloud sync yet" list. Flagged to Jaideep, who chose to drop auth entirely for now rather than reverse the rule. See `specs/T-023-onboarding.md`'s profile addendum. |
| 2026-09-01 | **Community feature is a placeholder screen only, reached via a link, not a nav tab** | Also part of Jaideep's broader vision, also on the "do not build yet" list (moderation/support burden). Jaideep chose the placeholder-only option. Separately, his proposed 5-tab layout (Home, My Tanks, Dex, Community, Settings) had no slot for the already-built Ask AquaAI tab — resolved by moving Ask to a floating chat bubble on Home, freeing the 5th tab slot for the Community placeholder. Final tabs: Home, My Tanks, Dex, Community, Settings. |
| 2026-09-01 | **New task T-027 (Guided Tank Setup Planner) added, sequenced after T-015** | Fills in T-023's currently-unbuilt "I'm thinking about getting fish" path with a real deterministic equipment-recommendation engine (heater/lighting/filter/substrate sizing + a static city-climate lookup table). Given real domain risk (wrong equipment sizing), the spec was written first for Jaideep to sanity-check the formulas and city numbers before any code is built, and it's sequenced behind T-015 (the already-speced hero feature) rather than in parallel, to avoid context-switching risk on a solo-directed project. See `specs/T-027-guided-tank-setup.md`. |
| 2026-09-01 | **New task T-028 (Home / Cross-Tank Calendar) added; nav restructure to a real centered Home tab is deferred until it ships** | Jaideep clarified his 5-tab vision meant a genuinely different Home screen (a cross-tank agenda), not the My Tanks list relabeled — this had been under-specified in the earlier nav-restructure pass, which deliberately left Home as My Tanks for the time being. Chosen tab order once T-028 ships: Tanks, Dex, Home, Community, Settings (Jaideep's choice over two alternative orderings). See `specs/T-028-home-calendar.md`. |
| 2026-09-01 | **Scan quota temporarily raised from 2/month to 50/month** (`QUOTA_LIMITS.scan` in `src/server/ai/quota.ts`) | The real 2-scan/month quota got exhausted twice during T-015 testing before the accept-flow's navigation bug fix could be re-verified live. Jaideep asked to raise it so testing isn't blocked by the production limit. **Temporary — revert to 2 before launch.** `ask` (15/month) is untouched. This was superseded on 2026-09-02 by "Early Bird" mode below, which raises both to 100,000. |
| 2026-09-06 | **GitHub repo stays public; the 433 scraped species reference photos stay in it as-is, unlicensed-source risk accepted** | Jaideep flipped the repo to public himself and, when the photo-licensing gap was flagged, chose to accept the risk rather than pull the photos or move them out of git — revisit if a source site ever objects, or before any serious monetization push |
| 2026-09-06 | **Repo license is all-rights-reserved (proprietary), not MIT/open-source** | Jaideep wants to keep monetization open as a future option; an open-source license would have made that harder to walk back later |
| 2026-09-02 | **"Early Bird" mode: everything free and unlimited for now, no Stripe/Pro built yet** — real spec numbers (2 scans/15 questions per month) kept in code as `SPEC_QUOTA_LIMITS` but unused while `EARLY_BIRD_MODE = true` | Reviewed the T-025 spec with Jaideep before building. Rather than half-build a paywall with nothing to actually sell (no Stripe account exists yet), he chose to keep the pricing table as future reference and run the whole app free for everyone now. Also chose to leave T-017's custom parameters free rather than gate them retroactively. Flip `EARLY_BIRD_MODE` to `false` in `src/server/ai/quota.ts` when Pro actually ships — that's the entire revert. |
| 2026-09-06 | **T-027 rebuilt AI-first, overriding its own spec's "no AI anywhere in this flow" rule** | Jaideep rejected the deterministic-only planner mid-build ("who asked you to build this with no internet? I want this to be AI-first") and re-specified the flow himself: tank type, then fish-first wish list in the user's own words, then a small/medium/large size band instead of exact dimensions, then AI recommendations on top of the deterministic sizing engine (which stays as the instant baseline and the offline fallback). The spec file `specs/T-027-guided-tank-setup.md` still describes the old design — it is superseded by the 2026-09-06 progress entry, not silently rewritten. |
| 2026-09-06 | **Planner-approved sizing numbers** (the gate T-027's spec required before any code): heater = litres x (target - room) x 0.3 rounded UP to standard sizes (25/50/75/100/150/200/300/400W), chosen over the spec's rougher "1W/L per 5C" which runs ~25% small; filter = 4x/h turnover (same constant as `checkFilterFlow`); lighting 0.3/0.4/0.6/0.9 W/L by tier; substrate 5cm planted / 3cm hardscape / none; city-climate bands as proposed and approved. Jaideep also asked for a visible cold-winter note recommending the next heater size up when demand sits within 20% of the recommended band — implemented as a readable note, never a silent bump. |
| 2026-09-06 | **Wishlist fish are real livestock rows with `status: 'planned'`** (Jaideep's explicit choice over keeping them in notes) | They never count as living in the tank — every read path filters `status === 'alive'` — and the Fish tab shows them in a separate "On the wishlist" section with an "Arrived" button that flips them to real, counted fish the day they come home. `compute90DaySurvival` excludes planned rows so the survival metric can't be dragged down by fish never bought. |
| 2026-09-06 | **Planner UI decisions from live feedback** (all Jaideep's calls, shipped same day): tank size in **feet with Long/Cube shapes**, not S/M/L litres ("litres is not the standard way of measurement; tanks are generally cube or long"); filters recommended **by type** (sponge/hang-on-back/canister), never L/h; planted substrate named **aquasoil with brand examples**, not "nutrition rich"; advisor suggestion rows **clamped to 2 lines**; heater section shows the **city's full indoor temp range** (winter nights / summer) so the sizing number reads in context; typing a fish name shows **2-3 instant catalog suggestions**; suggested fish are **one-tap adds** with the wishlist holding only what the user chose (counts chosen after saving); the AI-loading state is a **prominent full-width dark card**; and **adding fish is additive** — the search page never exits per add. |
| 2026-09-10 | **Fish compatibility check has no AI in it at all, anywhere** — reversed same-day from an earlier same-day reversal (per-fish AI check → batch AI check on "Done" → no AI check) | The per-fish version interrupted the add flow; the batch version (built first, at Jaideep's own request) turned out confusing/unreliable in his own real use ("it just completely fucked it up") and he asked for AI out of this flow entirely. What's left — a derived Size/Temp/Parameters/Setup readout computed straight from species/tank data, no model call — is now the only compatibility signal anywhere in adding a fish. Do not reintroduce an AI compat check without him asking again. |
| 2026-09-10 | **Quota (`x-device-id`) still has no connection to the real accounts added earlier the same day** — flagged, not fixed | Found during the AI-cost audit: the free-tier quota system is keyed on a client-generated, self-reported header, unrelated to the account a user is actually signed into. Clearing browser storage resets it; a free/paid gate built on top of this today would be trivially bypassable. Needs to move to the account id before quota (free or Pro) means anything — should happen before Pro ever actually launches, not urgent while `EARLY_BIRD_MODE` stays on. **Fixed 2026-09-11** — see the matching progress entry above; quota now keys off `requireUserId()`, not `x-device-id`. |
| 2026-09-11 | **All 62 safety-corpus topics from `docs/05-content-guide.md` now written** — T-004's remaining 57-topic gap is closed. None marked `live` yet. | Wrote all 51 remaining entries (11 already existed from T-004: the 2 worked examples plus `ph-ammonia-trap`/`nitrogen-cycle`/`temperature-swings`) across the doc's 9 groups: A water chemistry emergencies (9/9), B nitrogen cycle (6/6), C parasitic/protozoan disease (7/7), D bacterial/fungal/other disease (8/8), E medications (13/13), F environmental risk (7/7), G equipment failure (5/5), H water changes/acclimation (3/3), I quarantine (3/3) — 62 total. Every entry follows the content guide's real schema depth: symptoms, likely causes, an ordered immediate-actions list, a do-not-do list with the reasoning spelled out (not just a bare warning), when-to-escalate, treatments with `dangerous_to` and a source where relevant, and real source URLs (mainly merckvetmanual.com, plus extension/aquariumscience.org where used in T-004's original two examples). `node scripts/validate-corpus.mjs` passes clean: 62 entries, 0 live. Full production build (`npm run build`) succeeds — all 62 `/corpus/<id>` pages prerender as static HTML. `npx tsc --noEmit` clean. `npm run lint` has 4 pre-existing errors in `src/app/tank/[id]/livestock/page.tsx` (a `react-hooks/refs` rule flagging a ref read during render) that predate this session and are untouched by it — confirmed via `git log`/`git diff` before treating this task as done. **What's not done, and why this still can't go live to real users today**: every entry is `status: sourced` with `last_reviewed_by` empty — the corpus's own safety gate (`docs/05-content-guide.md` §7.3, enforced by the validator) refuses to let an entry go `status: live` without a real named human reviewer, so retrieval (`retrieveCorpus()`) still correctly returns zero hits for every question right now, exactly as it should until real review happens. Two different review bars apply: **Tier 2/3 entries** (most of groups A/B/C(diagnosis)/D(diagnosis)/F/G/H/I — general disease recognition, environmental risk, equipment, routine care) can be reviewed and signed off by Jaideep himself as the named hobbyist reviewer. **Tier 1 entries — all 13 of group E (medications) plus every disease entry whose `treatments[]` block carries a real dose (velvet, flukes, internal worms, hexamita, invisible-protozoa, fin rot, columnaris, bacterial ulcers/septicaemia, mycobacteriosis, saprolegnia, ammonia-spike, nitrite-spike, chlorine/chloramine, plus `co2-overdose` and `prophylactic-quarantine-treatment`) — need a real vet or aquaculture professional's sign-off; Jaideep cannot self-clear these per the project's own hard safety rule, and no entry should be flipped to `live` without that. |
| 2026-09-11 | **32 Tier 2/3 corpus entries flipped to `status: live`, `last_reviewed_by: "Jaideep"`** — Jaideep reviewed the full set and confirmed it accurate. | 31 of the 32 are the Tier 2/3 entries written the same day (general disease recognition, environmental risk, equipment, water-change/acclimation, quarantine); the 32nd was `ph-ammonia-trap`/`nitrogen-cycle`/`temperature-swings` from T-004's original batch, also Tier 2 and previously unreviewed. `node scripts/validate-corpus.mjs` now reports 62 entries, 32 live. Retrieval (`retrieveCorpus()`) can now actually surface this content in Ask AquaAI and Emergency Triage for the first time. **The 29 Tier 1 entries (all of group E medications, plus every disease entry whose `treatments[]` carries a real dose) are untouched, still `status: sourced`, `last_reviewed_by` empty** — Jaideep's review does not cover these; they still need a real vet or aquaculture professional before they can go live. Build/typecheck clean; deployed to production. |
| 2026-09-12 | **Reversed the medication/dosing/disease hard-refusal — Jaideep's explicit, informed decision, flagged plainly before building it** | Jaideep asked that for medication-dose questions (the 29 Tier 1 entries not yet vet-reviewed), the AI answer anyway with a "confirm with a vet" note at the bottom, instead of refusing. I stopped and spelled out the real risk first (a wrong or made-up dose — e.g. copper is lethal to invertebrates even at a normal treatment dose — reaching someone's tank with only a disclaimer between them and it) via `AskUserQuestion`, offering a middle option (show only my own sourced-but-unreviewed corpus text, clearly marked unconfirmed) as a lower-risk alternative; Jaideep chose the fullest option — the AI may answer from its own general knowledge, not just my corpus text. Implemented as asked: `ask/v4` (`prompts/ask.v4.md`, was v3) and `triage/v2` (`prompts/triage.v2.md`, was v1) both add a `medical_disclaimer: boolean` field, set true whenever a medication/dosing/treatment claim isn't backed by a retrieved, vet-reviewed corpus entry — the existing {{SAFETY_RAILS}} rules (confirmed volume+inhabitants before any dose, water test before treatment, never stack medications, named species-sensitivity warnings) still apply to whatever the model generates. The UI (`ask/page.tsx`, `emergency/page.tsx`) shows a loud `fixNow`-severity banner — "This is an AI-generated answer about medication or treatment — it has not been reviewed by a vet or aquaculture professional. Please confirm with a vet before trying it." — distinct from the softer neutral `uncovered` badge already used for ordinary general-knowledge care facts. CLAUDE.md's AI rules section updated in the same session to record this as a deliberate, acknowledged safety tradeoff, not a silent regression. `npx tsc --noEmit` and `npm run build` clean. **Not yet done**: the corpus's Tier 1 entries themselves are still unreviewed and not live — this change only affects what happens when no live entry backs an answer; a real vet review is still the actual fix, this is a stopgap Jaideep asked for in the meantime. |
| 2026-09-12 | **Reversed the 2026-09-06 "planner fish are a wishlist" decision — fish picked in the Guided Setup Planner now save as real, alive, counted fish immediately, not `status: 'planned'`** | Jaideep hit the wishlist behaviour as a bug ("I built and saved a tank, but the fishes did not come under the fishes section") — the fix I built first (surfacing the existing wishlist section on the main Tank Info page too, since the fish genuinely were saved, just invisible there) wasn't what he wanted; he asked to drop the wishlist framing entirely and just add the fish for real on save. `src/app/onboarding/planner/page.tsx`'s `addLivestock` call now passes `status: "alive"` instead of `"planned"`. The tank's own `status: "planned"` field (a different concept — whether the *tank* is physically set up yet) is untouched. **Flagging a real gap, not silently left**: the wishlist machinery this reverses (`listPlannedLivestockForTank`, `markLivestockArrived`, the "On the wishlist" section and `PlannedLivestockRow` component on `/tank/[id]/livestock`, `compute90DaySurvival`'s planned-row exclusion) is now orphaned — nothing in the app creates a `status: 'planned'` livestock row anymore, so that code paths are dead until/unless something else uses them again. Left in place rather than deleted, since removing working infrastructure wasn't asked for — worth a cleanup pass if it's confirmed nothing will ever need it again. |
| 2026-09-12 | **UI/UX redesign strategy adopted — the consolidated brief is saved as `docs/08-ui-redesign-brief.md` and the execution plan as `specs/REDESIGN-2026-09.md`; three scoping decisions settled with Jaideep before any code was written** | (1) **Themes: both, dark default** — the brief's dark palette becomes the app's default look AND a matching light mode is derived from it, so the existing Appearance setting (light/dark/system) keeps working; nothing shipped is removed. (2) **Nav label: "My Tanks" stays** — the brief proposed renaming the tab to "Tanks"; Jaideep's 2026-09-10 wording wins (the brief doc records the proposal verbatim; this entry records the supersession). The other renames (Dex → Discover, My Profile → Profile) proceed as the brief specifies. (3) **Localization: readiness only** — layouts flex for longer text and locale-aware dates/units ship in the redesign, but no new languages (German, Arabic/RTL, Thai, Japanese) are added; those are a separate future task. Work proceeds section by section per `specs/REDESIGN-2026-09.md` (10 sections, foundation-first), each ending with the full build/verify/deploy/log loop. |
| 2026-09-12 | **Redesign Section 1 (design tokens) done** — `tokens.css`/`tokens.ts` rebased onto the brief's palette; dark is now the app's default look (dark values on `:root`, light under `prefers-color-scheme: light` + `[data-theme="light"]`), type scale/spacing/radius all rebased to the brief's numbers. | Deployed to production. See `specs/REDESIGN-2026-09.md` Section 1 for the full verification record (real-browser pass with a throwaway account, both themes, no breakage). |
| 2026-09-13 | **Redesign Section 2 (shared components) done** — `Button` converged into a real 3-tier system (primary/secondary/new `TextButton` tertiary) plus a `fullWidth` opt-out; `Field` gained `SelectField`/`TextAreaField` siblings; four new primitives shipped: `ListRow`, `Status` (fixes the brief's own "never have '● Healthy' with no explanation" example — `explanation` is a required prop, not optional), `SegmentedControl` (replaces 5 inline-reimplemented toggle patterns the Section 0 audit found), `ChoiceCard`. All six demoed in `/dev/components` and piloted for real on Settings (language/Appearance toggles are now `SegmentedControl`, replacing hand-rolled buttons with a hardcoded `#fff` active-text color). | Two real bugs found and fixed during actual browser verification, not just written-and-assumed-correct: (1) `Status`'s dot and label shared the same CSS variant classes, so the label's background-color got set to the same hue as its own text color, rendering it invisible behind a solid color block — caught by looking at a real screenshot. (2) `/dev/components`'s own light/dark preview toggle set `data-theme` on an inner `<div>`, but the token system's light override only matches `:root[data-theme="light"]` (the `<html>` element, set by the real `ThemeProvider`) — the preview's "Light" button had silently never worked, a pre-existing bug unrelated to Section 2 but caught while verifying it. Both fixed. `npx tsc --noEmit`/`npm run build` clean, lint unchanged (same 4 pre-existing errors). Deployed to production. Screens are NOT migrated onto the new primitives yet beyond the Settings pilot — that's each later section's own job. Next: Section 3 (Navigation + My Tanks). |
| 2026-09-13 | **Redesign Section 3 (Navigation + My Tanks) done** — tab bar relabeled (Dex→Discover, My Profile→Profile, "My Tanks" kept per the earlier decision); My Tanks home rebuilt per the brief: small plain-text greeting (no more photo-background-behind-the-header, the brief's own named complaint), each tank card's photo bumped to 180px as the real hero, a real `Status` component replacing the bare "● Healthy" pill (now always paired with an honest explanation — fish count or "add your first fish," never asserted health with nothing behind it), actions consolidated into one row (Add tank / Help me build a tank / Ask Aqua), Emergency demoted from a loud full-width red link to a quiet `ListRow`. | Verified in a real browser: signed up a throwaway phone+PIN account, created a real tank, confirmed the zero-fish honest status text, both themes, the tab bar renames, and the Section 2 SegmentedControl pilot on Settings still works. **Cleaned up afterward**: local dev shares the production Postgres database, so deleted the throwaway test account via the real self-serve delete flow (incidentally re-verifying that flow too) — no test data left in production. `npx tsc --noEmit`/`npm run build` clean. Deployed to production. Flagging for Section 4: Tank Detail already reads as substantially brief-compliant on inspection (large photo, Size/Volume/Temp metrics, Health Check as the primary action, Fish/Gallery/Journal as list rows) — Section 4 should confirm/tighten rather than assume a rebuild from scratch. Next: Section 4 (Tank Detail). |
| 2026-09-13 | **Redesign Section 4 (Tank Detail) done** — header now pairs the tank name/water-type with a real `Status` (icon + label + a required, honestly-derived explanation: no fish → neutral, an open equipment warning from `checkFilterFlow`/`checkHeaterWattage` → watch, else → improve with a fish-count explanation), replacing the old hardcoded "● Healthy" text with no data behind it; `AboutSection` now leads with Volume and Temperature as the two primary metrics, with dimensions/creation-date demoted to a small fine-print line; a new Water card (pH/Ammonia/Nitrite/Nitrate, latest value + ✓/⚠ against each parameter's target range, or "Not logged yet") was added and wrapped in a link to `/tank/[id]/log` — which incidentally fixes a real pre-existing bug where that logging page had no link anywhere in the app; the three heavy inline Fish/Gallery/Journal accordions were replaced with three simple `ListRow`s that navigate to the existing full dedicated pages (confirmed those pages already have complete add/search/photo flows of their own, so nothing was lost). New i18n keys added in both `en.ts`/`hi-latn.ts` for the new status-with-equipment-warning copy and the Water card's labels. | Verified in a real browser on a throwaway phone+PIN account: zero-fish state showed the honest "No fish yet" status and all-four-rows "Not logged yet" Water card; added a real Betta via the still-fully-functional livestock search flow and confirmed status flipped to "Healthy · 1 fish · nothing flagged" with the Temp metric showing the species' real range; logged Ammonia = 2 (out of range) and pH = 7 (in range) via the newly-linked log page and confirmed the Water card updated to **pH 7.0 ✓** / **Ammonia 2.00 ⚠**. **Cleaned up afterward**: same shared production Postgres as prior sections, so deleted the throwaway test account via the real self-serve delete flow — no test data left in production. `npx tsc --noEmit`/`npm run build` clean, lint unchanged (same 4 pre-existing errors, untouched). Deployed to production. Next: Section 5 (Onboarding + tank creation + units). |
| 2026-09-13 | **Section 4 amended same day: Fish/Gallery/Journal accordions reverted to their original inline design; Water card hidden entirely (not shown with "Not logged yet" placeholder rows) until a real reading exists** | Jaideep reviewed the shipped Section 4 and said "revert the changes made to the following sections on tank page: Fish, Gallery, Journal. I liked them the way they were before" — the `CollapsedSectionCard`/`StackedThumbs`/`LivestockInlineRow` accordions plus the add-fish popup, `GalleryPanel`, `JournalPanel` are back exactly as they were pre-Section-4, restored from the `68e5dc6` commit. Separately asked "remove the water section if nothing is logged. no placeholders" — the Water card (still new from Section 4) now only renders once at least one of pH/Ammonia/Nitrite/Nitrate has a real logged value; the never-been-logged case shows nothing at all rather than four "Not logged yet" rows, and the now-unused `tankOverviewPage.notLogged` i18n key was removed from both languages. The Status/AboutSection changes from the original Section 4 build stand unchanged. `npx tsc --noEmit`/`npm run build`/`npm run lint` all still clean (same 4 pre-existing lint errors). Verified live: zero-fish tank shows no Water card and the Fish/Gallery/Journal accordions expand inline exactly as before. Deployed to production. |
| 2026-09-13 | **Section 4 amended again same day: dropped the redundant "nothing flagged" wording from the healthy status** | Jaideep: "If nothing is flagged. Why write 'nothing flagged'. Just dont show it na" — `home.statusOkExplanation` (shared by both My Tanks and Tank Detail's `Status` component) changed from `"{n} fish · nothing flagged"` to plain `"{n} fish"` in both languages. `npx tsc --noEmit`/`npm run build` clean. Deployed to production. |
| 2026-09-13 | **Redesign Section 5 (Onboarding + tank creation + units) done** — Onboarding trimmed to Welcome → name only (city and language/theme toggles removed, both already live in Settings), Get Started now routes straight into the retitled "Show us your aquarium" scan flow instead of an empty My Tanks list, with a new "Skip for now" escape and a "Set up manually instead" link on the scan screen (routes to the plain Add Tank form) so nobody photo-averse gets stuck. New app-wide units system: `src/lib/units.ts` formatters + `src/lib/UnitsProvider.tsx` (a `localStorage`-backed Metric/Imperial preference, same pattern as theme/locale), a new "Units" toggle in Settings, and Tank Detail's Volume/Temp/dimensions now read through it — switching the preference changes those numbers immediately, both directions (cm↔in, L↔gal, °C↔°F). Add Tank rebuilt: `SegmentedControl` for the cm/ft toggle, `ChoiceCard`s for water type, City/age-band moved into a collapsed "More details" section, and the live volume preview on every dimension-entry screen (Add Tank, Tank Size, the onboarding scan flow) now shows the brief's own dual format ("≈ 54 L · 14.3 gal") instead of a metric-only number. Planner's pre-plan steps 1–3 gained a "Step N of 3" indicator and real `ChoiceCard`s for tank-type/shape, with steps 1–2's footer CTA simplified to plain "Continue". | This supersedes the 2026-09-04/05 profile-first onboarding (name+city+language+theme, landing on My Tanks) — decisions-log entry. Verified live end to end on a throwaway account: simplified Welcome screen, the manual-setup escape hatch, the dual-unit volume line, both `ChoiceCard` pickers, "More details" correctly holding City/age-band, a saved tank reading `54 L`/`60×30×30 cm` in Metric and `14.3 gal`/`24×12×12 in` after flipping to Imperial in Settings, and all three Planner steps' new styling. **Cleaned up afterward**: deleted the throwaway test account via the real self-serve delete flow, same shared-production-Postgres pattern as every prior section. `npx tsc --noEmit`/`npm run build`/`npm run lint` all clean (lint unchanged: same 4 pre-existing errors). Deployed to production. **Left out of scope, flagged not hidden**: per-parameter units (the Water card's ppm/pH readings, log-entry input units) are untouched — only length/volume/temperature go through the new units system. Next: Section 6 (Discover). |
| 2026-09-13 | **Tank Detail now renders the floating bottom tab bar** | Jaideep: "when i saved a tank, the bottom nav should appear on the page. otherwise I have no option to go back to home since back buttons leads to last page" — right after saving a new tank the flow (Add Tank → Health Check → Tank Detail) is several `router.replace` hops deep, so the header's back arrow doesn't reliably lead back to My Tanks. Tank Detail (`src/app/tank/[id]/page.tsx`) sits outside the `(tabs)` route group (a pushed detail screen, not a tab destination) so it never got the `TabBar` other screens have, even though `Screen`'s own CSS already reserves bottom clearance for it on every page. Rendered `<TabBar />` directly on Tank Detail so there's always a guaranteed way to any tab (especially My Tanks) regardless of how tangled the back-stack got. `npx tsc --noEmit`/`npm run build`/`npm run lint` clean. Verified live: saved a tank, landed on Tank Detail, confirmed the tab bar is visible and tapping Profile/My Tanks navigates correctly. Deployed to production. |
| 2026-09-13 | **My Tanks: replaced the time-of-day greeting with a plain "Welcome, {name}"; the tank card's "⋮" quick-actions bubble now renders three explicit dots** | Jaideep: "The app says good morning. But its night. I think we should just say Welcome '[name]' rather than a greeting" — the `greeting()` helper (`src/app/(tabs)/page.tsx`) picked Good morning/afternoon/evening off `new Date().getHours()`, which was reading the wrong clock and showing "Good morning" at night; removed entirely in favour of a fixed `t.home.welcome` string ("Welcome,"), same layout otherwise. Separately: "the white bubble on tank images in My Tanks section doesnt convey what it does. Add 3 dots to it" — the per-card "⋮" button (top-right of each tank photo) rendered the single vertical-ellipsis character, which some fonts draw so small/blurred it doesn't read as a control; replaced with three explicit 3px dot spans so it always reads as "more actions" regardless of font rendering. `npx tsc --noEmit`/`npm run build`/`npm run lint` clean (same 4 pre-existing lint errors). Verified live: header now reads "Welcome, Jaideep 👋" regardless of time of day, and the tank card's top-right bubble clearly shows three dots when zoomed in. Deployed to production. |
